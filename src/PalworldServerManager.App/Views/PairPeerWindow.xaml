<Window x:Class="PalworldServerManager.App.Views.PairPeerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pair Palworld Server Manager" Height="230" Width="430"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PeerText" FontSize="18" FontWeight="SemiBold"/>
        <TextBlock Grid.Row="1" Margin="0,10,0,6" TextWrapping="Wrap"
                   Text="On the destination Manager, open LAN &amp; Transfers and click Generate Pairing Code. Enter the six-digit code here."/>
        <TextBox Grid.Row="2" x:Name="CodeBox" FontSize="22" MaxLength="6" HorizontalContentAlignment="Center"/>
        <TextBlock Grid.Row="3" x:Name="StatusText" Margin="0,8,0,0" TextWrapping="Wrap"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" IsCancel="True"/>
            <Button x:Name="PairButton" Content="Pair" Click="Pair_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
