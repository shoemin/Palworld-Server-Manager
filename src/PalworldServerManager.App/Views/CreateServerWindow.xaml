<Window x:Class="PalworldServerManager.App.Views.CreateServerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create New Palworld Server" Height="330" Width="500" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Create New Server" FontSize="22" FontWeight="SemiBold" Margin="4,0,4,14"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Text="Server name" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" x:Name="NameBox" Text="Palworld Server"/>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Text="Game port" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" x:Name="GamePortBox" Text="8211"/>
        </Grid>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Text="REST API port" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" x:Name="RestPortBox" Text="8212"/>
        </Grid>
        <TextBlock Grid.Row="4" Margin="4,14,4,4" TextWrapping="Wrap"
                   Text="A dedicated Steam runtime will be installed into an isolated manager-owned directory. REST administration and Palworld's built-in save backups are enabled for new managed servers."/>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" IsCancel="True"/>
            <Button Content="Create" IsDefault="True" Click="Create_Click"/>
        </StackPanel>
    </Grid>
</Window>
