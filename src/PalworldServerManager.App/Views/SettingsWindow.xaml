<Window x:Class="PalworldServerManager.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Server Settings" Height="720" Width="1120" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="TitleText" FontSize="22" FontWeight="SemiBold" Margin="4,0,4,8"/>
        <GroupBox Grid.Row="1" Header="Managed profile / launch settings" Margin="0,0,0,8">
            <Grid Margin="8">
                <Grid.ColumnDefinitions><ColumnDefinition Width="110"/><ColumnDefinition Width="220"/><ColumnDefinition Width="90"/><ColumnDefinition Width="100"/><ColumnDefinition Width="140"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <TextBlock Text="Profile name" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" x:Name="ProfileNameBox"/>
                <TextBlock Grid.Column="2" Text="Game port" VerticalAlignment="Center"/>
                <TextBox Grid.Column="3" x:Name="GamePortBox"/>
                <TextBlock Grid.Column="4" Text="Extra launch args" VerticalAlignment="Center"/>
                <TextBox Grid.Column="5" x:Name="LaunchArgsBox"/>
            </Grid>
        </GroupBox>
        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="230"/></Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" TextChanged="SearchBox_TextChanged" ToolTip="Search setting names and descriptions"/>
            <ComboBox Grid.Column="1" x:Name="CategoryBox" Margin="4" SelectionChanged="CategoryBox_SelectionChanged"/>
        </Grid>
        <DataGrid Grid.Row="3" x:Name="SettingsGrid" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Setting" Binding="{Binding Key}" IsReadOnly="True" Width="260"/>
                <DataGridTextColumn Header="Value" Binding="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="260"/>
                <DataGridTextColumn Header="Default" Binding="{Binding DefaultValue}" IsReadOnly="True" Width="220"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" IsReadOnly="True" Width="150"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" IsReadOnly="True" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock Grid.Row="4" Margin="4,8" TextWrapping="Wrap"
                   Text="All values are shown in Palworld's native OptionSettings syntax. Strings remain quoted, booleans use True/False, and compound values such as CrossplayPlatforms remain parenthesized. Unknown settings are preserved and shown under Advanced / Unknown."/>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Reset Selected to Default" Click="ResetSelected_Click"/>
            <Button Content="Open Raw INI" Click="OpenRaw_Click"/>
            <Button Content="Reload" Click="Reload_Click"/>
            <Button Content="Save" Click="Save_Click"/>
            <Button Content="Close" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
