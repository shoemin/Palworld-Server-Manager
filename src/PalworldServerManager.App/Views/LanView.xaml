<UserControl x:Class="PalworldServerManager.App.Views.LanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="820" MinHeight="520">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <DockPanel>
            <TextBlock Text="LAN &amp; Transfers" FontSize="24" FontWeight="SemiBold"/>
            <Button DockPanel.Dock="Right" Content="Refresh" Click="Refresh_Click"/>
            <Button DockPanel.Dock="Right" Content="LAN Help" Click="OpenLanHelp_Click" Margin="0,0,8,0"/>
        </DockPanel>
        <Border Grid.Row="1" BorderBrush="#B0B0B0" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,10,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock x:Name="ThisComputerText" FontWeight="SemiBold"/>
                    <TextBlock x:Name="LanStatusText" Margin="0,4,0,0"/>
                    <TextBlock Text="LAN access is intended only for trusted local networks. Do not port-forward the Manager LAN API."
                               TextWrapping="Wrap" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="ToggleLanButton" Content="Enable LAN" Click="ToggleLan_Click"/>
                    <Button Content="Generate Pairing Code" Click="GenerateCode_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <GroupBox Grid.Row="2" Header="Nearby Palworld Server Managers">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <DataGrid x:Name="PeersGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Computer" Binding="{Binding MachineName}" Width="2*"/>
                        <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*"/>
                        <DataGridTextColumn Header="Manager" Binding="{Binding ManagerVersion}" Width="*"/>
                        <DataGridCheckBoxColumn Header="Paired" Binding="{Binding IsPaired}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Content="Pair Selected" Click="PairSelected_Click"/>
                    <Button Content="Unpair Selected" Click="UnpairSelected_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <TextBlock Grid.Row="3" x:Name="PairingCodeText" Margin="0,10,0,10" FontSize="16" FontWeight="SemiBold"/>

        <GroupBox Grid.Row="4" Header="Incoming Server Transfers">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <DataGrid x:Name="OffersGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Server" Binding="{Binding ServerName}" Width="2*"/>
                        <DataGridTextColumn Header="From" Binding="{Binding SourceMachine}" Width="*"/>
                        <DataGridTextColumn Header="Size (bytes)" Binding="{Binding SizeBytes}" Width="120"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110"/>
                        <DataGridTextColumn Header="Received File" Binding="{Binding ReceivedPath}" Width="2*"/>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Content="Accept" Click="Accept_Click"/>
                    <Button Content="Reject" Click="Reject_Click"/>
                    <Button Content="Import Received Package" Click="ImportReceived_Click"/>
                    <Button Content="Open Incoming Folder" Click="OpenIncoming_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
