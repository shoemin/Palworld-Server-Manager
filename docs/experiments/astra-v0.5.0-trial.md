# Astra v0.5.0 development trial

## Review model — Product Owner update, 2026-09-05

GitHub Codex external review was intentionally removed from the Astra lane by Product Owner decision, effective immediately during #41. Astra now owns implementation and two distinct substantive reviews: Pass A before PR creation and Pass B against the exact final PR HEAD. No further Codex requests, waiting, review-surface inspection, reviewed-SHA requirement, or review-attempt merge gate applies. Normal-lane process is unchanged. Scope, invariants, product-decision boundaries, actual field evidence, CI, frozen baseline, and experiment-only merges remain mandatory.

Normal and Astra review models differ; benchmark comparisons must account for this. Historical exception: before this update, #41 received one external finding already read and under correction. That evidence is retained below and is not relabeled as an Astra finding. The revised no-external-review model applies from this update forward; claiming the entire earlier history contained no external review would be inaccurate.

Each completed item records Pass A/Pass B findings, test-discovered defects, review-only defects, correction counts, and any later escaped defect. Final reporting uses **Astra review effectiveness** and identifies this workflow transition explicitly.


## Baseline and isolation

- Repository: `shoemin/Palworld-Server-Manager`.
- Frozen baseline: `97e99e2bc826a1e553064d99b3e6d87ddc696920`, accepted through #40.
- Trial started: 2026-09-05, in a previously empty dedicated workspace.
- Integration branch: `experiment/astra-v0.5.0`, created and pushed at exactly the baseline.
- No normal-lane implementation branch or PR was read. Only the exact baseline was fetched.
- Canonical Issues and Project are read-only inputs; all progress below is shadow state.
- No real release, tag, package publication, or main merge is authorized.
- Input snapshots: local `.git/astra-trial-inputs/issues.json` and `project.json`; these contain issue bodies and Project metadata, not PR implementations or comments.

## Trial start planning discrepancy

The live Project marks #41 **Product Decision**, while the current issue body says **Ready**, reconciled 2026-09-04, and lists all dependencies as satisfied. The trial instruction permits shadow execution of canonical Backlog work but explicitly preserves Product Decision stop conditions. Clarification has been requested without inspecting the normal-lane PR or its review findings. Resolved by explicit Product Owner reply: use the current #41 issue body for this isolated trial. Canonical Project remains unchanged.

## Shadow dependency graph

Accepted baseline inputs: #17, #19, #33, #39, #40. Existing acceptance is inherited, not claimed as new trial validation.

| Items | Dependencies beyond accepted baseline | Initial shadow state |
|---|---|---|
| #41 Windows platform | Complete in experiment; PR #61 | SHADOW DONE |
| #34 Design system/shell | None; readiness audit needed | NOT REACHED |
| #43 Protocol/contracts | None; readiness audit needed | NOT REACHED |
| #35, #36, #37 Design surfaces | #34 | NOT REACHED |
| #38 Final design | #34–#37 | NOT REACHED |
| #42 Local IPC/identity | #41 | SHADOW BLOCKED |
| #27 Host secure storage | #41 | SHADOW BLOCKED |
| #44 Remote transport/pairing | #42, #43, #27 | SHADOW BLOCKED |
| #45 Authorization | #42, #43, #44 | SHADOW BLOCKED |
| #28 Revocation | #27, #44, #45 | SHADOW BLOCKED |
| #46 Durable operations | #43, #45 | SHADOW BLOCKED |
| #47 Semantic settings | #43, #46 | SHADOW BLOCKED |
| #48 Server lifecycle | #41, #42, #43, #46 | SHADOW BLOCKED |
| #49 Server data | #46, #48 | SHADOW BLOCKED |
| #50 Observability | #43, #48 | SHADOW BLOCKED |
| #51 Migration | #42, #45, #27, #28 | SHADOW BLOCKED |
| #52 Avalonia shell | #38, #41, #42, #43 | SHADOW BLOCKED |
| #53 Avalonia server workspace | #35, #36, #47–#50, #52 | SHADOW BLOCKED |
| #54 Avalonia Manager Settings | #37, #38, #44–#46, #27, #28, #52 | SHADOW BLOCKED |
| #55 Windows parity | All required design/implementation children | SHADOW BLOCKED |
| #21 Linux Epic | #55; bounded decomposition after gate only | NOT REACHED |
| #22 Packaging | #20, #21; bounded decomposition required | NOT REACHED |
| #23, #24, #25 Qualification/docs/release | Final platform and validation gates | NOT REACHED |
| #16, #18, #20 | Containers only, never executed wholesale | NOT REACHED |

## #41 preflight

Canonical issue: **#41 — Windows platform slice — Host service lifecycle, activation, and client platform services**, parent #20, milestone v0.5.0.

- Dependencies #33/#19/#39/#40 are accepted at the baseline.
- Expected production files/layers: Host composition root; Platform.Contracts/Windows; Client.Platform.Contracts/Windows. SelfTest references/target and explicit integration harness/CI are authorized test changes.
- No production project-reference topology change is permitted. App/Core/Lan remain frozen for this issue.
- Authority paths to inspect: service provisioning, service DACL, query/start client path, privileged lifecycle/boot configuration, Host-data directory ACL, startup mutex acquisition, SQLite open/migration, deterministic stop cleanup.
- Credential holders: per-user DPAPI store only, shared by ordinary clients; injected key generator with no production signature choice. Host machine secrets remain #27.
- Persistence writers: #40 HostDatabase under #40 HostExclusivityLock only; per-user client credential storage is separate from authoritative Host state.
- Operation executors: SCM lifecycle and bounded client shell launch only. No server operations, IPC/authentication, Owner issuance, Linux, packaging-layout choice, or idle policy.
- Cross-section audit: group membership grants transport/start eligibility only; never Owner or enrollment; service start is not Host authentication; uninstall cannot remove authoritative data; boot-start and login-start remain separate.
- Applicable invariants: ARCH-001/002, HOST-001/002, PERSIST-001, CLIENT-001/002/003, LOCAL-002/003/004, OWNER-002, SEC-001, PLATFORM-001/002, LINUX-001. Full registry read; implementation audit not yet performed.
- Environment: current token is medium integrity, Administrators SID deny-only. Privileged integration cannot be reported as passed locally under this token. Mandatory actual service/multi-user evidence remains required.
- Branch: astra/41-windows-platform; implementation result: see stage-1 record below.
- Tests: pending baseline run; no #41 tests exist yet.
- GitHub Codex: not requested; no reviewed implementation HEAD.
- Allowed Decisions made: none yet.
- Product Decisions: resolve Project/body discrepancy without importing normal-lane work.
- Field-test requirements: all 15 issue-specified real Windows integration criteria remain unfulfilled.
- Deviations: normal workflow review/wait/Project-mutation steps replaced only as explicitly authorized by the trial request.
- Final result: pending.

## #41 implementation and stage-1 audit

Branch: `astra/41-windows-platform`, from integration commit `0ccc6af`.
Product Owner explicitly authorized using the current #41 body despite the normal Project's Product Decision label.

Implemented bounded Host/client contracts, native SCM and local-group provisioning, virtual-account service lifetime, protected machine root, #40 runtime composition, query/start-only client activation, test-injected HKCU startup and Explorer integration, and atomic versioned CurrentUser-DPAPI client credentials with an injected generator. No production generator is supplied. SelfTest now targets Windows with authorized test-only references; the production reference graph/allowlist is unchanged.

Allowed decisions: stable service `PalworldServerManagerHost`, group `PalworldServerManagerUsers`; compact activation enum; version 1 encrypted JSON payload; caller-supplied test path/write seam; file-sharing lock serializes same-user clients; real logon helper in SelfTest; unique integration service/group/user names. The running Host never provisions itself. Existing registrations are not silently adopted. Groups and authoritative data are retained at service uninstall.

Self-review found and corrected an existing-state ACL edge in the new provisioner: securing a root alone does not remove explicit permissions from old child files/protected directories. The implementation rejects unsafe explicit child access and reparse paths before permitting the new service to start. A build found an ambiguous test-harness TimeoutException name; qualified it. These are Astra/test findings, not GitHub Codex findings.

| Invariant | Why affected | Evidence/check | Result |
|---|---|---|---|
| ARCH-001 | Legacy WPF boundary | No App/Core/Lan edits | PASS |
| ARCH-002 | Legacy LAN isolation | No new production references; existing architecture guards | PASS |
| HOST-001 | Machine runtime | #40 mutex held by service runtime, second runtime rejected | PASS local; SCM field pending |
| HOST-002 | Mutation executor | Service work Host-side; client shell is bounded local action only | PASS |
| PERSIST-001 | Writer lifetime | Acquire before DB open/migration; clear pool/dispose before mutex release | PASS local; service field pending |
| CLIENT-001 | Client topology | Core-independent client contracts/implementation; production guard unchanged | PASS |
| CLIENT-002 | Machine secrets | No client Host-key API | PASS |
| CLIENT-003 | Shared per-user identity | One per-user DPAPI file shared by both clients, no Host.Cli change | PASS storage; IPC deferred #42 |
| LOCAL-002 | Private-key holder | Generator/storage client-side only | PASS |
| LOCAL-003 | Group eligibility | No auto-membership, enrollment, reactivation, or grants | PASS code; real-token field pending |
| LOCAL-004 | Activation boundary | Query/start result is not authentication; no IPC/credential transmission implemented | PASS scope |
| OWNER-002 | Bootstrap authority | Handoff contract only; no bootstrap execution or Owner creation | PASS scope |
| SEC-001 | At-rest secrets | DPAPI CurrentUser over entire payload; atomic writes; no secret logging | PASS same-user; cross-user field pending |
| PLATFORM-001 | OS seams | Windows APIs confined to Windows implementations/composition and tests | PASS |
| PLATFORM-002 | Interactive actions | Explorer client-only with injected launcher and local-path validation | PASS |
| LINUX-001 | Windows-first gate | No Linux implementation | PASS |
| IDENT-001–004, LOCAL-001, OWNER-001 | Identity/recovery boundaries | No changes to HostIdentity/authentication/recovery | Unaffected |
| REMOTE-001–002, PAIR-001–004 | Remote trust/authority | No remote implementation or routes | Unaffected |
| AUTH-001–005, PROTO-001 | Grants/protocol | No grant or protocol implementation | Unaffected |
| OPS-001–004, RECOVERY-001, MIG-001 | Operations/migration | No business operations, recovery, migration changes | Unaffected |

Baseline validation: 161/161 self-tests. Final stage-1 local suite: 169/169, including all #39/#40 tests, after the ACL and cleanup audit corrections. Release build succeeds. Strict docs build succeeds. Explicit integration invocation fails at the privilege gate under this medium-integrity token before creating OS objects: **FIELD EVIDENCE REQUIRED / privileged integration pending mandatory PR CI**. This is not an integration PASS. CI now has a dedicated explicit integration job, as #41 requires; trigger branches remain unchanged. No remote run or Codex result yet.

Acceptance mapping: service runtime/account/start-type/DACL/root ACL/boot toggles/uninstall/path quoting are covered by provisioner plus the explicit SCM harness; non-admin eligibility and cross-user DPAPI are covered by actual-logon probes; local activation/credential lifecycle/quoting/runtime tests pass; #39/#40 checks remain in the full suite. All real Windows criteria require a successful integration run before Shadow Done.

Stale-reference search checked Host scaffold wording, package versions, production project references, service/account/DPAPI scopes, shell launch placement, and issue-closing phrases. No production topology change, Host-side shell, production signature selection, Linux, IPC, or frozen source edit was introduced.

## #41 external review round 1

PR #61 targets the experiment branch. Reviewed SHA `d2ecae8f6ed43c7448801710479ce7eaa0e7b4de`. All four review surfaces checked. Two requests were made after the initial five-minute window; GitHub Codex returned review `5122646739` and inline finding `3941740363` / thread `PRRT_kwDOT8td0c6fmFRi`.

Valid P1 finding credited to `@chatgpt-codex-connector[bot]`: protected descendants containing Administrator/SYSTEM-only ACLs or a service deny ACE passed the existing-state check but prevented service startup. Correction requires protected descendants to grant the service full, non-inherit-only access; deny ACLs fail closed instead of guessing group effective access. Related self-review also rejects unprivileged child owners, who can change DACLs despite lacking an explicit data-write ACE. A dedicated regression test covers protected administrator-only state, service/group denies, outsider ownership/access, and usable protected/inheritable layouts.

Original exact-head remote evidence: CI workflow run `33985777171` PASS (169/169 plus real service/multi-user integration and successful service/group/user/profile/file cleanup); Docs run `33985778847` PASS. These establish actual virtual-account identity, DACL, boot toggles, database/lock lifecycle, non-admin rights, cross-user DPAPI, and uninstall preservation. They are pre-correction evidence; fresh runs and a fresh invariant audit are required for the correction HEAD.

Full registry re-audit of the correction: same matrix above; changes affect HOST-001/PERSIST-001/SEC-001 provisioning access and preserve the three-identity Host-state boundary. No other authority creation/executor/credential path was added. Frozen source and production dependency graph remain unchanged. No new Product Decision. External re-review was subsequently removed by Product Owner authorization; correction proceeds through Astra Pass B.

## #41 Review Pass B under revised workflow

Fresh final-diff review examined all changed files against integration `0ccc6af`, including native handle/error ownership, SCM access delegation order, runtime resource cleanup, encrypted credential concurrency, path quoting, negative tests, actual CI evidence, and privileged helper cleanup. One additional Astra-only security finding: the provisioner delegated SERVICE_START before securing the data root, allowing an existing activation-group member to race installation. Corrected by completing Host-state protection before publishing the activation DACL. Default SCM permissions do not delegate start to that group. This is a Pass B finding, independent of the earlier external protected-descendant finding.

Pass A findings: explicit permissions on existing descendants could survive root-only protection; integration group cleanup needed proof of creation. Pass B findings: protected descendants' service usability/owner/deny checks (continuation of the pre-policy external finding plus related Astra owner audit), and activation-delegation ordering. Test-discovered defect: ambiguous TimeoutException type caused the first integration-harness compile failure. Correction iterations: initial Pass A corrections, post-PR ACL regression correction, and fresh Pass B provisioning-order correction. No post-merge escaped defect known (not merged yet).

The final corrected ACL and provisioning-order suite passed locally at 170/170; Release build had zero warnings/errors and strict docs passed. Provisioning-order change requires fresh final-head remote integration. Full applicable invariant matrix re-evaluated: no changes to IDs/scope; LOCAL-003 and SEC-001 now additionally require root protection before group activation rights. Review Pass B is pending final validation and exact-head recheck; no Codex re-review will be requested or inspected.

## #41 acceptance record

Implementation HEAD `4fa361a3399adbf036e12a1582a6808814bf8386` passed local 170/170 tests, zero-warning Release build, strict docs, remote CI `33986519538` (ordinary suite plus dedicated real Windows integration) and Docs `33986520567`. Both remote runs report this exact SHA. Integration passed actual virtual-account identity, exact DACL, boot toggles, DB/mutex lifecycle including crash, confirmed non-admin eligibility/denied rights, cross-user DPAPI, uninstall preservation, and full cleanup. No remaining mandatory #41 field evidence.

Pass B repeated against that exact HEAD: **PASS**, complete diff and all 18 changed files reviewed, every #41 criterion mapped to local or real CI evidence above, full invariant matrix PASS, negative frozen-source/production-topology/scope search clean, no Product Decision. The production service is Windows-only with no idle policy, no Owner/bootstrap execution, no production signature selection, no Host secret store, and no server operations. Compatibility with accepted #40 is covered by the retained suite. Three correction iterations are recorded above. No known escaped defect or regression remains. Historical external finding is not a completion gate under the revised authorization.

This acceptance-record update changes documentation only; code/tests remain byte-identical to validated `4fa361a`. Final PR metadata/base/HEAD and strict docs are rechecked before merge. Shadow result: accepted for integration merge; canonical #41 remains untouched. Post-merge commit identity and dependency refresh will be appended to the integration ledger.




## Integration checkpoint after #41

PR #61 merged at ffe78fee3fd6d197308a9807e965b123a086b5de. Final PR HEAD d787a7d403f3840ae319cd77d97461d2d9852334 changes only the ledger relative to validated implementation 4fa361a; strict Docs run 33986674874 passed on that final HEAD. Pass B rechecked that exact final diff and merge base. Shadow #41 DONE; normal issue and Project untouched. Dependency refresh: #42 and #27 now satisfy their declared implementation dependencies and need readiness/impact analysis; #43 is independently in progress; #34 remains an independent design candidate. Later security/server/UI/Linux gates remain closed.


## #43 independent readiness/preflight

Canonical issue **#43 — Protocol slice — Host contracts, ServerRef identity, and version/capability negotiation**, parent #20, milestone v0.5.0. Dependencies #33/#19/#39/#40 are accepted at the frozen baseline. The bounded objective, exclusions, allowed naming/package choices, testable criteria, validation, stop conditions and review protocol are explicit in its current body. **SHADOW READY** despite canonical Backlog.

#41 has passed local/remote validation on PR #61 but is awaiting its independent reviewer after two requests. #43 has no #41 dependency and does not prejudge any Windows decision. Its branch `astra/43-protocol` starts at integration `0ccc6af`, not the unmerged #41 branch.

Preflight: change Contracts schemas, OS-neutral identity/compatibility helpers, SelfTest and protocol documentation only. No production reference topology change. Contracts runtime remains protobuf-only; Grpc.Tools is private build-time tooling. gRPC services are defined in schemas; endpoint/stub hosting belongs in subsequent transport composition. No credentials, authority-creation path, persistence writer, or operation executor is implemented. Grant DTOs express accepted types; they do not issue/evaluate grants. No arbitrary shell, general filesystem RPC, TLS, pairing, permission evaluator, business operation, UI, or Linux implementation.

Applicable invariants: CLIENT-001, IDENT-001/002, PROTO-001, REMOTE-001, AUTH-001/005, PLATFORM-001; ARCH-001/002 and all untouched security/authority families remain negative-scope checks. Audit cross-section consequences: malformed/default protobuf identities, host-qualified equality/routing/grants, unknown authorization values, incompatible-major versus additive-minor negotiation, removed-field reservation. Allowed decisions: exact schema names/field numbers, initial protocol 1.0, Google.Protobuf 3.36.1 and private Grpc.Tools 2.83.0 (verified official package listings). No product decision identified. No field evidence required by this bounded contract-only issue. PR/review/tests/result pending.


## #43 Review Pass A

Integrated accepted #41 through experiment `9d0b8e0`; only the experiment lane was merged. Resolved overlapping SelfTest references/test registrations and combined both ledger histories without dropping either issue's evidence. Full combined suite: **174/174 PASS** (170 existing Windows/legacy checks plus four protocol groups). Standalone pre-integration suite had 165 checks; counts are not additive across reruns. Production dependency topology remains unchanged.

Pass A examined every changed schema/helper/test/project/doc and its diff against the current integration branch. Findings corrected: routing assertion initially compared reference inequality instead of value equality; grant collision test varied unrelated actor fields and was made a same-grant clone differing only by Host target; schema guard needed immutable history snapshots for every added revision, and reserved fields still must not authorize removal within the same major. Strengthened negative tests cover these boundaries. Tests then found that protoc descriptor sets contain inferred JSON names and a different source-relative filename from embedded C# descriptors. Used the actual project proto path and reflection-computed JSON names to compare semantically identical full descriptors without dropping explicit JSON-name checks. A subsequent compile caught a shadowed local test variable; renamed it.

| Invariant | Why affected | Evidence/check | Result |
|---|---|---|---|
| CLIENT-001 | Contract consumers | Contracts runtime uses protobuf only, no Core/Host/platform dependency | PASS |
| IDENT-001 | Stable Host identity | HostId UUID contains no credential fingerprint | PASS |
| IDENT-002 | Wire/domain/routing/grants | Both Host/profile required; same profile on different Hosts remains distinct; malformed/default input rejects | PASS |
| PROTO-001 | Negotiation/evolution | Explicit-major comparison, immutable known-capability intersection, unknown-enum deny, product string ignored, append-only schema history with negative mutations | PASS |
| REMOTE-001 | Route shape | Host-qualified target only; no client outbound connection or transport | PASS |
| AUTH-001/005 | Grant wire shape | Distinct Host/server capability types and exact Host target; syntactic validation only, no grant issuer/evaluator | PASS |
| PLATFORM-001 | Portability | No OS APIs in Contracts | PASS |
| ARCH-001/002 | Frozen legacy | No App/Core/Lan edits or reuse | PASS |
| HOST-001/002, PERSIST-001 | Authority/persistence | No runtime executor or persistence writer added; #41 remains unchanged | Unaffected |
| CLIENT-002/003, LOCAL-001–004, OWNER-001/002, SEC-001 | Identity/security | No credentials, bootstrap/authentication, or protected channel implementation | Unaffected |
| IDENT-003/004, PAIR-001–004, REMOTE-002, AUTH-002–004 | Trust/delegation | DTOs do not rotate, pair, issue grants, or evaluate provenance | Unaffected |
| OPS-001–004, RECOVERY-001, MIG-001, PLATFORM-002, LINUX-001 | Other gates | No operations/recovery/migration/shell/Linux implementation | Unaffected |

Acceptance mapping: Core independence → architecture guards; host-qualified references/routing/grants → identity collision group; explicit version+feature gate and informational product strings → negotiation group; unknown authorization enums → serialization/negative group; no field-number reuse → immutable descriptor-history comparison plus deliberate bad-schema mutations. Full build/self-test passed after corrections. Release/docs and stale-reference search are rerun before push. Pass A correction cycles: 3 (assertion/schema strengthening, descriptor normalization, compile correction). No Product Decision or field requirement; no known escaped defect. Pass B pending PR creation under the revised Astra-only review model.

## #43 Review Pass B

PR #62, initial HEAD `cdccadcb85f42e61983873c04bf1b0b4e5931ade`, passed remote CI `33986933232` (174/174 and retained Windows service/multi-user integration) and Docs `33986934817`. Fresh Pass B inspected the complete 10-file diff, identity equality/invalid inputs, grant-versus-authority distinction, mutable protobuf offer isolation, unknown enum values, schema history, package/runtime boundaries, field gates, and interaction with #41.

Pass B found a test-coverage hole: schema evolution walked top-level messages but would not protect nested types introduced by a later additive revision. Added recursive nested-message/enum checks, reservation preservation, and an explicit nested-field reuse rejection test. Local validation after this correction: **174/174 PASS**, zero-warning Release build, strict docs. Full invariant matrix re-audited with PROTO-001's evidence strengthened; no production source change or scope expansion. Correction cycle count now 4; Pass B repeated after correction is clean, pending applicable remote validation. No escaped/after-merge defect known. No external reviewer used for #43.

## #27 preflight decision packet

**SHADOW PRODUCT DECISION REQUIRED** — #27 Host secure storage, parent #20. Declared dependencies now satisfied through #41. Accepted #19 section 7 describes encrypted-blob storage as readable only by the specific service account, while sections 2c/5a require privileged offline Host.Cli bootstrap/recovery to use the secure store. The exact ACL treatment of that exceptional caller is not explicit, and #27's Allowed Decisions name blob layout/key naming without authorizing a security-boundary change.

Decision requested: (A, recommended) explicitly grant the dedicated service plus elevated Administrators/SYSTEM access, matching #41's Host-root identities and #19's stated privileged-machine-owner limitation; or (B) retain a service-only DACL and separately specify the exceptional privileged access mechanism used by offline recovery. A avoids inventing impersonation/ACL-takeover behavior and still excludes ordinary users/clients; B makes direct file access narrower but requires a defined privileged recovery path. Only #27's Windows store/security integration is blocked; #43 and bounded UI design can continue without prejudging this choice. No #27 implementation has begun and no canonical issue/Project has been mutated. Product Owner input requested; unanswered is not approval.

## #43 acceptance record

Final implementation and reviewed PR HEAD `22f21020c400a5e8ec61e049c63cec0587531df5` passed local 174/174 self-tests, zero-warning Release build, strict documentation build, remote CI `33987200947` (including retained real Windows integration) and Docs `33987202883`. Both remote runs completed successfully at that exact SHA. Repeated Pass B is clean after the recursive schema-guard correction; the full invariant matrix and scope/contradiction search remain clean. No mandatory field evidence or Product Decision applies to this contract-only slice. Four correction cycles; no known escaped defect or regression. No external reviewer used for #43.

This acceptance record is documentation-only. Code and tests remain identical to the validated SHA. PR #62 targets only `experiment/astra-v0.5.0`; merge identity and refreshed dependency status will be recorded after merge. Canonical #43 and its Project state remain unchanged.
## Integration checkpoint after #43

Shadow #43 DONE. PR #62 merged at `ee8e558a12cad12b8108a511cadccf613730677a`; final PR HEAD `16233206433dca4d5243ee4fc1c157851d066f1e` differed from tested `22f2102` only by its acceptance record. Strict Docs `33987906177` passed on final HEAD. Canonical planning remained untouched. Dependency refresh: #34 is independently eligible; #27 awaits its explicit ACL Product Decision; #42 needs bounded preflight and secure-channel dependencies; later #44+ and production UI/Linux gates remain closed.
## #34 readiness and preflight

#34 — UI design slice: design system, application shell, and unified server rail; parent #18, v0.5.0. Dependencies #33/#19 accepted at the frozen baseline. Current canonical issue body reread: bounded design-only scope, explicit exclusions and six testable criteria; shadow authorization supplies autonomous two-pass review. SHADOW READY on branch `astra/34-shell-design`, from integration `41fc7bf`.

Expected changes: only `docs/design/astra-34/` vector prototypes, token data, deterministic generator and design specification, plus this ledger. No production project/dependency change, authority creation, credential consumer, persistence writer or operation executor. Applicable IDs: ARCH-001/002, HOST-001/002, CLIENT-001/002/003, IDENT-001/002, REMOTE-001/002, PAIR-001/002, AUTH-001/003/005, PROTO-001, OPS-001–004, PLATFORM-002, SEC-001. Remaining registry families audited as negative scope. Recheck all routes/labels for exact Host context, permission-filtered inventory, trust-versus-authority distinction, stale/error states, and no client-owned mutation. No detailed settings, permissions, Activity flows, production Avalonia or Linux.

Allowed decisions: 4-unit spacing scale, semantic palette, type hierarchy, project-owned vector icons/angular decoration, responsive dimensions/focus/motion guidance. Validation plan: generated 1600×900 and 2100×900 boards, 800×900 collapsed board, component/state board and token alternatives; inspect rendered boards for clipping/identity/context; programmatic SVG/XML, palette contrast, fixture identity uniqueness and reproducibility checks; strict docs; two distinct full-diff/invariant reviews. Accepted canonical #18 textual direction has been read. Canonical image browser capture repeatedly times out; no successful visual comparison is claimed. If still unavailable at acceptance, record the exact visual-evidence limitation, not a fabricated PASS.
## #34 Review Pass A

All design sources and seven rendered SVG boards inspected, including the canonical #18 image (successfully viewed at natural size using the in-app browser; the earlier Edge capture limitation is resolved). Six acceptance criteria map respectively to duplicate-name/ID-prefix fixtures and full identity expansion; 1600×900 plus 2100×900 boards; 800×900 collapsed and keyboard-identity boards; shared token/generator components; identical-layout Dark/Light palette boards; explicit Host-only authority and filtered-inventory specification. No production source is changed.

Review-only findings: neutral control borders initially measured 2.70:1 on the flagship surface despite passing text checks; strengthened the border token and expanded checks to all surfaces. Collapsed identity behavior initially existed only in prose; added a separate focused identity board with both full UUIDs. Several core dimensions were literal in the generator; bound core chrome/rail/drawer/control/corner/title dimensions to their shared token values. One correction iteration covers these related foundation/evidence changes. No automated-test defect before review, no escaped defect known.

Validation: seven deterministic SVG/XML/reproducibility checks PASS; text contrast >=4.5:1 and interactive boundary/focus contrast >=3:1 for all three palettes PASS; exact fixture Host/profile pairs distinct PASS; strict MkDocs PASS. Visual review found no clipped labels or overlapping controls in the demonstrated layouts; distinct local/remote target context remains visible. Static SVGs do not establish live keyboard/screen-reader behavior or final cross-surface acceptance; those remain #38/production validation, not required evidence waived from #34.

| Invariant | Why affected | Evidence/check | Result |
|---|---|---|---|
| ARCH-001/002, CLIENT-001 | Design-only boundary | Only docs/design and ledger; no frozen/source/dependency edits | PASS |
| HOST-001/002, PERSIST-001 | Shell command placement | Host alone owns inventory/mutations; no executable process/filesystem/store path | PASS |
| CLIENT-002/003, LOCAL-001–004, OWNER-001/002, SEC-001 | Client authority and secrets | No keys, bootstrap, enrollment, recovery, or privilege shortcut; synthetic public IDs only | PASS / unaffected mechanisms |
| IDENT-001/002 | Collision-safe presentation | Stable semantic UUIDs; duplicate Host/server names and prefixes; exact pair selection and full details | PASS |
| IDENT-003/004 | Rotation/recovery | No credential fingerprint substituted for HostId; no recovery design | Unaffected |
| REMOTE-001/002, PAIR-001/002 | Remote navigation | Local-Host route; only authorized inventory; trust never entitlement; no PeerBound ordinary management | PASS |
| PAIR-003/004 | Trust transactions/replacement | No pairing/replacement flow | Unaffected |
| AUTH-001–005 | Authority creation | No grants, presets, scopes or delegation engine; full target retains authoritative Host | PASS / unaffected mechanisms |
| PROTO-001 | Capability presentation | Unsupported actions retain reason; no app-version inference | PASS |
| OPS-001–004, RECOVERY-001 | Activity/lifecycle summaries | No stale write or client-owned operation; closing drawer never releases/cancels work; detailed flow deferred | PASS / unaffected mechanisms |
| MIG-001 | Add/Import entry | Placement only; no copy/move/merge semantics | Unaffected |
| PLATFORM-001/002, LINUX-001 | OS boundary | Static project-owned vectors, no shell execution/Avalonia/Linux production | PASS |

Negative/contradictory-reference review checked remote routing, grants/trust, full identity, stale status, Files/Logs versus bounded parity, #35–#38 ownership, client mutation, private keys, import targeting, theme reuse, and final-acceptance claims. No Product Decision or mandatory field gap identified within this bounded design slice. Pass A clean; next action is PR creation into the experiment branch, then fresh Pass B.
## #34 Review Pass B and acceptance

PR #63 final design HEAD `9aba886d7f8ed23ce9c54a31483292cc5e92e0c7`. Fresh post-PR review covered the complete 11-file diff, every vector/source/token/document, all six criteria, collision/alias behavior, unavailable/remote actions, token contrast, layout/focus evidence, retained #41/#43 boundaries and all invariant families above. No additional defect found. No Product Decision and no mandatory unperformed #34 field criterion; static evidence is explicitly not live accessibility or #38 final design acceptance. No external reviewer used.

Local generator/reproducibility/contrast/identity checks and strict docs PASS. Remote Docs `33988354529` PASS on the exact design SHA. No production source/tests or CI workflow changed, so the existing 174-test/real-Windows evidence remains intact and is not relabeled as a new run. Pass A and Pass B clean. One correction iteration, review-only findings as recorded; no known escaped defect or regression. This acceptance update is ledger-only; final strict docs and PR HEAD/base are rechecked before integration merge.
## #34 integration checkpoint and escaped fixture defect

PR #63 merged at `9e2b551f5cf769209fdc63205c795948f2cf4b47`; final ledger-only HEAD `8f5a41467e777ec9a96e1ceda9eed64b7aa9c1d4` passed strict Docs `33988406217`. Design #34 was accepted; #35–#37 dependencies are now satisfied, subject to preflight. #27 remains Product Decision and implementation security gates remain closed.

Before starting #35, cross-checking accepted backup parity in `docs/guide/backups.md` exposed an escaped #34 fixture defect: the active backup targeted R1/Main Server while its header showed Running. Backups require a stopped server. Both reviews missed that semantic combination. Bounded follow-up `astra/34-status-fixture-fix` is rooted at the #34 merge; no production behavior is changed. Correct the active backup target to the stopped local server, replace the speculative lock-waiting example with a completed historical remote backup, and make active counts one. This also avoids pre-designing #38 lock/queue semantics. No new product decision or field evidence; full invariant audit remains the #34 matrix, with HOST-002/OPS-002 evidence corrected. Escaped-defect count for #34: 1; follow-up correction cycle: 1 (total 2).

Follow-up Pass A: reviewed generator and every regenerated board against current integration, cross-checked stopped-backup constraint and historical-versus-active status, searched all Activity/count/target copies. Same seven-board structural/contrast/reproducibility checks and strict docs required; no source or normal-lane change. No further issue identified.

Follow-up Pass B on PR #64 at `c12b1a726f5a8227c10c480f4648dcb598373c3d`: clean. Fresh full eight-file diff confirms the shared drawer changes propagate to all themes/wide layouts and narrow active counts; unrelated component/identity data is unchanged. Read-only geometry, exact targets, stopped-backup condition and completed-history distinction are consistent. Repeated full #34 invariant matrix PASS. Local design/strict-doc checks PASS; remote Docs `33988512961` PASS at that exact SHA. No additional finding. This paragraph changes only the ledger; final docs/HEAD/base verification precedes merge.
## #34 correction integration / #35 readiness

The bounded #34 fixture correction PR #64 merged at `bd947811ea14ee7901cecb674aed074a9486d131`. Final ledger-only HEAD `6a1759b715dd05170f66da5bb47b849f6227715c` passed Docs `33988558414`. Shadow #34 DONE with the escaped defect corrected; no known remaining defect. Canonical planning unchanged.

#35 — UI design slice: server workspace and Add/Import flows, parent #18, v0.5.0. Current issue body reread; dependencies #33/#19/#34 satisfied. SHADOW READY; branch `astra/35-workspace-design` from exact integration `bd947811ea14ee7901cecb674aed074a9486d131`. Scope: fleet/overview/players/metrics/backup-history and guided create/import/destination selection only. Cross-checked accepted backup/package/observability requirements in baseline guides and canonical #49/#50. No player moderation, generic REST/filesystem UI, resumable transfer or new management capability.

Preflight: new docs/design/astra-35 artifacts/spec/generator plus ledger only; reuse #34 semantic components. No production references/authority creators/private-key holders/persistence writers/executors. Applicable HOST-001/002, IDENT-001/002, CLIENT-001–003, REMOTE-001/002, PAIR-001/002, AUTH-001/003/005, PROTO-001, OPS-001–004, RECOVERY-001, SEC-001, MIG-001, PLATFORM-002; remaining full-registry families negative-scope audit. Recheck stale/live data, exact targets, permission-filtered resource visibility versus unavailable actions, stopped backup/restore, pre-restore backup, package integrity/runtime exclusion and Host-routed remote creation. No #27 ACL decision is prejudged.

Allowed choices: dense tables/chart hierarchy, single-step guided panels, secondary navigation and restore-point selection. Validation: reproducible vector boards for each authorized surface/flow, rendered visual review, #34 token checks retained, explicit criterion/state/authority mapping, strict docs and remote Docs, two-pass complete review. Detailed settings, trust/permission editor, Activity/transfer/conflict/recovery system remain #36–#38. No Product Decision identified in this preflight.
## #35 Review Pass A

Complete new-artifact review against integration `bd94781`: generator, specification and all eight rendered boards. Acceptance mapping: unified local/remote mental model → paired local/remote Overview plus exact Host rail/header; guided add/import/create → separate sequential storyboard panels with retained destination; backup history → stopped snapshot and scoped restore-point review/safety backup; useful Players/Metrics → semantic roster and unit/tick/time/gap charts; unavailable actions → explicit Not authorized/Remote unavailable/offline examples; no client authority → full target/Host-routed operations and bounded input specification.

Review-only corrections: All Servers incorrectly retained R1 selection; degraded Metrics retained a live-looking roster count in the rail; added independent local Overview evidence; replaced unlabeled schematic chart ranges with numeric ticks and integer player-count steps; added a visibly unauthorized backup action; kept stopped-state Restart unavailable; corrected inherited remote copy on the local board; added the selected opaque restore-point reference to distinguish same-time history entries; moved metric units clear of axes. Three source-edit correction iterations during Pass A. Automated structural/contrast checks found no defect before review. No known escaped #35 defect. The previously escaped #34 fixture issue is recorded separately and was corrected before this branch.

Local eight-board XML/reproducibility and shared palette checks PASS; retained #34 seven-board check PASS; strict MkDocs PASS. All boards visually inspected; updated identity/freshness compositions and local/remote layout rereviewed. Static charts/data remain explicitly synthetic; no live service/UI/accessibility/field run claimed.

| Invariant | Why affected | Evidence/check | Result |
|---|---|---|---|
| ARCH-001/002, CLIENT-001, PLATFORM-001 | Layering | New docs/design only; shared design primitives, no production reference/source change | PASS |
| HOST-001/002, PERSIST-001 | Read/mutation authority | Host inventory, observations, create/import/backup/restore; no client executor or authoritative writer | PASS |
| CLIENT-002/003, LOCAL-001–004, OWNER-001/002, SEC-001 | Security boundary | No machine credentials, secret data, bootstrap, enrollment or privileged shortcut; advanced player IDs not credentials | PASS / unaffected mechanisms |
| IDENT-001/002 | Every scoped surface | Exact Host/profile pair, local/remote headers/rail, collision examples, Host-target creation before profile exists | PASS |
| IDENT-003/004, PAIR-003/004 | Trust recovery/transactions | No trust/replacement/rotation behavior designed | Unaffected |
| REMOTE-001/002, PAIR-001/002 | Remote data/destinations | Local-Host route, independent permissions, Active usable trust, filtered inventory, no inferred entitlement | PASS |
| AUTH-001–005 | UI authority | Exact Host CreateServer and server targets; no grants, presets or creator-Owner shortcut | PASS / unaffected mechanisms |
| PROTO-001 | Unavailable actions/streams | Feature-specific negotiated support, no app-version inference or generic fallback | PASS |
| OPS-001–004, RECOVERY-001 | Submission/restore lifecycle | Revalidate state/target/lock; stopped backup/restore; safety backup failure stops restore; no offline queue/client unlock; #38 owns detailed recovery | PASS |
| MIG-001 | Import boundary | New profile, source preserved; not v0.4 migration or store merging | PASS / unaffected migration |
| PLATFORM-002, LINUX-001 | Filesystem/platform | Bounded package handoff/local-folder seam only; no remote browser/UNC or Linux production | PASS |

Negative/contradictory search covered direct filesystem/REST, hidden inventory, trust-as-permission, implicit target, raw backup files, resume/queue, revision/lock bypass, stale samples, #36–#38 scope and final field claims. No unresolved Product Decision or mandatory #35 field evidence. Pass A clean; open PR then fresh Pass B.
## #35 Review Pass B

PR #65 initially at `eb83a2c8bc30ca6d5d1dcf9ce28e82e03f276047`. Fresh complete 11-file review found one additional fixture contradiction: the Metrics board declared REST unavailable while Safe Stop/Restart still appeared usable. Corrected those controls to unavailable with REST-specific reasons; running process status remains independently visible. Rendered the correction and final restore-reference/axis details. No additional issue on repeat review. This is a Pass B-only finding; correction count now 4. No automated-test defect or escaped #35 defect known.

Repeated full invariant matrix PASS: HOST-002/PROTO-001 unavailable-action evidence strengthened, OPS/identity/data-ownership/security/platform boundaries unchanged. No new product semantics, generic endpoint, field substitution or #27 decision. Eight-board and retained seven-board checks PASS; strict docs rerun before commit. Pass A and repeated Pass B clean for final corrected diff; remote Docs and exact committed HEAD/base verification remain the merge gate. No external reviewer used. Final SHA/run/merge evidence will be appended at the integration checkpoint after those checks succeed.
## #35 integration checkpoint

Shadow #35 DONE. PR #65 final reviewed HEAD `f32418161225c74b24e30354de3270a8935a251b` passed local design/strict-doc checks and remote Docs `33989127338`; exact PR metadata/base and committed correction rechecked. Merged at `320e7760ade771d49a3e791657247d5f69f283ac`. Four correction iterations, no known escaped #35 defect. All source remains unchanged from #41/#43, whose 174-test evidence is retained without claiming a new run. Canonical planning untouched. Dependency refresh: #36/#37 design preflights eligible; #38 still waits for both. Host secure storage #27 remains an unanswered Product Decision, and production/security/Linux gates remain closed.
## #36 readiness / preflight

#36 — UI design slice: semantic Server Settings, parent #18, v0.5.0. Dependencies #33/#19/#34 satisfied; accepted #35 navigation also available. SHADOW READY on `astra/36-settings-design` from integration `799320f11d75e7d9b746219bda23aab3dbb53468`. Current canonical body already read in this turn. Scope is settings design/prototypes only: categories/search, semantic control families, modifications/save/discard/navigation/validation, unknown preservation and metadata-availability states.

Sources checked at frozen baseline: Core `PalworldSettingSchema`, `SettingModels`, `PalworldSettingsService`, settings-editor guide, architecture section 12, canonical #47. The schema establishes meanings/categories and some enum choices/units; defaults are installation-derived and optional, and it does not establish general numeric bounds/restart metadata. Therefore no numeric limits/defaults/restart rules will be guessed. Conditional control templates may consume future Host metadata but will not bind invented ranges or path-setting semantics to a real Palworld key. Concrete examples use known boolean, enum, text/password and multiplier meanings, with explicitly synthetic configured values; unknown metadata disables the corresponding reset/range/restart claim. Saving still requires stopped state under accepted parity.

Expected changes: docs/design/astra-36 and ledger; reuse accepted design primitives. No Core parser/catalog, production Avalonia, client references, credential store, permission UI or actual write. Applicable ARCH-001/002, HOST-002, PERSIST-001, CLIENT-001/002/003, IDENT-001/002, REMOTE-001/002, PROTO-001, OPS-001–004, SEC-001, PLATFORM-001/002, MIG-001; all other registry families checked as negative scope. Recheck every input/reset/preset/raw/secret/save path for authority, stale revisions, unknown preservation and secrets. Optional presets are not introduced by this slice.

Allowed choices: category/search/control composition and modified-state language only. Validation: 16:9/21:9/narrow settings boards, semantic-control gallery, validation/unsaved-navigation/raw/secret states, rendered review, deterministic XML/token checks, strict docs, remote Docs and two distinct invariant audits. #38 owns final cross-surface acceptance; no live UI evidence will be claimed from SVG. No Product Decision identified: unknown metadata remains unknown rather than selecting product semantics.
## #36 Review Pass A

Complete source/specification/seven-board review against integration `799320f`. Acceptance mapping: semantic normal UI → mixed typed editor and twelve-family gallery; domain meaning rather than storage type → known baseline examples and explicitly unbound templates; unknown preservation → Advanced/raw specification and panel; unsaved/validation/restart clarity → two-change editor, stopped/running/invalid/navigation panels and unknown-metadata labels; responsive/keyboard/reduce-motion → 1600×900, 2100×900, 800×900 plus expanded narrow row and inherited focus/motion contracts.

Review-only findings corrected in one iteration: the first gallery described semantic families but drew too many as identical textboxes; changed segmented boolean, integer stepper, multiline, compound choices and unit-bearing compositions. Global Alerts entry had been omitted from settings chrome; restored it. Narrow reset/revert existed only in prose with no affordance; added Details controls and an expanded-row board showing unavailable Reset and actual Revert. No automated-test failure before review; no known escaped defect.

All seven SVGs rendered and inspected; shared contrast and reproducibility/XML checks PASS, strict docs PASS. Unknown numeric bounds/defaults/restart rules remain unselected. Path/bounded-number templates bind no invented real setting. Synthetic configured values are never represented as defaults. No actual secret or live UI/accessibility/field behavior is claimed.

| Invariant | Why affected | Evidence/check | Result |
|---|---|---|---|
| ARCH-001/002, CLIENT-001, PLATFORM-001 | Layering | New design docs only; no parser/catalog/production dependency changes | PASS |
| HOST-001/002, PERSIST-001 | Save/reset/raw effects | All saves are Host-authorized scoped writes; reset/revert are draft-only, no client INI/file path | PASS |
| CLIENT-002/003, LOCAL-001–004, OWNER-001/002 | Client security | No Host identity credential/bootstrap/enrollment/Owner shortcut; same authenticated local route | PASS / unaffected mechanisms |
| IDENT-001/002, REMOTE-001/002 | Target continuity | Stable full server/Host target, local-Host remote routing, dual authorization retained | PASS |
| IDENT-003/004, PAIR-001–004, AUTH-001–005 | Trust/grants | No trust, recovery, permission or preset mechanism introduced; unavailable authorization never bypassed | Unaffected / PASS boundary |
| PROTO-001 | Schema compatibility | Unknown metadata remains unknown; unsupported schema cannot trigger direct-file fallback | PASS |
| OPS-001–004, RECOVERY-001 | Saving concurrency | Exact revision rejection; stopped-save parity; no forced overwrite/automatic stop/offline queue; Host owns locks/effects | PASS |
| SEC-001 | Every secret presentation path | Unchanged/replace/clear explicit; redaction before normal/raw/error/search/comparison; no logs or disk drafts | PASS |
| MIG-001 | Unknown/config preservation | No migration/normalization/store merge; unrelated unknown entries/comments preserved | PASS / unaffected migration |
| PLATFORM-002, LINUX-001 | Platform scope | No generic file browser, Host shell, Avalonia or Linux production | PASS |

Stale/contradictory search covered guessed ranges/defaults/restart, effective/configured conflation, hidden raw-value rewrite, secret placeholder serialization, running saves, revision bypass, missing chrome entries and #37/#38 scope. No Product Decision is resolved by this design and #27 remains open. No mandatory unperformed #36 field criterion; Pass A clean, PR then fresh Pass B.
## #36 Review Pass B

PR #66 initially at `d37c19229a7c4900f9926c59965c814a41c2bd6f`. Fresh full ten-file review checked all seven boards against their specification and baseline metadata, each acceptance criterion, all secret/raw/reset/save paths, stopped-save/revision requirements, keyboard composition, prior design navigation and the complete invariant matrix. Found a Pass B-only evidence defect: the bounded-slider pattern omitted its promised numeric-entry alternative. Added the entry field. Also clarified softly wrapped description text cannot imply stored newlines unless the Host definition allows them. No setting range/default/restart semantics added.

Rendered the final gallery and repeated design checks; no further defect on repeat review. Full matrix PASS with keyboard/known-metadata evidence strengthened; authority/security/unknown-preservation boundaries unchanged. Two correction iterations total. No automated-test defect or known escaped #36 defect. Local seven-board plus retained #34/#35 checks and strict docs pass; remote Docs and exact final committed HEAD/base verification remain before merge. No external reviewer or field-evidence substitution. Integration checkpoint will record the final SHA and successful run.
## #36 integration checkpoint / #37 readiness

Shadow #36 DONE. PR #66 final HEAD `4d37fe7d48910a96c7c844566d858871dd81d35b` passed local design checks and remote Docs `33989699852`; exact metadata/base and correction checked before merge at `f4ae27931102d267919930784d5e69c56dfe38bc`. Two correction iterations, no known escaped #36 defect. Canonical planning untouched. #37 is now the remaining prerequisite before #38 final design work; #27 remains Product Decision.
## #37 readiness / preflight

#37 — UI design slice: Manager Settings, trust, sharing and permissions, parent #18, v0.5.0. Current canonical body reread; dependencies #33/#19/#34 accepted. SHADOW READY on `astra/37-security-design`, from `c5a4010c612ad17cec15d39bf577e530b5466a67`. One coherent design-only security/Manager Settings slice; no implementation, generic OS administration or cloud/relay scope.

Read accepted architecture sections 2/2a/2b, 4b, 5/5a/5b and 8, with current full registry already available. Expected files: docs/design/astra-37 prototypes/spec/generator and ledger. No production topology, credential holders, writers or executors change. Applicable all CLIENT/HOST/IDENT/LOCAL/OWNER/REMOTE/PAIR/AUTH/PROTO/SEC/PERSIST families, plus ARCH, OPS, RECOVERY, PLATFORM, LINUX negative boundaries. Authority-creation audit includes structural Owner bootstrap/root grants, ordinary delegated grants, role-preset expansion, Owner-only DefaultGrantTemplate, creator convenience, Owner adoption/re-home and credential replacement. None may become an alternate grant-issuance path through UX.

Design distinctions to preserve: exact Host/server scopes (no AllServers grant); LocalPrincipal versus RemoteManager actor; independent CanDelegate/CanDelegateOnwardDelegation; single-parent provenance forest; ManagePermissions is inspection, not grant issuance or defaults editing; defaults applied only at Active and not retroactively; same-HostId unproven replacement needs Owner approval; local revocation is immediate regardless of peer acknowledgment. Privileged offline recovery is guidance only, never an ordinary/remote Owner replacement button. Host boot configuration remains privileged setup while UI sign-in is per-user; no new privileged helper chosen. #27 ACL choice remains untouched.

Allowed decisions: internal Manager Settings navigation, role-card/graph/confirmation layout. No preset membership or factory-default grant list is invented: role cards preview Host-defined entries, Custom shows explicit synthetic grants, factory template is identified as Host-provided least privilege. Validation: scoped Manager Settings, trust/defaults/custom-grants/provenance/revocation/update-diagnostics boards, static rendering/semantic audit, deterministic XML/shared token checks, strict/remote Docs, full two-pass review. No Product Decision found in this bounded presentation plan.
## #37 Review Pass A

Reviewed every source/spec/board and complete proposed change against integration c5a4010. Nine rendered boards cover all seven acceptance criteria: trust versus grants; typed exact targets; root/derived forest and independent onward flags; structural Owner boundaries; future-only defaults; subtree preview; unsupported action with reason. Canonical scope maps General/Appearance/trust/defaults/grants/provenance/revocation/recovery/update-history to concrete boards. No production files change.

Two review-only correction iterations: restored the permanent server rail when Manager Settings initially replaced it; added a concrete unsupported history-export control because prose alone was weak unavailable-capability evidence. All nine initial renders inspected; final changes checked in generator/SVG. Deterministic XML and shared contrast checks for #34–#37 PASS, strict docs PASS. No automated-test defect or known escaped #37 defect. No live keyboard, security or field test is claimed by static art.

| Invariant IDs | Why affected / evidence | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001, PLATFORM-001 | Docs-only diff; frozen WPF and dependency topology untouched | PASS |
| HOST-001/002, PERSIST-001 | Authoritative Host context; no UI writer; offline recovery retains same lock | PASS |
| CLIENT-002/003, LOCAL-001–004, OWNER-001/002 | No private Host key/client shortcut, first-connection Owner, group enrollment or ordinary Owner reset; privileged preparation/intended-user completion | PASS |
| IDENT-001/002, REMOTE-001/002 | Full Host and server identities, typed actors and local-to-remote dual authorization | PASS |
| IDENT-003/004, PAIR-001–004 | Rotation versus loss; PeerBound no management; Owner-gated known identity; immediate unilateral revocation, no distributed transaction | PASS |
| AUTH-001–005 | Exact type/target/source and both independent flags; preset/default paths obey issuance; Owner-only nonretroactive templates; forest cascade leaves independent roots | PASS |
| PROTO-001 | Negotiated support; unknown/unsupported denied, no version-string authority or raw-file fallback | PASS |
| OPS-001–004, RECOVERY-001 | Recheck stale graph before commit; Host owns effects; no new lock/recovery kind or blind retry | PASS / unchanged mechanisms |
| SEC-001 | No actual secrets; redacted history; #27 storage policy explicitly unresolved | PASS boundary |
| MIG-001, PLATFORM-002, LINUX-001 | No migration, generic OS UI, service shell or Linux production | Unaffected / PASS boundary |

Stale-reference audit examined trust-as-authority, ManagePermissions-as-issuer, default retroactivity, derived rights escalation, HostId changes, global-server scopes, own-Owner remote authority, peer-ack revocation, guessed preset membership, recovery and #27 ACL assumptions. Final design preserves accepted semantics; Pass A clean. PR then independent Pass B required.
## #37 Review Pass B

PR #67 at 30d7f85931d5797ffd00dee3a2bfc2578405fab3 reviewed afresh: full twelve-file diff, all acceptance criteria, all invariant rows above, source and generated boards, negative issuance/recovery paths and sibling design consistency. Found a consequence-preview omission: trust revocation explained dependent grants generically without naming the concrete affected grant in this scenario. Added G103, zero descendants and unaffected G101/G102/G201, with current-graph recheck. This is the third correction iteration total; no automated-test defect or known escaped defect.

Rechecked both revocation columns against the forest: subtree revocation G102 affects G102/G103; peer R1 revocation affects only G103 here. All other acceptance/invariant checks remain PASS; no Owner, preset, default or #27 policy changes. Generator/reproducibility and strict docs pass. Pass B clean after correction review; final-head remote Docs and exact metadata/base check remain before merge. No external review used.
## #37 integration checkpoint

SHADOW DONE. PR #67 final HEAD 09e26ac1d4a4a05231f148043fec90de52a807ed; remote Docs 33990370967 PASS; metadata/base and final correction renders verified. Merge 31658163b3daeeb6b6dbd67b2f07d118600351ed. Three correction iterations, no known escaped #37 defect. No canonical mutation. #38 prerequisites now accepted in shadow.
## #38 readiness / preflight

SHADOW READY on astra/38-final-design from 91d1b092af71177b26610aec1e95b45144ea9a33. Canonical #38/#18 reread; #33/#19 and shadow #34–#37 accepted. Design-only final slice, no Avalonia or service implementation. Read accepted architecture §§9–10 and baseline transfer parity, cross-checked canonical #49/#50; legacy LAN navigation/bearer semantics are superseded by accepted #19/A1-U, not copied.

Expected docs/design/astra-38 specification, deterministic boards and a local interactive design prototype to exercise focus/reflow, plus ledger. Scope: Activity, Send/incoming review, conflict/busy/recovery, stale/offline/degraded/permission/protocol/failure, same-token three themes and final responsive/keyboard consistency. New operation types, resumable transfer, offline queues, destructive continuation and new authority are prohibited. Exact per-kind recovery/lock mappings remain implementation contracts; presentation uses declared Host outcomes rather than guessing them. All registry families reviewed; OPS-001–004/RECOVERY-001/IDENT-002/REMOTE-001–002/PROTO-001 are central; security/Owner/store mechanisms remain untouched. No production dependency/credential/writer/executor changes.

Validation plan: concrete static failure/transfer boards plus interactive local presentation exercise at normal/narrow/enlarged text, all three palettes/shared token checks, full cross-surface acceptance map, local strict docs, two independent reviews and final-head remote Docs. Browser prototype is synthetic, not production accessibility/service/field evidence. #27 remains a required product decision; #42 preflight reveals practical secure-store dependency despite its declared list omitting #27.
## #38 Review Pass A

Reviewed all fourteen changed files (spec, HTML, generator, ten SVGs, ledger), full proposed integration diff, canonical #18/#38 acceptance and full registry. Cross-surface map in README binds every critical accepted surface to boards/interaction rules, including bounded log detail rather than a new Files/transport tab. Ten boards rendered/inspected; retained #34–#37 and new generator/XML/shared contrast checks PASS; strict docs PASS. No C# production change or new C# test-count claim.

Review corrections across four iterations: (1) scenario transitions reset exact target rather than retaining an unrelated Host header, and focus uses the active palette accent; (2) narrowed rail labels without losing accessible names, retained decided-offer state on reopening, corrected Send source rail state/destination availability and separated Activity filter spacing; (3) fixed offline-server Overview reverting to a stopped/live example, added concrete bounded log evidence and Add/Import accessible name; (4) added actual incoming Close/Escape with focus return and no accept/reject side effect. These are review/browser-exercise discoveries, not automated C# test defects. No escaped #38 defect known.

Browser evidence (local synthetic HTML): Activity opens with Close focused; Escape returns to Activity, and separately to Alerts when Alerts opened it. Tab leaves the nonmodal panel. Keep draft via Enter returns to Reload trigger; Review focuses the explicit latest-value selector. Closing incoming review leaves it undecided; rejection survives navigating away/back with Accept disabled. Offline R1 then Overview stays timestamped/non-live with Start disabled. Narrow 640×900 plus 200% text renders both grant flags and full identity with 88-unit rail; document scroll width equals 640. Light conflict and Dark grant layouts inspected at enlarged text. 1600×900 all three themes preserve workspace width 1318 / rail 280 / scroll width 1600; 2100×900 Light transfer preserves rail 280 / scroll width 2100. Theme switches use one DOM and actual shared JSON. Viewport override reset. No real Host, OS preference, production screen reader or field test was simulated as passing.

| Invariant IDs | Why affected / concrete audit evidence | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001, PLATFORM-001 | Docs-only diff; unchanged executable/dependency topology | PASS |
| HOST-001/002, PERSIST-001 | All effects/status/recovery Host-owned; HTML only synthetic memory and local token fetch | PASS |
| CLIENT-002/003, LOCAL-001–004, OWNER-001/002 | No machine key, bootstrap/enrollment/group/Owner shortcut; protected local channel and intended-user guidance retained | PASS |
| IDENT-001/002, REMOTE-001/002 | Exact target/actor boundaries, separate local/destination authorization; no UI remote-key transport | PASS |
| IDENT-003/004, PAIR-001–004, AUTH-001–005 | No new trust/issuance/rotation/default rules; two grant flags/targets preserved; offer acceptance grants no standing authority | PASS / unchanged mechanisms |
| PROTO-001 | Unsupported/unknown/incompatible distinct from denial/offline; no version-string or generic fallback | PASS |
| OPS-001 | Original/draft/current, rejected revision, explicit review/reload, repeated-conflict path, no overwrite/merge | PASS |
| OPS-002/003/004 | Host operation lifetime, target versus lock scope, Host/server hierarchy, safe reads, no UI close release | PASS |
| RECOVERY-001 | Declared disposition; retained manual-review lock; no blind resume/force unlock or resumable transfer feature | PASS |
| SEC-001 | Synthetic data only; secret comparison clearing/redaction; no tokens/log/disk draft, #27 unmodified | PASS boundary |
| MIG-001, PLATFORM-002, LINUX-001 | No migration writes; bounded client-only folder/log actions; no Linux production | PASS / unchanged mechanisms |

Acceptance map: nonblocking lifetime=Activity/HTML; stale non-live=states/failures/offline-navigation exercise; conflict/lock/recovery=three distinct boards and scoped actions; scoped receipt=incoming/HTML; alternate hierarchy=identical Activity geometry/shared DOM; all critical surfaces=README #34–#38 map; implementation handoff=explicit responsive/focus/availability/operation rules, backend policy kept in accepted contracts. Search covered hidden authority, offline queues, live-world copying, partial import, blindly resumed transfer/update, secret raw/comparison paths, palette forks and stale contradictory targets. All gates for design scope satisfied locally; Pass A clean. PR creation then fresh Pass B and exact-head Docs remain.
## #38 Review Pass B

PR #68 at 9b159676529644103cd0c682ae38ac6e0c17dbd3 reviewed afresh, all fourteen files and complete acceptance/invariant matrix, generated/source coherence, interaction handlers and cross-surface negative paths. Found one target-clarity defect: incoming receipt's HTML header still showed an existing local server/profile, implying a server destination instead of receipt on a Host before separate import. Corrected Host-only incoming/recovery headers and central target rendering so returning to Overview restores full ServerRef. Browser verified incoming Host-only -> Close -> local ServerRef, and Host recovery -> Overview remains remote offline/non-live with full ServerRef. Fifth correction iteration total.

Fresh acceptance audit: no copy of running world, automatic partial adoption, implicit permission, stale-live fallback, queue, forced overwrite, scope inference, or blind recovery. Static board and HTML effects remain presentation-only. All invariant rows from Pass A PASS after exact-target correction; no source/private-store/production boundary changed. Local strict docs PASS and retained deterministic checks remain valid. Pass B clean; no automated-test defect or known escaped #38 defect. Final committed HEAD/base/remote Docs must pass before merge.
## #38 integration checkpoint / design gate

SHADOW DONE. PR #68 final HEAD 4ea861e4ae7bfc8fef96c0e63fd02b4e2b26e224; remote Docs 33991037280 PASS on that exact commit; base/metadata and final correction verified. Merge 562c67a95af51847b4ed796ae6956f167fb63587. Five correction iterations, no known escaped #38 defect. #34–#38 are now accepted in shadow, satisfying the #18 design container gate only. Canonical issue/Project states remain untouched. Production Windows parity has not passed.

## #42 preflight / dependency stop

Current canonical #42 body reread. Declared #33/#19/#40/#41 prerequisites are accepted, but the complete normative registry adds LOCAL-003/004, OWNER-002 and IDENT-004 obligations beyond the issue's older invariant list. Local IPC must authenticate the Host using its machine credential and protected public trust descriptor before any principal challenge/bootstrap/enrollment secret or RPC crosses it. Bootstrap/offline recovery must establish/use the secure credential reference under the same exclusive writer lock; ordinary clients must never obtain that machine private key. #43 contracts are accepted, but #27's secure Host store is still Product Decision.

Consequently #42 is SHADOW BLOCKED by the practical #27 dependency. A plaintext/development credential store, self-signed trust-anyway channel, identity-only named-pipe challenge or implementing the unresolved ACL/access policy inside #42 would bypass the accepted security boundary. No production #42 branch/PR or partial implementation is claimed. Required eventual evidence remains real Windows multi-user named-pipe/TLS/authentication/bootstrap/recovery tests plus full suite/invariant audit. No transport algorithm/library was selected prematurely.

## Stopping checkpoint — remaining authorized work

All currently independent bounded design/protocol/platform work is exhausted. #27 retains the unanswered Product Decision packet above. #42 and downstream security/server/UI work are dependency blocked; #55 is not eligible and still needs real Windows field/parity evidence. Linux #21 remains hard-gated by #55; packaging #22 and qualification/docs #23–#25 are not reached, with future oversized work to be decomposed only when ready. #26 observation and #29/#30/#31 remain unscheduled; no invented cause or new capability. No real release/tag/package/main mutation occurred.

Completed trial work: #41, #43, #34–#38 (seven bounded issues), with one separate escaped-fixture correction PR for #34. Eight experimental PRs #61–#68 total. Full suite grows from baseline 161 to final validated 174; no summing reruns. Last production CI 33987200947 contains actual Windows integration. Design package has 41 current SVG boards plus one local synthetic HTML prototype. This checkpoint is documentation only; no code changes since accepted #43. #34 has one known escaped design defect, fixed in PR #64; no known remaining escaped defect. Review correction iterations: #41 3, #43 4, #34 including follow-up 2, #35 4, #36 2, #37 3, #38 5 (23 total under recorded per-item counting).

The revised Astra trial used no external GitHub Codex reviewer; Astra was responsible for implementation and technical review under two-pass self-review. Historical exception remains explicit: one #41 external finding was received before that workflow change. It is not erased or attributed to Astra. Comparison must stratify the pre-change #41 finding from the revised lane.
## #27 Product Decision approved / resumed preflight

The Product Owner explicitly approved the recommended decision: encrypted Host credential blobs permit the dedicated service plus elevated Administrators/SYSTEM. Ordinary users, activation-group members and ordinary clients remain excluded. This resolves the earlier service-only wording versus offline recovery access ambiguity in this isolated branch; canonical Issue/Project are unchanged. The earlier stopping report is a historical checkpoint, not current completion state.

SHADOW READY at d3cf320b247ced1025c89a90d6ccb9d210a91b42 on astra/27-secure-store. Current #27 body and complete invariant registry reread. Changes expected: Platform.Contracts secure-store/redacted-secret contract, Platform.Windows DPAPI/ACL implementation, SelfTest reusable store suite and real virtual-service/nonadmin integration, architecture wording and bounded documentation. No executable reference topology changes; no pairing/Owner issuance/recovery implementation/migration/Linux/WPF changes. Host/privileged offline callers own store effects under their existing same-machine lease; the platform store does not acquire a second competing Host lease or impersonate clients. Current Host initialization is #42 and no private keys are added to SQLite here.

Credential holders: dedicated Host service and authorized exceptional elevated offline caller only; per-user principal keys stay in the independent client store. Validate security of root/store/blob before use; reject permissive, missing mandatory identities, reparse or unprivileged-owned state rather than silently repairing it. Atomic encrypted-file replacement, key-bound DPAPI entropy, idempotent missing delete, bounded blobs, cancellation and redacted exception behavior require negative evidence. Secret-marked values must redact default formatting/JSON, with deliberate byte extraction for cryptographic consumers. No blanket claim that arbitrary caller-logged byte arrays are automatically redacted.

Primary invariants SEC-001, IDENT-001, CLIENT-001/002/003, LOCAL-002, PERSIST-001, HOST-001/002, PLATFORM-001 and ARCH-001/002; all other registry families audited for unchanged authority/transport/operations boundaries. Validate full build/suite plus lifecycle/restart/tamper/path/ACL/cancellation/redaction and real service-to-elevated-caller persistence, nonadmin denial, and retained #41 integration. Microsoft's DPAPI documentation confirms machine context is not a per-user security boundary; ACLs are mandatory. No live secret, remote credential or real release involved.
## #27 pre-PR validation candidate

Local Release build: zero warnings/errors. Full suite: 178/178 PASS, including four new secure-store groups. Strict docs PASS. Review of the initial tests found evidence gaps: moved the reusable contract body into a Windows-API-free file, made real elevated offline store tests explicitly hold the machine lease, and added actual symlink rejection plus update-style executable replacement before service restart. These are one pre-PR correction iteration; no automated defect found so far. Actual privileged Windows CI remains required before Pass A can be declared clean; local medium-token execution is not substituted for it.

## #27 second pre-PR correction

Candidate 85f670cf0ba953fe5ab746e9deeccd40c05ae635 passed actual Windows CI 33995319774 (service and multi-user integration plus full build/self-test) and Docs 33995321511. Review then found orphan encrypted temporary writes were not associated with a key and could survive later retirement. Added per-root in-process serialization and exact-key temporary retirement on retry/delete; file creation now installs its protected ACL atomically, avoiding a crash between inherited creation and explicit protection. The first crash-fixture test failed because copying a file then applying an unmodified ACL object did not protect the new file; corrected the fixture to create with the exact ACL. This is one automated test/fixture failure within correction iteration 2, not evidence of a released defect. Full local suite now 178/178, Release build zero warnings/errors, strict docs PASS. Updated candidate requires fresh real Windows CI before Pass A closure.

## #27 Review Pass A

Reviewed the entire ten-file diff against d3cf320b247ced1025c89a90d6ccb9d210a91b42, every changed file, current canonical #27 and the complete normative registry. Acceptance map: private material stays in encrypted files, never database DTOs (ciphertext/tamper/database checks); ordinary clients retain no Host-side project dependency and two actual non-elevated users are denied file read/write/delete; marked logging/audit/JSON payloads redact; real virtual-service -> elevated offline lease holder -> restarted/replaced service reads persist; threat limitations explicitly cover privileged ownership, snapshots and memory; plaintext legacy and corrupt/wrong-key material fail closed for later migration; portable contract tests contain no Windows/GUI dependency. No new rotation, recovery, enrollment, grants, RPC or migration implementation is smuggled into the store.

| Invariant IDs | Concrete evidence / applicability | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | Ten-file boundary; no WPF/Lan/client reference changes; topology tests pass | PASS |
| HOST-001/002, PERSIST-001 | Trusted Host-side composition only; real service lease and stopped/elevated offline lease; no online CLI path or competing lease | PASS |
| CLIENT-002/003, LOCAL-002 | Separate Host and per-user stores, protected blob ACL, real two-user denial; no client principal private keys in Host store | PASS |
| IDENT-001/003/004 | Opaque stable key refs; atomic replacement/idempotent exact-key retirement supports later orchestrators; no HostId, current-ref, staged rotation or recovery state changed | PASS boundary |
| SEC-001 | Machine DPAPI plus approved service/BA/SY ACL and owner validation, atomic protected creation, key-bound entropy, tamper/legacy/reparse denial, redaction tests and internal buffer clearing | PASS |
| LOCAL-001/003/004, OWNER-001/002 | No bootstrap/enrollment/Owner or local-channel authority introduced; approved recovery ACL is not RPC authorization | PASS unchanged |
| IDENT-002, REMOTE-001/002, PAIR-001–004, AUTH-001–005, PROTO-001 | No transport, peer/grant/state/negotiation changes; retained full suite passes | PASS unchanged |
| OPS-001–004, RECOVERY-001 | Store is a bounded primitive, no durable operation bypass; failed replacement retains current blob; orphan encrypted writes discarded only for exact-key retry/delete under serialized writer | PASS boundary |
| MIG-001, PLATFORM-001/002, LINUX-001 | No migration execution, GUI or Linux implementation; OS behavior confined to Windows seam, cross-platform contract independent | PASS |

Stale/reference search covered service-only ACL claims, client access, secure-store consumers, legacy plaintext fallback, unqualified authority and store layout. Architecture §7 is changed only for the explicit Product Owner ACL decision; historical blocked checkpoint remains labelled historical. Two pre-PR correction iterations are recorded above. One failed test fixture was corrected; no released/escaped #27 defect known. No unavailable real field behavior is labelled PASS: reboot, release-package qualification and Linux are outside this store slice, while actual privileged service/multi-user behavior was executed.
`475b092f6826a3b0c9db8c42cba752e554ba7266`: CI 33995667758 PASS (both jobs including real service/multi-user), Docs 33995668602 PASS. All required local and remotely executable validation passed; Pass A clean.

## #27 Review Pass B correction

PR #69 at 1bb28fd08b2d9565056660d5657b6ec0209da659 reviewed afresh against the exact experiment base: all ten files, acceptance map, whole invariant registry and rollback/cleanup/threat claims. Found a negative-evidence flaw unique to Pass B: restoring the original, unmodified ACL object need not write its DACL, so a later missing-SYSTEM rejection could pass because the earlier outsider ACE was still present. Fixed explicit DACL restoration and required a successful baseline read between each independent fault. The first correction attempted to write the audit-security section and failed under the real medium token; limited restoration to the DACL, requiring no extra privilege. Also normalized a trailing root separator so two trusted store instances share their gate, and added concurrent writers/retrieval/retirement coverage (179 test groups total).

One existing synthetic force-stop wiring test also failed once (expected one tracker call, observed zero) during that run; it uses a ten-second real helper process. No causal link to this store delta is established, and no legacy production fix is claimed. This observation is retained rather than hidden. Fresh full local and Windows CI results are required below. This is correction iteration 3 for #27; no escaped production defect known.
Corrected local full suite: 179/179 PASS, including the unchanged force-stop wiring test. Release build zero warnings/errors, strict docs PASS. All Pass A invariant rows re-audited against these changes: PASS; DACL fixture repair restores independent negative evidence, root canonicalization changes no authority. Await exact candidate remote validation.

## #27 Review Pass B final audit

Re-reviewed the complete final code/test diff at 0ff6eafcca90a9a0bb3d00b7e3852c1c28f46d32, including exact DACL restoration, independent success assertions and normalized-root concurrent writers. Acceptance and all invariant families from Pass A remain PASS. No outstanding substantive finding; Pass B clean after its one correction cycle (three total for #27). Corrected suite is 179/179 locally; remote CI 33995908279 has passed both substantive build/self-test and real privileged Windows integration steps, with final job teardown still pending at this recording. Docs 33995909971 PASS at that code SHA. Final job conclusion, exact PR HEAD/base and docs-only final HEAD remain explicit merge gates. No external review used.

## #27 integration checkpoint

SHADOW DONE. PR #69 final HEAD e6d005ba38e7dc641411a57082c1f5ed48ff4e77; final implementation 0ff6eafcca90a9a0bb3d00b7e3852c1c28f46d32 passed CI 33995908279 including actual privileged Windows integration; final Docs 33996000718 PASS. Exact HEAD/base verified before merge 18cb97fb61937b4a624573880c599315cacad9b3. Three correction iterations, Pass B found the independent-ACL-evidence flaw, one unchanged transient force-stop observation retained; no known escaped #27 defect. Full suite 179/179. The approved policy is implemented only in the experiment, with canonical planning unchanged.

## #42 refreshed preflight / bounded trial decomposition

#27 now removes the practical secure-store blocker. Current canonical #42 and architecture §2b/2c/3a/3b reread. Declared #33/#19/#40/#41 and practical #27/#43 dependencies are accepted. #42 is too large for one reviewable change once the complete registry's LOCAL-003/004, OWNER-002 and IDENT-004 obligations are included; it remains IN PROGRESS until all following trial-only units and full issue acceptance pass. No duplicate canonical issues are created.

1. **42a: selected Windows local-transport preflight spike.** Prove .NET 8 Kestrel named-pipe TLS, dedicated-group connect ACL, no TCP listener, certificate rejection before sensitive request delivery, and whether IConnectionNamedPipeFeature exposes exact native SID across actual users. Test/tooling only, no production authority. Acceptance is actual Windows evidence and a documented selection/limitation report. Same two-pass review and experiment PR required.
2. **42b: production authenticated local-channel and machine-publication foundation.** Platform/client seams, restricted public descriptor, existing machine identity credential via #27, protected TLS transport, startup reconciliation and explicit absent-versus-untrusted outcomes. No authority granted merely by channel access; no ordinary RPC until initialized.
3. **42c: principal authentication and enrollment/bootstrap.** Per-user key generation/store binding, connection-bound nonce/signature, transactional exact principal mapping, privileged intended-user initial ticket/handoff, Owner-only additional enrollment/removal, retry/expiry/attempt limits/ABA invalidation and minimal authorization hooks. Private client keys never enter Host state.
4. **42d: bounded offline recovery and full composition.** Owner rotate/re-home, machine credential loss/compromise reconciliation/retirement, exact-one-Owner/audit/exclusivity, executable wiring and complete real multi-user service integration. Peer/grant invalidation must preserve accepted schema semantics without introducing pairing or general delegation engines.

Primary applicable IDs across #42: ARCH-001/002, HOST-001/002, PERSIST-001, CLIENT-001–003, IDENT-001/003/004, LOCAL-001–004, OWNER-001/002, SEC-001, PLATFORM-001; all remaining families audited for unchanged boundaries per unit. No Linux, remote listener/pairing, server operations, feature screens or generic Administrator RPC. Stop for Product Decision only if the stack cannot meet accepted ACL/auth behavior or another decision exceeds allowed scope. Required #19 native-identity spike is first; Microsoft documents .NET 8 Kestrel named pipes and TLS configuration, and the installed .NET 8 reference exposes IConnectionNamedPipeFeature. Availability alone is not evidence of reliable multi-user behavior.

## #42a initial spike findings

Implemented the test-only selected-stack spike and multi-user integration extension. Automated local test found Schannel rejects EphemeralKeySet; captured diagnostic is in the trial-local evidence folder, with no production key used. DefaultKeySet synthetic loading succeeds, but creates a native CNG container, now explicitly tracked/deleted/verified absent after probe disposal. This is not silently selected as production storage. Local full suite 180/180 before the cleanup assertion; focused spike including cleanup passes. One implementation correction cycle covers the failed ephemeral-key assumption and honest native-key lifecycle. Actual two-user CI remains required; no issue #42 completion or principal authorization claimed.

## #42a Review Pass A

Reviewed all five changed files against integration 18cb97fb61937b4a624573880c599315cacad9b3: full spike, mode routing, real-user integration, preflight/decomposition/evidence. Local Release zero warnings/errors, full 180/180 suite and strict docs PASS. CI 33996302179 PASS at dbcbe8ca31d1939f1ed50bd280c8cc043306cbc9: real nonmember denial, two fresh nonadmin logons with distinct native SIDs, TLS wrong-pin requests absent from handler, explicit native-key cleanup plus retained service/store tests. Docs 33996303365 PASS. One correction cycle; failed ephemeral-key assumption found by execution; explicit native-container tracking/cleanup and scoped evidence wording found during review. No escaped defect known.

| Invariant IDs | Concrete applicability / evidence | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001, PLATFORM-001 | Test/docs-only delta; production executable dependency graph and WPF/Lan unchanged | PASS |
| LOCAL-004, CLIENT-002, SEC-001 | Real TLS before request, wrong-pin failure classified as authentication, no secret delivery, synthetic-only certificate; new native persistence finding explicitly reserved for production decision | PASS for spike |
| LOCAL-001/002/003, OWNER-001/002, CLIENT-003 | Native SID observed but grants/enrollment/principal bootstrap not implemented or inferred; no Host private key issued to a client | PASS unchanged |
| HOST-001/002, PERSIST-001, IDENT-001/003/004 | Unique test endpoint/credential only; no product database, HostId, current reference, rotation or recovery change; existing machine lease integration retained | PASS unchanged |
| IDENT-002, REMOTE-001/002, PAIR-001–004, AUTH-001–005, PROTO-001 | No product RPC/protocol/authority or remote listener; HTTP/2 probe is explicitly not logical operation implementation | PASS unchanged |
| OPS-001–004, RECOVERY-001, MIG-001, PLATFORM-002, LINUX-001 | No server operations, migration, shell action or Linux production; bounded native-key/server/test-identity cleanup | PASS unchanged |

Acceptance map: named-pipe TLS=local and real CI; ACL=actual nonmember denied before membership; native identity=two exact OS SIDs at server; untrusted endpoint=no handler calls after wrong pin; no TCP=only ListenNamedPipe configured and observed pipe address; cleanup=key absence assertion plus suite cleanup. No virtual-service-hosted Kestrel or production native-cache protection is claimed. Stale/reference search rejects describing DefaultKeySet as ephemeral/memory-only or group eligibility as authority. Pass A clean for 42a. Production #42b remains at the documented native-cache Product Decision, not silently resolved by this spike.

## #42a Review Pass B

PR #70 at 8f08fd560290a28eaf527a68b0bae9cebd9c8fe0 reviewed afresh: complete five-file diff, all acceptance/invariant rows, native identity lifetime, actual-user helper exit/result checks, TLS failure typing, handler counts, exact-container deletion and documentation limits. No further substantive finding. Pass B clean with one total correction cycle. The successful CI evidence belongs to the unchanged test implementation at dbcbe8ca31d1939f1ed50bd280c8cc043306cbc9; later changes are only docs/ledger. Every Pass A invariant row remains PASS for the test-only unit. No external review used, no escaped defect known. Production #42 is not complete and its native-key-cache decision remains unresolved; the isolated preflight unit itself has met its acceptance criteria. Exact final HEAD/base and final Docs remain merge gates.

## #42a integration checkpoint / second stopping condition

42a SHADOW DONE as a bounded preflight unit only. PR #70 final HEAD a19cfaa741aae22452cd864b0e8d3148878ab005; implementation dbcbe8ca31d1939f1ed50bd280c8cc043306cbc9; CI 33996302179 and final Docs 33996515288 PASS. Exact HEAD/base verified before merge 6934f50e49c1092788957b09e98a1ad358789446. Both review passes clean; one correction cycle. Full suite 180/180, including retained #27; no known escaped 42a defect.

#42b is SHADOW PRODUCT DECISION REQUIRED: the working Windows TLS path needs a separately persisted native private-key container, beyond the explicitly approved encrypted-blob ACL decision. The concrete recommendation and required protection/cleanup/authority constraints are recorded in developer/v0.5-local-ipc-preflight.md. No production cache, runtime upgrade, custom TLS, new machine identity or plaintext fallback has been introduced. #42c/d depend on completing that authenticated production channel and remain SHADOW BLOCKED. #42 as a whole is not complete.

Refreshed milestone issue metadata read-only. Newly complete canonical child issues are #27, #34–#38, #41, #43 (eight), plus 42a partial preflight and the separate #34 escaped-fixture correction. Ten experimental PRs #61–#70 merged total. Review correction cycles: earlier 23 + #27 three + 42a one = 27. Known escaped defect remains the #34 design fixture, corrected in PR #64; no known remaining escaped defect. Baseline 161 -> current 180 self-tests, never summing reruns.

Remaining state: #18 design container SHADOW DONE; inherited #17/#19/#33/#39/#40 unchanged accepted inputs. #42 PRODUCT DECISION (42a done, 42b pending, 42c/d blocked); #28/#44–#54 SHADOW BLOCKED on the remaining local-auth dependency chain. #55 FIELD EVIDENCE REQUIRED and not yet dependency-ready. #20 and #16 containers SHADOW BLOCKED. #21–#25 NOT REACHED behind the Windows parity hard gate and later packaging/qualification sequence. #26/#29/#30/#31 unscheduled, NOT REACHED. No currently independent authorized implementation item bypasses this stop. Canonical Issue/Project, normal-lane code/reviews, main/tags/releases/packages untouched.

## Native TLS cache decision approved / #42b1 preflight

The Product Owner explicitly approved the documented native-key-cache recommendation. It is now authorized for this isolated trial: a Windows Schannel container is a derived cache of the same DPAPI-backed machine credential, restricted to service/elevated Administrators/SYSTEM, never authoritative over SQLite or a new identity, with tracked startup reconciliation and retirement. The previous stopping report remains historical.

Split 42b into reviewable sequential units: **42b1 native cache** (this branch) implements the approved Platform seam and validates actual provider/file ACLs, key equivalence, missing-cache reconstruction, unsafe-cache rejection, restart/retirement and Schannel use under real service/nonadmin identities. **42b2 authenticated channel/public descriptor** follows using the accepted cache; 42c/d remain as already planned. No full #42 completion is claimed by either foundation alone.

Branch astra/42-native-tls-cache starts at af398cafd7759ff5c572da854a73e5c0ad164f9a. Current canonical #42 reread, live Project Backlog read-only; shadow eligibility follows the trial authorization. Primary IDs SEC-001, CLIENT-001/002, IDENT-001/003/004, HOST-001/002, PERSIST-001, PLATFORM-001, LOCAL-004; complete registry audited for untouched authority/Owner/peer/operation boundaries. Expected changes: Host-side contract/Windows implementation, reusable and privileged integration tests, architecture/implementation docs and ledger. No client dependency on Host platform, no ordinary RPC, no credential minting/rotation authorization, no Linux/WPF/runtime upgrade. Exact native API/layout are implementation details within this newly approved policy; production callers must hold the existing Host/offline lease.

## #42b1 initial native-provider validation

Initial local build passes. The first probe exposed that Windows PFX import export-policy flags need explicit volatile plaintext-export permission on the in-memory source before PKCS#8 import; that source remains ephemeral, while the persisted cache's export policy is zero. Machine-key finalization then correctly refused this local medium token (NTE_PERM 0x80090010). Native machine-cache tests therefore run only in the explicit privileged Windows integration entry point; they are not skipped and labelled PASS in the ordinary local suite. Real CI evidence is pending. Native API definitions were checked against installed Windows SDK ncrypt.h and Microsoft documentation; provider ACL is set before finalization, with no overwrite flag.

## #42b1 expanded validation and pre-review corrections

CI 33997788664 at 091e1bc2c4dd388b9065459a0ba595ecd60f4fa3: initial privileged native-cache lifecycle and retained Windows integration PASS; ordinary suite 179/180 due to the previously observed force-stop fixture (expected tracker call 1, observed 0). No cache failure occurred in that run. Investigation found the test did not establish that the newly launched process was observable before StopAsync, whose already-stopped path intentionally registers no operation. The fixture now explicitly waits for that precondition and fails with a specific diagnostic if it never occurs; legacy production code remains frozen. This is a test-evidence repair, not a claim of a demonstrated Windows scheduler root cause for prior runs.

Expanded pre-PR tests now exercise actual Schannel use, mismatched/missing authoritative keys, unsafe file permissions with independent restoration, real service/offline cache reuse, service normal/crash restart and actual nonadmin provider/file denial. The service fixture initializes native crypto only after SCM Running, retains its Host runtime lease, and stops TLS before disposing it. Code inspection tightened the accepted curve to exact NIST P-256 and validates persisted export policy; failed native imports dispose their handles. Local full suite at 14ba463: 180/180 PASS. Exact expanded privileged validation pending. These changes form pre-PR correction cycle 1; the two initial native-provider implementation failures are recorded above.

## #42b1 service-evidence corrections

Expanded CI 33998163313 (14ba463) passed all 180 ordinary tests and the elevated native-cache TLS/negative lifecycle, then exposed a fixture assumption: the virtual account has no SCM QUERY_STATUS grant on itself under the accepted minimal service DACL. The elevated harness now observes Running and sends a PID-bound, protected readiness signal to its test worker. No service ACL expansion was made for that test. CI 33998275304 (5d6941c) then reached native finalization and failed NTE_PERM under the actual virtual account; this is evidence of a missing provisioning permission, not a TLS or ACL validation pass.

Correction cycle 2 adds an elevated, create-only, non-inheriting permission for the dedicated virtual-service SID on the machine KSP directory. It does not grant access to unrelated keys. This is explicit provisioning within the approved native-cache policy; it does not add an authority holder. Added cancellation/invalid-reference and weakened-export-policy tests; tightened callback-ACE rejection, native resource cleanup and pre-write path validation. An explicit stale-container assertion now checks service startup retirement, and cleanup verifies removal of the unique provisioning grant. Required actual service evidence remains pending; no acceptance pass is inferred from compilation or a privileged-user-only test.

## #42b1 native creation failure investigation

CI 33998479709 (577854e) and diagnostic rerun 33998591754 (adebe7f) failed actual service finalization. The latter preserved the primary NTE_PERM failure and showed the KSP-created file still inherited World read / SYSTEM and Administrators full control, with service ownership. The primary error had previously been obscured by reconciliation correctly refusing that unsafe file during cleanup. Therefore the create-only grant alone is insufficient and no protection/lifecycle pass is claimed. Diagnostics contain only synthetic identifiers and ACLs, never private material.

A bounded precreation probe (46ee384, CI 33998752294) found `Unique Name` unavailable before finalization (NTE_NOT_SUPPORTED 0x80090029), also reproduced locally. It was removed. The next candidate tests the documented creation-time Security Descr property without `NCRYPT_PERSIST_FLAG`, which is specified for persisted objects. These failed investigations are retained as pre-PR correction cycle 3. Review Pass A is not yet clean; there is no PR or integration merge for #42b1.

## #42b1 corrected Windows evidence

The final creation flags are DACL_SECURITY_INFORMATION only (4); the provider assigns the creator's default owner, and the cache independently verifies that owner. Requesting OWNER alongside DACL had caused finalization to fail under the virtual service token. No owner-setting privilege, broader directory inheritance, helper service, global key access, runtime upgrade or policy relaxation was introduced. The failed precreation probe is absent from the final implementation.

CI 33999036023 at 6ec9b9c passed both jobs: 180/180 ordinary tests, elevated cache TLS/negative lifecycle, actual virtual-service TLS, normal and killed-process restart with the same key/pin, elevated offline reopen/rebuild, service startup removal of a stale key, two actual nonadmins denied provider/file access, retained #27/#41/#42a behavior, and complete fixture cleanup. The successful run did not need the test-only fallback for rejected containers. Release builds are warning/error free; local ordinary suite 180/180. Pass A then removed the now-unused requested owner from the creation descriptor and made fixture secret clearing unconditional on cleanup failure. This is a small evidence/code clarity correction within cycle 3; fresh validation follows.

## #42b1 Review Pass A audit

Reviewed every changed file against af398cafd7759ff5c572da854a73e5c0ad164f9a, current #42, the explicit native-cache approval, and the entire normative registry. This is the cache/provisioning primitive and its real Windows proof, not a partial claim that all of #42's principal/bootstrap/recovery criteria passed. Acceptance maps to: authoritative retrieval before cache use and key equality (missing/mismatch tests); protected native provider and file ACLs (real service/admin and actual two-user denial plus unsafe ACL rejection); native non-exportability (positive signing and rejected export/weakened cache); repeat use and recovery from cache loss (normal/crash service restart, offline rebuild); tracked retirement (HostId namespace and authoritative retained set, actual stale key removed before service readiness); lifecycle/exclusivity (real online/offline lease contention and cleanup); no private logging/SQLite transfer (fixed diagnostics, no private DTO, source buffers cleared).

| Invariant IDs | Evidence / applicability | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | Only Host-side Platform contract/Windows implementation and SelfTest additions; legacy change only establishes the synthetic test precondition; no WPF/Lan/Core production edits; topology suite retained | PASS |
| HOST-001/002, PERSIST-001 | Trusted Host/offline seam; existing machine lease held by service runtime or stopped elevated runner; no additional authority process, online CLI or SQLite writer | PASS |
| CLIENT-002/003, LOCAL-002 | Host key stays outside clients/per-user credential store; actual two nonadmins denied native open and file read/write/delete; client dependency graph unchanged | PASS |
| IDENT-001/003/004 | HostId-separated cache names, authoritative key equality, no cache-as-recovery fallback; retained-set/retirement primitive tested; durable rotation/recovery orchestration explicitly remains #42b2/d | PASS boundary |
| SEC-001 | Microsoft software KSP, DACL before finalization, validated default owner/protected provider and file ACL/export policy, no UI, cleared transient buffers, no private diagnostics; actual service proves this is not merely an elevated-user success | PASS |
| LOCAL-004 | Actual pinned Schannel HTTP/2 named-pipe TLS works with this cache; existing wrong-pin rejection retained; production authenticated channel and descriptor remain #42b2 | PASS boundary |
| LOCAL-001/003, OWNER-001/002 | Synthetic readiness fixture creates no principal/Owner or product authority; group access is only transport eligibility; bootstrap/recovery policy unchanged | PASS unchanged |
| IDENT-002, REMOTE-001/002, PAIR-001–004, AUTH-001–005, PROTO-001 | No cross-Host identities, peers, grants, pairing, ordinary RPCs or negotiation changes | PASS unchanged |
| OPS-001–004, RECOVERY-001 | No server/operation executor; native failures reject use, unsafe leftover state rejects reconciliation; caller must stop TLS before retirement; no cross-store atomicity or blind operation resume claim | PASS boundary |
| MIG-001, PLATFORM-001/002, LINUX-001 | Native APIs confined to Windows seam; no interactive service UI, migration or Linux implementation; parity/reboot/release field gates remain later | PASS |

Stale/contradictory search covered cache-vs-authority, native key ownership, encrypted-blob-only claims, unresolved approval labels, atomicity, generic admin authority, client dependency/credential access and production-completion claims. Marked the earlier preflight decision heading explicitly historical; the new approval and §7 cache policy are current. Three pre-PR correction cycles are recorded, including automated native/provider/fixture failures and code-review resource/policy/evidence fixes. No known escaped #42b1 defect (unmerged). Production recovery/rotation completion, real reboot, Palworld execution and release qualification are not claimed by synthetic credential/service tests.

Code candidate 59173b754d874d15a8d456f962834f644f729a94: Docs 33999277959 PASS; required fresh CI/local results still pending before declaring Pass A clean and opening a PR.
Local corrected Release build (zero warnings/errors), 180/180 ordinary self-tests, and strict docs PASS. CI 33999275841 at 59173b754d874d15a8d456f962834f644f729a94 PASS, including actual service/multi-user/cache integration. All Pass A rows remain PASS. Pass A clean after three pre-PR correction cycles; no external reviewer used. Only documentation/ledger changed after this validated code candidate.

## #42b1 Review Pass B correction

Fresh review of PR #71 at exact HEAD 89b6563e1d7a7d492d6d3663dd5ae97f094362d7 against unchanged experiment base af398cafd7759ff5c572da854a73e5c0ad164f9a re-read the complete implementation/tests/docs, acceptance map and all registry families. Two test-evidence weaknesses were found only in this pass: fallback fixture cleanup could log failure yet permit a green test if production reconciliation returned successfully but left a key; and a recycled process ID could make a prior readiness file look current after restart. Fixed the fallback to explicitly fail after cleaning every known fixture key, and clear prior readiness/start markers in the synchronous fixture startup before launching its worker. No production authority/storage behavior changed. The earlier successful CI did not use fallback cleanup; no observed false pass is asserted retroactively.

This is correction cycle 4 (three before PR, one in Pass B). Re-audited every Pass A invariant row: all retain the same boundary/evidence classification. Fresh full local and actual Windows validation is required before repeating final Pass B. No external review, new Product Decision or escaped production defect.
The same fresh cross-section review also corrected §3b's absolute statement that the private key never leaves the Host process: that conflicted with the already-approved offline caller and native provider. It now states the actual boundary—no ordinary client or remote peer receives the private key; only the approved Host/offline/platform consumers use it. This applies existing Product Owner decisions, adds no holder, and changes no implementation. Included in Pass B correction cycle 4 and the full SEC-001/CLIENT-002/PERSIST-001 audit.

## #42b1 Review Pass B final audit

Re-reviewed the full final PR diff at e30c289c8e2a226ba832976754634451d00aeb5f, not just the correction, against af398cafd7759ff5c572da854a73e5c0ad164f9a. The final executable/test implementation is 43e32dde76c0e703d4f3d9b7deda1121c08bce6f; the later change is the approved-consumer wording and its ledger record. CI 33999463411 PASS at that exact code SHA (both full suite and actual service/multi-user/cache integration); Docs 33999512242 PASS at e30c289. Local Release build zero warnings/errors, full suite 180/180, strict docs PASS. No fallback cleanup was needed in the accepted execution.

All Pass A matrix rows remain PASS with their explicit boundary/unchanged qualifiers. Rechecked native import flags/cleanup, protected ownership and mandatory identities, non-exportability, missing/mismatched authority, stopped-connection retirement, cancellation, root/path checks, create-only setup and removal, native default-owner semantics, client project isolation, actual two-user evidence, service startup sequencing and fresh readiness. No unresolved substantive finding or Product Decision. Pass B clean after one correction cycle, four total for #42b1. The new unit is ready for an experiment-only merge; full #42 remains incomplete. Final documentation/ledger HEAD and unchanged experiment base must still be checked at merge.

## #42b1 integration checkpoint

#42b1 SHADOW DONE as the approved native-cache/provisioning primitive. PR #71 final HEAD ef03650b83a2e39e0331ca2c03c6e4b8929021a2, code/test SHA 43e32dde76c0e703d4f3d9b7deda1121c08bce6f, merged bb96c8db7e9ddaf751fffa4c19d63d153d347957 after exact HEAD/base checks. CI 33999463411 and final Docs 33999595998 PASS. Four correction cycles (three pre-PR, one final review); both reviews clean; no known escaped #42b1 defect. Test count remains 180 ordinary groups, plus explicit real Windows integration; native tests are not miscounted as ordinary tests or field qualification.

Native-cache approval is resolved and implemented, not a current stop. Eleven experiment PRs #61–#71 merged, eight canonical child issues completed, plus 42a/42b1 partial foundations and the separate #34 design-fixture correction. Total review correction cycles now 31 (prior 27 + four). The known escaped #34 fixture defect remains fixed; no known remaining escaped defect. Canonical #42 remains incomplete; continue with #42b2 public descriptor/authenticated local channel, then #42c/d and downstream dependencies. No main/normal-lane or canonical Issue/Project changes.

## #42b2 preflight: protected public descriptor and local TLS adapters

Branch astra/42-local-trust-channel starts at eb1a02c (accepted #42b1 checkpoint). Current canonical #42 reread; prerequisites #33/#19/#40/#41 and trial #27/#43/#42a/b1 are satisfied. This unit implements public descriptor publication/read validation and Windows TLS transport adapters. Authoritative SQLite-driven startup/rotation/recovery composition and production executable integration remain with #42c/d; no machine credential or Owner is minted by a transport adapter.

Keep the accepted project graph: Host-side Platform contracts own publication inputs; client-side BCL contracts own validated trust anchors and connection errors. The two public shapes share the documented versioned JSON artifact format, tested by cross-boundary round trips, without a client dependency on Host/Core/Host Platform or a Host dependency on client-platform contracts. Windows adapters enforce protected file/directory ownership and ACLs, fresh descriptor reads on each connection, expected HostId/current-or-pending public-key pinning, local-only named pipes and standard TLS/HTTP2. Kestrel wiring/native SID extraction live in Platform.Windows; business handlers stay outside that adapter. No remote listener, principal capability or group-derived enrollment is introduced.

Expected files: Host/client platform contracts and Windows implementations, Windows adapter framework reference for Kestrel (no project-edge changes), SelfTest/explicit Windows integration, implementation documentation and ledger. Primary IDs LOCAL-004, LOCAL-003, CLIENT-001/002/003, LOCAL-002, HOST-001/002, PERSIST-001, IDENT-001/003/004, SEC-001, PLATFORM-001/002. Audit every other registry family unchanged. Prove real nonadmin public read but denied write/delete; unsafe ownership/ACL/path/format rejection; same-machine pinned TLS with current and staged credentials; TLS mismatch never delivers sensitive request bytes; endpoint absence distinguished from authentication failure; cancellation/cleanup; existing native cache/service and full suite retained. Review Pass A, then experimental PR, then fresh Pass B remain mandatory.

## #42b2 implementation and pre-PR corrections

The adapter implementation and [artifact contract](../developer/v0.5-local-trust-channel.md) are present. The first local fixture used a user-TEMP ancestor that the security check correctly rejected; it now uses a unique explicitly protected ProgramData tree. A discarded manual TLS/h2c attempt failed HTTP/2 scheme validation; the final adapter uses HttpClient standard HTTPS TLS over the raw local pipe. The initial expanded suite passed 182/182. No production identity/rotation coordinator is claimed.

Pre-PR correction cycle 1: implementation tests exposed those fixture/transport failures. Review then added raw ACE-form checks, fresh directory metadata, explicit attribute lookup (Exists can hide access failure), dangling-reparse rejection, per-request URI/version validation even for pooled connections, and disabled TLS resumption to force fresh pin validation. Expanded negative evidence covers protected orphan cleanup and no-clobber on malformed publication. The first orphan fixture copied the file then failed to apply a modified DACL; it was corrected to explicitly mark the access section for persistence. No public security rule was weakened.

Pre-PR correction cycle 2: handshake failure classification needed to cover plaintext/EOF and silent connected endpoints, not only nested AuthenticationException. A per-request connection-attempt record now separates pipe absence, pre-TLS failure/timeout and post-handshake HTTP errors; caller cancellation remains cancellation. Plaintext and silent-endpoint probes verify explicit authentication failure and no sensitive body delivery. Actual service publication, two-user public read/no-write, production client TLS and dangling-symlink rejection have been added to the explicit Windows harness; remote execution is pending. Review Pass A is not yet declared clean.

## #42b2 Review Pass A invariant and acceptance audit

Complete diff reviewed against eb1a02c, including all 16 files, both independent security validators, standard TLS client/server adapters, public contract/parser, real service harness, local negative tests and documentation. Acceptance for this bounded primitive maps to versioned host/client round trip and malformed schema rejection; protected file plus ancestor checks; real publication/read/no-write fixture; TLS current/pending pins and expected HostId; absence versus plaintext/silent handshake failure; cancellation, failed-input preservation and orphan retirement. Full #42 principal/Owner/recovery criteria are explicitly deferred to 42c/d, not marked passed.

| Invariant IDs | Evidence / applicability | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | WPF/Lan/Core production untouched; no project-edge change; independent client BCL contract and Windows reader; architecture guard retained | PASS |
| HOST-001/002, PERSIST-001 | Publisher is trusted Host/offline platform primitive; caller retains existing lease; no new authoritative store/writer or client mutation path | PASS boundary |
| CLIENT-002/003, LOCAL-002/004, SEC-001 | Public SPKI hashes only; private machine key supplied from approved cache; standard TLS before HTTP; no secrets in descriptor/logging; fresh pin/HostId checks and explicit handshake errors | PASS boundary |
| LOCAL-001/003, OWNER-001/002 | Pipe/group/native SID creates no enrollment or authority; no bootstrap, Owner mutation or RPC added | PASS unchanged |
| IDENT-001/003/004 | Stable HostId distinct from current/pending fingerprints; file is derived, never authority; primitive cannot select/rotate/recover keys; full SQLite startup reconciliation remains 42c/d | PASS boundary |
| IDENT-002, REMOTE-001/002, PAIR-001–004, AUTH-001–005, PROTO-001 | No remote listener, cross-Host routing, grant/pairing engine or negotiation changes; schema version explicitly rejected if unsupported | PASS unchanged |
| OPS-001–004, RECOVERY-001 | No operational executor; atomic file replacement has explicit commit boundary; validated orphan cleanup, no cross-store atomicity or blind operation recovery claim | PASS boundary |
| MIG-001, PLATFORM-001/002, LINUX-001 | Windows details confined to platform adapters; Kestrel wiring seam adds only framework reference; no migration, interactive service UI or Linux implementation | PASS |

Stale-reference search covers descriptor authority, certificate versus SPKI pin, production/completion claims, pending current/abort semantics, private-key consumers, group-derived authority, missing-endpoint retry and external-review requirements. Historical test-only certificate fingerprint remains explicitly named PublicPin; production uses PublicKeyPin/SPKI. The service fixture comment now correctly places identity composition in 42c/d. Earlier stop/checkpoint sections remain historical; approved cache decision is resolved. No newly unresolved Product Decision. Actual privileged run is pending; Pass A cannot be declared clean before that evidence.

Code candidate 31b5bcf6ba2818ca6b92a4691efb1563a893197b: local Release build zero warnings/errors, full suite 182/182 and strict docs PASS. Docs 34000994127 PASS. CI 34000993054's explicit Windows job 101399714716 PASS: real service publication, actual two-user public read/write-delete denial, production client pinned TLS, dangling-symlink rejection, retained service normal/abnormal restart and native-cache tests, and complete fixture cleanup. No fallback cleanup reported. The ordinary CI job is still running; overall Pass A awaits its result.

CI 34000993054 completed PASS at exact code candidate 31b5bcf6ba2818ca6b92a4691efb1563a893197b, both full suite and explicit real Windows integration. All audit rows retain their stated PASS/boundary classifications. Review Pass A is clean after two pre-PR correction cycles. No external reviewer; no known escaped defect. Only implementation documentation/ledger changed after that code candidate. Proceed to experiment-only PR and a fresh exact-HEAD Pass B.

## #42b2 Review Pass B correction

Fresh complete-diff review of PR #72 at ee36c3dde60fa4a0a3ed3009df3cb8653f58bb55 found shared mutable DirectoryInfo metadata in the otherwise reusable client reader. Concurrent connections could refresh/read that unsynchronized cache simultaneously. No observed trust bypass is claimed, but the per-read validation should own its metadata. Changed the reader to retain only its immutable normalized path and instantiate metadata per read. Added four concurrent readers against repeated atomic replacement, asserting every snapshot is a complete current or staged publication. This is correction cycle 3 (two pre-PR, one Pass B), found only during review. The complete invariant matrix was re-evaluated and remains unchanged; required fresh validation follows. A local ledger encoding error during the earlier audit append was caught by MkDocs and fixed before PR; final diff retains original ledger line endings and adds only the intended new entries.

The new concurrent-reader/replacement test subsequently exposed MoveFileEx returning Windows error 5 under overlapping reads: local full suite 181/182, despite the single focused probe passing earlier. This is an actual publication defect found by the stronger Pass B test, not an environment waiver. Added bounded sharing/access-error retry with cancellation and root/target revalidation on every retry; no delete-before-replace, permission relaxation or partial contents. Retry exhaustion still fails closed and removes the owned temporary. This is correction cycle 4 (second in Pass B). CI 34001281687's privileged job passed; the ordinary job result is retained separately, never relabeled. Fresh corrected tests and full audit are required.

CI 34001281687 and Docs 34001283082 both ultimately passed at 3e127dd; that does not erase the local concurrent-publication failure. The corrected retry also has explicit locked-reader exhaustion and cancellation tests proving the old file survives and temporary files are removed. Re-audit: LOCAL-004/SEC-001 continue to require the protected opened object, IDENT/PERSIST remain derived-publication-only, RECOVERY/OPS now include bounded transient retries and fail-closed exhaustion, and every untouched matrix family remains unchanged.

## #42b2 repeated final review

Re-reviewed exact PR HEAD 7b25855047d5ac18aad660ab3883206d9f51b3cd against unchanged base eb1a02ca79f9de3b617067512d166c1816bce230. The full 16-file diff, not only the last patch, was reconsidered for every audit family: project isolation; public-only data/credential holders; caller-owned lease/SQLite authority; protected ACL/path/handle checks; current/pending identity pinning; standard TLS/HTTP ordering; per-request error classification; native SID without authority; cancellation/resource cleanup; concurrent reader/replacement failure; scope and field-evidence limits. No further substantive finding. Three consecutive local expanded trust probes PASS, including overlapping reads/replacement and locked-reader exhaustion/cancellation. Final full local/CI validation still must pass before declaring Pass B clean or merging. Four correction cycles total: two pre-PR, two after PR; the second post-PR defect was found by a test added due to the first review finding.

Final validation at code/test SHA 7b25855047d5ac18aad660ab3883206d9f51b3cd: Release build zero warnings/errors, local full suite 182/182, strict docs PASS; CI 34001487926 PASS in both jobs, including the corrected concurrent-publication tests and real service/multi-user lifecycle; Docs 34001489828 PASS. All final-review invariant rows remain PASS with their explicit unchanged/boundary qualifiers. Pass B clean after two post-PR correction cycles, four total. No unresolved product decision or known escaped defect. Final ledger-only HEAD must still match the PR and the experiment base at merge; no external GitHub Codex reviewer used.

## #42b2 integration checkpoint

#42b2 SHADOW DONE as the protected public-artifact and Windows TLS adapter unit. PR #72 final HEAD 67b1ccadc44ce80eb6f9a5107ac5b5dd8e2b45ff merged as cc11e982f1e09901bdc532d646d999ccec80afc2 after exact HEAD/base checks. Final code/test SHA 7b25855047d5ac18aad660ab3883206d9f51b3cd; CI 34001487926 and final Docs 34001621719 PASS. Four correction cycles, both reviews clean. The real publication concurrency defect was caught and fixed before merge; no known escaped #42b2 defect.

Twelve experiment PRs #61-#72 merged; eight canonical child issues complete plus 42a/42b1/42b2 foundations and the separate #34 correction. Total review correction cycles 35. Full suite 182 ordinary groups plus explicit actual Windows integration. Canonical #42 remains incomplete; downstream #28/#44-#54 remain blocked on the remaining local-auth chain. Windows parity #55 and Linux/release gates remain untouched. Current #42 issue body and Project (Backlog, read-only) refreshed; accepted dependencies are present in this lane. No canonical Issue/Project, normal-lane code/reviews, main/tags/releases/packages changes.

Refine the already authorized 42c decomposition before execution: **42c1 per-connection principal authentication** (selected client P-256 generator/signing, shared challenge frame, exact persisted principal lookup, native SID cross-check, nonce lifetime/consumption, fresh-state identity/Owner hook); **42c2 bootstrap/enrollment and revocation** (transactional tickets/verifier consumption, one Owner, bounded attempts/expiry/retry, ABA invalidation, audit and existing grant-subtree invalidation without a general grant-issuance engine). **42d** retains privileged offline preparation/recovery and complete production composition, including authoritative machine-credential startup/publication reconciliation, protected handoff and real multi-user principal/recovery tests. Each unit receives its own preflight, validation and two-pass review. No full #42 completion is inferred from these primitives.

## #42c1 preflight: per-connection LocalPrincipal authentication

Branch astra/42-local-principal-auth starts at bdc1907e776c42e2c35e6c57d72122e4a2a13a8b. Current #42 and architecture section 3a were reread; its allowed algorithm/framing decision selects BCL ECDSA NIST P-256 with SHA-256, fixed 64-byte IEEE P1363 signatures, SPKI public keys and PKCS8 client private keys. A versioned, domain-separated challenge binds HostId, LocalPrincipalId, a random connection binding and a fresh random nonce. Single consumption and a 30-second monotonic deadline bound the challenge; a new challenge invalidates the prior attempt. Native OsPrincipalRef is supplied only by the already-proven platform seam.

Expected files/layers: Contracts public challenge/verification framing; BCL-only Client.Platform.Contracts production generator/signing helper (no OS dependency and no Host project path); Host.Persistence read-only active-principal lookup; Host per-connection authentication state; isolated tests and docs. No project-edge change, private-key import on the Host side, Host/private-store writer, Owner creation/mutation, operational executor or remote listener. Principal authentication is distinct from capabilities; the fresh-state identity hook does not implement the general permission engine. Future mutation handlers must perform their authorization check within their own transaction.

Protected-effect paths: challenge issuance, signature verification, replay/expiry, reauthentication, native SID mismatch, current persisted key/state changes, cross-connection/Host/principal substitution, malformed/noncanonical key/frame/signature input, client key generation/store/reload, and disposal/concurrent verification. Host stores/verifies only public material; ordinary clients continue using their own DPAPI credential store. Test fixtures may seed isolated rows but cannot represent real enrollment/recovery acceptance. Actual two-user principal/bootstrap/recovery service qualification remains the later complete-composition gate; existing real Windows platform tests remain required for this unit.

Primary audit IDs LOCAL-001/002/003/004, CLIENT-001/002/003, IDENT-001, OWNER-001/002, SEC-001, HOST-001/002, PERSIST-001, PLATFORM-001/002; all other families unchanged and audited. Acceptance: exact active principal mapping; wrong key/native SID/Host/principal rejected; unknown or revoked user rejected despite transport eligibility; nonce replay/expiry/cross-connection rejection; current-key/state change invalidates prior authentication; concurrent verification succeeds at most once; private key stays client-side; identity hook never manufactures grants; disposal closes authentication. No bootstrap/enrollment, offline recovery, production listener integration, Linux/WPF or general grant engine in this unit. Required full build/suite/docs, remote CI, two-pass review and exact experiment merge gate.

## #42c1 implementation and initial validation

Implemented the bounded [principal-authentication primitive](../developer/v0.5-local-principal-authentication.md): independent client-side P-256 key/signature handling, public Contracts frame/verifier, read-only SQLite lookup and disposable per-TLS-connection Host state. Nonces are connection/Host/principal bound, expire monotonically, and are consumed before verification. Native mismatch is rejected through an enum-only reporting hook. Current identity/key/Owner state is re-read on every authenticated use; the hook is not a grant or a cached authorization token.

Focused tests pass. Initial test-evidence correction: a private-material absence check attempted to read the live SQLite file with incompatible file sharing. It now closes the isolated writer and clears its pools before inspecting quiescent persisted Host files; client files live in a separate sibling test root. This is pre-PR correction cycle 1, not a production failure or a live-WAL backup claim. Code review also made the repository connection explicitly read-only, with a missing-root no-create test, and added strict ASCII rejection to the signer. Full local/remote validation and Review Pass A remain pending.

Pre-PR correction cycle 2: substantive review found that checking the Initialized flag alone did not reject a malformed initialized database with no active Owner, and default pooling could retain read-only database handles beyond the Host writer's shutdown cleanup. Added the Owner-cardinality predicate to the same query snapshot and disabled pooling for these owned lookups. New tests reject an active row on an uninitialized Host and an ownerless initialized Host, and verify exclusive file access after closing only the Host writer pool. The prior uninitialized fixture had no row at all, so its rejection did not prove the initialization predicate; that evidence gap is now closed. Added a synthetic committed Owner-state transition to prove the identity hook reads current Owner status and revocation, plus a DPAPI-reloaded production client key authenticating against its actual persisted public verifier. These are primitive tests, not recovery/enrollment ceremony or multi-user field passes.

## #42c1 Review Pass A invariant audit

Reviewed all eight changed files against bdc1907e776c42e2c35e6c57d72122e4a2a13a8b, current #42, architecture sections 2/3/5/7/8 and the entire registry. Enumerated challenge issuance/consumption, current-state lookup, signature/key parsing, callback failure, native cross-check, disposal/concurrent requests and every private-material consumer. No general capability or Owner mutation surface exists in this implementation.

| Invariant IDs | Evidence / applicability | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | Existing WPF/Lan/Core unchanged; zero new project edges; client crypto remains BCL-only without Host dependencies | PASS |
| LOCAL-001/003, OWNER-001/002 | Same-query initialized/one-Owner/active checks; native mismatch and unknown/revoked denial; no enrollment or Owner writes; current identity hook is not authority issuance | PASS boundary |
| LOCAL-002, CLIENT-002/003, SEC-001 | Client-only PKCS8 generation and per-user DPAPI reload; Host imports only canonical SPKI; private-format rejection and quiescent Host-file absence test; errors/reporting expose only generic text/enum | PASS |
| LOCAL-004, IDENT-001 | Constructor belongs to an already-authenticated TLS connection, with platform-derived native identity; frame binds expected HostId/principal/random connection/nonce; replay/expiry and client cross-Host/principal checks | PASS boundary |
| HOST-001/002, PERSIST-001 | Read-only, non-pooled lookup under caller-owned lease; no state mutation or database creation; independent connections; existing Host/offline ownership unchanged | PASS |
| IDENT-002/003/004, REMOTE-001/002, PAIR-001-004 | No cross-Host server identity, machine-key rotation/recovery, pairing or remote actor path added | PASS unchanged |
| AUTH-001-005, PROTO-001 | No grants or capability decisions; current Owner flag is a snapshot requiring transactional use; signing domain version is exact, with no app-version comparison | PASS boundary |
| OPS-001-004, RECOVERY-001 | No operation executor; failed proof consumes nonce and clears authentication; disposal closes state; real bootstrap/recovery and operation recovery remain later units | PASS unchanged |
| MIG-001, PLATFORM-001/002, LINUX-001 | No migration/interactive service action/Linux implementation; OS identity supplied through existing Windows seam; shared BCL signing is not a Linux production implementation | PASS |

Stale/contradictory search covers key holders, private/public formats, first-connection Owner claims, group-derived authority, cached identity/Owner flags, initialized-versus-ownerless state, read-only handle lifetime, production wiring claims and deferred recovery. The implementation document explicitly preserves the remaining #42 gates. Initial full local suite 186/186 passed before the stronger review tests; corrected full local/remote validation remains pending. Review Pass A is not yet declared clean.

Corrected code/test SHA dbd1519ab20e0d94464eef3384a6a82e9ca6da4a: local Release build zero warnings/errors, 186/186 full self-tests and strict docs PASS. CI 34002397371 PASS (both ordinary and retained actual Windows service/multi-user integration), Docs 34002398130 PASS. Initial candidate 0375030 also had CI 34002222778 and Docs 34002223710 PASS; those results did not waive the subsequent review corrections. The complete diff and all matrix rows were rechecked after the corrections; Review Pass A clean after two pre-PR correction cycles. No unresolved Product Decision or known escaped defect. Open the experiment-only PR and perform fresh Pass B.

## #42c1 Review Pass B layering correction

Fresh review of PR #73 at 2e0adf1aa00ff74cbee314a9d47e145f0b70be6e re-read the complete diff against bdc1907 and revisited the exact architecture project roles. The contract assemblies had accumulated actual signing/verifying implementation rather than only public framing and seams. Moved the pure public-proof verifier into new BCL-only Core domain code, retained only the challenge encoder in Contracts, added ILocalPrincipalChallengeSigner to client contracts and moved the selected generator/signer implementation into Client.Platform.Windows. Host retains all identity/trust/lifetime decisions; ordinary clients acquire no path to Core or Host. No wire or key format changed.

This is correction cycle 3, the first post-PR cycle, found only during review. The impact expands to one additive Core source file, with no changes to existing Core/WPF/Lan behavior, project edges or package dependencies. Re-audited every Pass A matrix family: ARCH/CLIENT/PLATFORM now reflect correct implementation placement; SEC/LOCAL/OWNER/PERSIST proof, ownership and read-only behavior are unchanged; all remote/peer/grant/operation/migration/Linux boundaries remain unchanged. Focused principal probes PASS. Full validation is rerun before repeating final Pass B.

## #42c1 repeated final review

Repeated Review Pass B at exact code/test HEAD b23d28f7b565da4a39943d57349027e9fa1aff84 against unchanged experiment base bdc1907e776c42e2c35e6c57d72122e4a2a13a8b. Read every file in the complete ten-file diff, including the moved implementations, public frame/seam, persisted lookup, connection state, negative tests, documentation and historical ledger. Deliberately rechecked key holders/formats, connection and native identity binding, expiry/replay/concurrency, failed reauthentication and callback errors, disposal, fresh key/Owner state, no-create/non-pooled persistence, one-Owner gating, project-role isolation and deferred production/field criteria. No further substantive finding. All registry families in the Pass A matrix remain PASS with their boundary/unchanged qualifiers, adjusted for the additive pure Core verifier and Windows client implementation. Stale-reference search found no remaining production reference to the removed contract implementation; earlier ledger placement descriptions are historical and superseded by the explicit Pass B correction.

Final validation: local Release build zero warnings/errors, 186/186 ordinary tests, strict docs PASS; CI 34002763084 PASS at the exact code SHA in both jobs, including retained actual Windows service/multi-user integration; Docs 34002764561 PASS at the same SHA. Pass B clean after one correction, three total cycles for this unit (A2/B1). No unresolved Product Decision or known escaped defect. Bootstrap/enrollment/recovery and final real multi-user principal composition remain incomplete, never inferred from synthetic identity fixtures. Final ledger-only PR HEAD and unchanged integration base must still be checked before merge.

## #42c1 integration checkpoint

#42c1 SHADOW DONE as the per-connection principal proof foundation. PR #73 final HEAD 6c52707282653400c126dc61d8c2e9028a03cc63 merged as 2b35f4aabfa0add93c72c3acd9489f1b31a27c0b after exact HEAD/base checks. Code/test SHA b23d28f7b565da4a39943d57349027e9fa1aff84; CI 34002763084 and final Docs 34003178083 PASS. Three correction cycles (A2/B1), both reviews clean, no known escaped defect. Thirteen experiment PRs #61-#73 merged; eight canonical child issues complete plus four #42 foundations and the separate #34 correction. Total correction cycles 38. Full suite 186 ordinary groups plus retained actual Windows integration. Canonical #42 remains incomplete; proceed to 42c2 transactional bootstrap/enrollment/revocation, then 42d offline recovery and complete production composition. No main, normal-lane, canonical Issue/Project, tag/release/package mutation.

## #42c2 preflight: transactional local enrollment lifecycle

Branch astra/42-local-enrollment starts at a39f5908507d38ff0f99e46122c0e734134a0d9d. Canonical #42 body/Project refreshed read-only; accepted dependencies and four prior #42 foundations remain present. Architecture sections 2c, 3a, 5, 7 and 8 determine the transaction and retry behavior. This bounded unit implements persistence and online Host seams, not final executable RPC or privileged preparation/handoff composition (42d).

Expected changes: additive Core HMAC/verifier and trusted actor-evidence types; Host.Persistence ticket lifecycle transactions and grant-subtree invalidation; Host secure-store consumer and online authentication adapter; isolated tests, docs and ledger. Preserve the exact project graph and existing schema; raw bearer secrets never enter persistence APIs, only redacted keyed verifiers. A stable independently generated secure-store HMAC key is separate from the rotatable TLS credential; online reads never create or silently replace it. Actual offline privilege/lease/key provisioning and intended-user handoff remain 42d. The persistence preparation primitive requires the caller's offline lease, like the accepted identity primitive, and is not an RPC.

Protected effects: initial Owner creation/Initialized transition; additional Owner-authorized enrollment; revoked-row reactivation; wrong-code attempt counter; consumed-result read; principal revocation; grant-subtree invalidation; dependent-ticket invalidation and same-transaction audit. Recheck current authenticated Owner identity/native binding/key inside each authorizing write transaction. Immediate SQLite writer transactions serialize competing consumption, failure counters and revocation. First use requires correct native identity, verifier, expiry and expected target state; ten wrong codes lock out first consumption. Correct consumed retries bypass expiry/lockout/new-authority gates and return only the recorded ID without key/state/authority changes. No ordinary Owner mutation, delegated identity management, grant issuance, server executor, remote protocol or client Host-store access.

Primary IDs LOCAL-001/002/003/004, OWNER-001/002, PERSIST-001, HOST-001/002, CLIENT-001/002/003, AUTH-002/003, SEC-001, IDENT-001, RECOVERY-001; audit all other registry families unchanged. Required evidence: bootstrap one-Owner/rollback/expiry/retry; two-principal and wrong-actor/native proof; ten-attempt/concurrent limits; duplicate/new and revoked-target races; full ABA invalidation across enrollment/re-home tickets; grant-descendant invalidation without restoring unrelated grants; durable audit and redaction; secure-store fail-closed reads; full build/suite/docs plus retained real Windows CI. Actual production multi-user ceremony/recovery remains 42d, never inferred from these fixtures. Two-pass review and exact HEAD/base merge gate remain required.

## #42c2 implementation and Review Pass A

Implemented the [transactional enrollment primitive](../developer/v0.5-local-enrollment.md) in eight changed files. Initial focused tests and initial full suite 190/190 PASS; no test-discovered production defect in the first candidate. Review correction cycle 1 moved the stable HMAC key-name contract into Core so future approved offline composition can reuse it without a forbidden Host.Cli-to-Host dependency, and moved creation-deadline evaluation inside the acquired writer transaction rather than before a potentially blocking acquisition. Review also strengthened evidence: forced enrollment-audit rollback (including sibling invalidation), recovery-target ABA invalidation, an Owner key change between authenticated snapshot and write, and encoded-secret absence. Those new focused tests PASS. No identity-model change or Product Decision.

Reviewed all eight files, existing schema/identity primitive, current issue, architecture 2c/3a/5/7/8 and the complete registry. All protected effects are covered: bootstrap preparation/completion; Owner enrollment authorization; first consumption and consumed retry; wrong attempts; reactivation/revocation; sibling/recovery-ticket invalidation; two grant forests; audit rollback; online secure-key retrieval; actor snapshot-to-transaction race; resources and failure paths.

| Invariant IDs | Evidence / applicability | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | Additive pure Core domain code; no frozen WPF/Lan changes or project-edge changes; ordinary clients cannot reach repositories or Host secrets | PASS |
| HOST-001/002, PERSIST-001 | Caller retains machine lease; one immediate transaction per operation; read-write/no-create/non-pooled owned connections; offline preparation not exposed by online adapter | PASS boundary |
| LOCAL-001/003, OWNER-001/002 | Exactly one initial Owner atomically; current Owner/key/native/Host checked inside authorizing write; wrong/unregistered actors gain nothing; no ordinary Owner mutation; offline actual privilege/handoff composition still 42d | PASS boundary |
| LOCAL-002/004, CLIENT-002/003 | Host receives only public principal keys; online methods require prior authenticated TLS/native composition; client generation/store retained; no raw principal private key enters persistence | PASS boundary |
| SEC-001, IDENT-001 | Version/purpose/Host/ticket-bound HMAC; independent stable secure-store key; online missing/corrupt key fails closed; redacted code/verifier diagnostics; nonsecret atomic audit; raw/encoded secret absence | PASS boundary |
| AUTH-001/002/003/004/005 | Separate grant tables unchanged; revocation walks exact single-parent descendant forests; unrelated trees unchanged; no grant issuance, restoration, defaults or delegated identity authority | PASS boundary |
| IDENT-002/003/004, REMOTE-001/002, PAIR-001-004, PROTO-001 | No cross-Host actor/routing/pairing/rotation or remote messages; HMAC key is not the TLS identity; reserved recovery purpose values do not implement recovery | PASS unchanged |
| OPS-001-004, RECOVERY-001 | No long-running operation executor; ticket consumption/result/audit transactional; failed final audit rolls back authority/cascades; consumed retries return prior fact without redoing effects | PASS boundary |
| MIG-001, PLATFORM-001/002, LINUX-001 | No schema migration, WPF migration, OS-specific shared logic, interactive service action or Linux production implementation | PASS unchanged |

Stale-reference search covers bootstrap first-connection claims, Owner mutability, group/delegated authority, raw secret/keys, machine-credential/HMAC coupling, indefinite consumed retries versus first-use expiry, grant restoration, callback-to-transaction races, project roles, final composition and field completion claims. Earlier #42c1 no-write descriptions remain scoped to that completed primitive. No contradictory new production claim found. Full corrected local and remote validation is required before declaring Pass A clean or opening a PR. Actual privileged offline/bootstrap/recovery and multi-user final ceremony remain mandatory 42d evidence, never counted as this fixture's PASS.

Corrected candidate 2de428b08b0a2b3408f8d0afeaf94a584616cac7: Release build zero warnings/errors, full local 190/190 tests and strict docs PASS; CI 34003756228 PASS in both ordinary and actual Windows service/multi-user jobs, Docs 34003757388 PASS. Full diff and each invariant row rechecked after the corrections, preserving explicit boundary/unchanged qualifiers. Review Pass A clean after one pre-PR correction cycle. No unresolved Product Decision or known escaped defect. Proceed to the experiment-only PR and fresh exact-HEAD Pass B.

## #42c2 Review Pass B

Fresh final review of PR #74 at b75ec3d7f2225909d90cd2d003bfee24c9b6fea4 against a39f5908507d38ff0f99e46122c0e734134a0d9d inspected all eight files again. Reconsidered actual public/internal call paths, current Owner proof versus transactional actor recheck, raw versus keyed secret consumers, HMAC purpose/Host/ticket separation, initialized/one-Owner and consumed-result ordering, atomic writer serialization, ten-attempt failure commits, malformed/stale native/key/target cases, proactive invalidation across sibling recovery mechanisms, recursive grant forest behavior, audit-trigger rollback, no-create/non-pooled cleanup, and production/field exclusions. No additional substantive defect or Product Decision found. No new executable change after tested code SHA 2de428b08b0a2b3408f8d0afeaf94a584616cac7.

Every invariant family in the Pass A matrix was re-evaluated and retains its stated PASS/boundary/unchanged classification. Stale-reference search and diff checks clean. Local build zero warnings/errors, 190/190 suite, strict docs and CI 34003756228/Docs 34003757388 PASS. Both reviews clean; one correction cycle total (A1/B0), no test-discovered production failure or known escaped defect. Existing privileged Windows platform evidence remains real; final principal/bootstrap/recovery ceremony remains unexecuted and required in 42d. Final ledger-only HEAD and experiment base still require exact merge checks.

## #42c2 integration checkpoint

#42c2 SHADOW DONE as the transactional local bootstrap/enrollment/revocation foundation. PR #74 final HEAD 26913895055217f0c62211ad444cceb1cabb561e merged as ea7472f7c99720c03f74649196b58b3ff9506f94 after exact HEAD/base checks. Code/test SHA 2de428b08b0a2b3408f8d0afeaf94a584616cac7; CI 34003756228 and final Docs 34003924401 PASS. One correction cycle (A1/B0), both reviews clean, no known escaped defect. Fourteen experiment PRs #61-#74 merged; eight canonical child issues complete plus five #42 foundations and the separate #34 correction. Total correction cycles 39. Full suite 190 ordinary groups plus retained actual Windows integration. Canonical #42 remains incomplete; downstream local-auth dependencies, #55 field/parity and Linux/release gates remain blocked or untouched as before.

Refine the remaining 42d scope into bounded review units before implementation: **42d1 Owner recovery transactions** (rotation/re-home preparation snapshots and online consumption, proactive invalidation, one Owner, atomic audit/cascade and indefinite consumed-result retries); **42d2 privileged offline preparation and protected handoff** (real elevation/exclusivity, machine identity and secure-key setup, authoritative credential/cache/publication reconciliation and intended-user handoff); **42d3 production local channel integration and real multi-user qualification** (executable service/client/Host.Cli composition, bootstrap/enrollment/recovery, client key retry/binding, unauthorized denial and lifecycle tests). Machine credential loss/compromise remains the accepted offline-only path and must be reconciled within these boundaries, never authorized by its old key. No unit can independently claim full #42 completion.

## #42d1 preflight: bounded Owner recovery transactions

Branch astra/42-owner-recovery starts at 2c94d7ebaa5fd81c459735b7a028b5cae41c73cf. Current #42 body/Project refreshed read-only. Architecture 2b/2c/3a/7/8 and current schema define rotation and re-home precisely; dependencies and prior primitives are accepted in this lane. Expected changes: a partial recovery implementation in Host.Persistence reusing existing immediate transaction/audit/invalidation helpers; online Host completion methods using the independent HMAC key and purpose; isolated negative/concurrency tests; documentation/ledger. No project or schema changes.

Protected effects: offline preparation snapshots for exact Owner identity/key and exact re-home target state; first-use secret/native/expiry verification; existing-Owner public-key replacement; old-Owner full tombstone/grant cascade and new-Owner creation or adoption; proactive invalidation on both affected rows; same-transaction audit; consumed-result retries. Rotation keeps the existing Owner ID/state. Re-home must target a different native principal and retain exactly one active Owner at either committed side; an already-active target submits its existing public key, a new/revoked target supplies its fresh client key. No Host-side client private key, ordinary Owner setter, delegated identity authority, remote recovery operation or old-machine-credential proof path.

Primary IDs OWNER-001/002, LOCAL-001/002/003/004, CLIENT-001/002/003, PERSIST-001, HOST-001/002, AUTH-002/003, SEC-001, IDENT-001/004, RECOVERY-001. Audit all registry families. Tests must cover wrong/stale/expired native/key/secret; same-ID rotation; single-user/new-account, existing-active and revoked-target re-home; one Owner and old-key denial; lost-response indefinite retry with no repeat mutation; rotation/re-home competing snapshots and full ABA; dependent enrollment/recovery invalidation; atomic audit rollback and grant cascade. Actual Administrator/lease enforcement, protected delivery and final two-user recovery ceremony remain 42d2/d3; these persistence preparation methods are never exposed by the online adapter. Full build/suite/docs, retained privileged CI and two-pass exact-HEAD review required.

## #42d1 discovery: escaped #42c2 grant-helper defect

While composing Owner re-home, reviewing architecture section 5's exact forest rule exposed a latent defect in PR #74's shared InvalidateGrants helper: its seed included GrantedByLocalPrincipalId as well as GranteeLocalPrincipalId. Ordinary non-Owner delegated grants normally descend from that actor's held grant, so the existing non-Owner tests did not distinguish the redundant issuer seed. For the displaced Owner, however, it would invalidate every independently issued root grant to other actors, violating the explicit per-subtree rule (not every grant an actor ever issued). This escaped both #42c2 reviews and was discovered before #42d1 integration. Fix the helper to seed only grants HELD by the revoked principal, then traverse DerivedFromGrantId descendants; add recovery regression evidence for independent Owner-issued roots and descendant cascades in both grant tables. Update the implementation document's corresponding claim. This is #42d1 correction cycle 1, and an escaped prior-unit defect, not a product-decision ambiguity. Historical #42c2 clean/no-known-defect statements remain snapshots, superseded by this finding; the audit must be repeated after validation.

## #42d1 implementation and Review Pass A audit

Implemented rotation/re-home preparation snapshots and online completion using the existing immediate transaction, keyed verifier, audit and invalidation boundaries. Initial focused recovery tests PASS. The new Owner grant-forest regression was then deliberately run once with PR #74's old issuer-based seed restored: it FAILED with `Owner re-home cleared independent issued roots or missed held-grant descendants.` Restoring the correction makes the complete focused probe PASS. This confirms the escaped defect rather than merely theorizing about it; the old implementation is not retained. Added concurrent different re-home attempts to prove only one can consume, in addition to repeated same-ticket rotation consumption. One correction cycle so far (A1), covering the escaped helper correction and its stronger recovery evidence.

Reviewed all changed source/tests/docs and existing schema, authorization and lifecycle interactions against architecture 2b/2c/3a/5/7/8 and every registry family. Protected effects include prepared snapshots; current Owner and target checks; first-use versus consumed-result ordering; old-Owner tombstone; correct grant-forest seeds/descendants; target creation/reactivation/promotion; both-row dependent-ticket invalidation; one active Owner; audit/rollback and private-key consumers.

| Invariant IDs | Evidence / applicability | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | Partial persistence implementation and online Host adapter only; no project edges, schema, frozen WPF/Lan or client Host dependency | PASS |
| HOST-001/002, PERSIST-001 | Same caller-held lease and owned immediate writer transaction; preparations absent from online adapter; actual offline privilege gate remains 42d2 | PASS boundary |
| OWNER-001/002, LOCAL-001/003 | Only prepared exact-target secret authorizes exceptional recovery; ordinary Owner setter absent; all target states, current-owner snapshots, sibling/ABA invalidation and exactly-one committed Owner covered | PASS boundary |
| LOCAL-002/004, CLIENT-002/003 | Only submitted public keys; intended native identity checked; old client private key not needed or imported; server-authenticated production composition and protected handoff remain 42d2/d3 | PASS boundary |
| AUTH-001/002/003/004/005 | Corrected held-grant seed plus exact descendants in both tables; independent Owner-issued roots preserved; no automatic grant restoration/issuance or scope/default changes | PASS after escaped-defect correction |
| SEC-001, IDENT-001/004 | Independent secure-store HMAC key and distinct purposes; redacted private ticket/verifier fields; preparation/completion audit without secrets; no use of old machine credential to authorize its own replacement | PASS boundary |
| IDENT-002/003, REMOTE-001/002, PAIR-001-004, PROTO-001 | No cross-Host identity/routing, pairing, staged TLS rotation, remote recovery message or negotiation change | PASS unchanged |
| OPS-001-004, RECOVERY-001 | Bounded transactions, no operational executor; forced final audit rollback restores Owner/key/ticket/grants; correct consumed retries do not repeat any effect even after later recovery | PASS boundary |
| MIG-001, PLATFORM-001/002, LINUX-001 | No migration, shared OS-specific behavior, interactive service action or Linux production work | PASS unchanged |

Stale-reference search updates the enrollment document's issuer-based cascade claim and reserved-recovery wording. Historical clean audits remain chronological snapshots and are explicitly superseded by the escaped finding. No unresolved Product Decision. Full corrected local/remote validation is pending before declaring Review Pass A clean; actual OS privilege/exclusivity/handoff and multi-user ceremony remain unexecuted 42d2/d3 gates.

Corrected candidate 181a5b15215ddcba5e4cadb80b30c437801d9aeb: local Release build zero warnings/errors, full suite 194/194 and strict docs PASS; CI 34004404180 PASS in both jobs, Docs 34004405388 PASS. Every audit family and complete diff rechecked after the correction. Review Pass A clean after one correction cycle, including the escaped #42c2 grant-helper fix. No unresolved Product Decision or known remaining escaped defect; two escaped defects are now known historically (#34 fixture, #42c2 helper), both corrected. Proceed to the experiment-only PR and fresh Pass B.

## #42d1 Review Pass B

Fresh complete-diff review of PR #75 at a5671ab886977597d7a1dfb56ddcf265793dece5 against unchanged base 2c94d7ebaa5fd81c459735b7a028b5cae41c73cf re-read all nine files and the exact section 5 grant-forest rule. Re-evaluated both preparations, all completion/consumed-retry branches, active/revoked/absent target handling, changed-Owner and target snapshots, current/public key validation, immediate writer serialization and competing re-homes, proactive ABA invalidation, complete old-row tombstone, held-grant-only cascade, independent historical issuer roots, audit rollback and caller-owned lease/private-key boundaries. No further substantive finding. No test or executable change after code SHA 181a5b15215ddcba5e4cadb80b30c437801d9aeb.

Every registry family retains its Pass A classification; the escaped #42c2 defect remains explicitly acknowledged and is fixed by this exact candidate, with a demonstrated failing old-helper regression. Stale/contradictory-reference search and diff checks clean. Local zero-warning Release build, 194/194 tests, strict docs, CI 34004404180 and Docs 34004405388 PASS. Both reviews clean, one total correction cycle (A1/B0). No unresolved Product Decision or known remaining escaped defect. Real offline/handoff/production recovery acceptance remains 42d2/d3. Final ledger-only HEAD and unchanged experiment base must still pass exact merge checks.

## #42d1 integration checkpoint

#42d1 SHADOW DONE as the Owner recovery transaction foundation. PR #75 final HEAD cbe2ac99ea542b1ef67c466ed84f474021f638c1 merged as daba56d92de286936e67686ddfab3159021c1743 after exact HEAD/base checks. Code/test SHA 181a5b15215ddcba5e4cadb80b30c437801d9aeb; CI 34004404180 and final Docs 34004608330 PASS. One correction cycle (A1/B0), both reviews clean. The escaped #42c2 issuer-based grant-cascade defect is now fixed in integration with a failing-old/passing-new regression. Two known historical escaped defects (#34 fixture and #42c2 helper), neither remaining open. Fifteen experiment PRs #61-#75 merged; eight canonical child issues complete plus six #42 foundations and the separate #34 correction. Total correction cycles 40; 194 ordinary tests plus retained actual Windows integration. Full #42 and its dependent gates remain incomplete.

Bound 42d2 further to preserve reviewability: **42d2a authoritative machine-credential publication/reconciliation** (public metadata, tracked creation/retirement, independent enrollment-key lifecycle, offline loss/compromise state transitions and current/pending projection); **42d2b Windows protected intended-user handoff** (actual privilege, restrictive recipient ACL, bounded artifact, retry-safe ownership/cleanup and real two-user tests); **42d2c offline Host.Cli coordinator** (actual elevation/exclusivity, unconditional reconciliation, bootstrap/Owner recovery/machine recovery, composition of already-tested seams). These together fulfill the earlier 42d2 boundary; 42d3 retains final service/ordinary-client composition and multi-user ceremony. No child independently claims full #42 completion.

## #42d2a preflight: authoritative machine-trust reconciliation

Branch astra/42-machine-trust-reconciliation starts at e2f0796695759b0541b9135dfe0d392a6e2b3ea2. Canonical #42 refreshed read-only; architecture 3b/4a-i/7 and native-cache acceptance reread. Expected layers: pure Core publication/retirement plan and reconciliation seams; Host.Persistence credential metadata/state writer; an additive migration for public SPKI fingerprint metadata on SecureCredentialReferences; Windows machine-credential generation/validation and independent enrollment-key provisioning through existing secure-store/cache interfaces; isolated tests and docs. Preserve all project edges. No client private key, remote pairing engine, normal rotation initiation/cutover policy, service/client/Host.Cli executable wiring or intended-user handoff in this unit.

Record opaque planned credential references before material creation, then public metadata after verified generation, to make abandoned creation discoverable without enumerating arbitrary store keys. SQLite CurrentCredentialRef remains the sole selection authority; its joined public metadata permits reconciliation and offline replacement without reading the outgoing private credential. Existing records without verified public metadata fail closed rather than trusting a file or inventing a fingerprint. Store publication is derived; no cross-store atomicity claim. Only the independently provisioned HMAC key serves consumed enrollment/recovery retries and is never retired with TLS identity credentials.

Protected effects: first credential installation on an uninitialized Host; loss/compromise replacement authorized only by later actual offline caller; same-transaction current reference, reason-specific audit and Active peer recovery markers; aborting every nonterminal rotation including CutOver; current/pending publication from authoritative metadata; tracked secure-store/native-cache retirement before connections; missing/corrupt secret and unsafe native state fail closed. Prepared/staging/ready rotations retain their current and new material; CutOver retains Old until peer completion while publishing only New; Completed/Aborted clear Pending and retire unused material. No routine rotation eligibility or peer approval is fabricated here. Caller owns the machine lease and quiesces connections/mutations during reconciliation; the later CLI must retain it and retry after a committed recovery until publication and cleanup complete.

Primary IDs IDENT-001/003/004, LOCAL-004, SEC-001, HOST-001/002, PERSIST-001, CLIENT-001/002/003, OWNER-001/002, RECOVERY-001 and PLATFORM-001; audit every other family. Required tests: real v1-to-v2 metadata migration preserves state, initial/current selection, every rotation-state projection, stale Pending cleanup, recovery abort/peer markers and distinct audits, atomic rollback, planned-material crash cleanup, publication-before-retirement and retry failure, no old-private read on replacement, HMAC persistence/fail-closed behavior and unchanged Owner/local-principal/grant state. Full build/suite/docs and retained actual Windows CI, then two-pass review. Actual offline privilege/lease/handoff and final service/multi-user acceptance remain 42d2b/c and 42d3.

## #42d2a implementation and Review Pass A audit

Implemented a pure Core projection/reconciliation plan, additive public-only migration 2, Host.Persistence initial/recovery/retirement transactions, and a Windows P-256 material plus independent enrollment-HMAC adapter. The existing service harness now uses that material adapter. No project edges changed. Each of the ten changed files was inspected, including all transaction predicates, failure paths, migration ordering, test fixtures and documentation.

Two pre-PR correction cycles strengthen freshness: A1 records activation history so an old credential cannot be reused merely because cleanup has not yet retired it; A2 rejects the same previously activated/staged public key under a fresh reference. These were review-discovered gaps, with negative regression assertions added before merge. No escaped defect or unresolved Product Decision is identified in this unit. Initial focused probes passed; full corrected local and remote validation remains required before declaring Pass A clean.

| Invariant IDs | Evidence / applicability | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | Existing project graph retained; no WPF/Lan/client dependency changes; Core owns pure domain data only | PASS |
| HOST-001/002, PERSIST-001 | Existing-file, non-pooled owned SQLite connections and immediate writer transactions; caller-held machine lease explicit; no online recovery entry point; actual executable enforcement remains 42d2c | PASS boundary |
| IDENT-001/003/004, LOCAL-004 | Stable HostId; SQLite-only current selection; every rotation state including CutOver; fresh replacement and both audited reasons; no old private read; publication precedes retirement; routine cutover/peer eligibility remains deferred | PASS boundary |
| CLIENT-002/003, SEC-001 | Private PFX confined to platform/secure store and cleared; public-only migration/projection/audit; independent HMAC retained for all ticket histories; actual service material test included in CI | PASS boundary |
| OWNER-001/002, LOCAL-001/002/003 | Exact initialized/Owner cardinality; initial credential is not Owner creation; machine recovery leaves Owner, client keys, grants and enrollment untouched | PASS |
| IDENT-002, REMOTE-001/002, PAIR-001-004 | Active peers marked recovery-required atomically, never cleared here; no remote protocol, pairing, replacement approval or route added | PASS boundary |
| AUTH-001-005 | No grant issuance, cascade, default, capability or scope changes; machine recovery preserves existing authorization rows | PASS unchanged |
| PROTO-001 | Unknown recovery reasons and rotation states fail closed; no protocol negotiation or enum compatibility changes | PASS |
| OPS-001-004, RECOVERY-001 | No operational executor; atomic audit rollback; tracked creation, retryable publication/deletion and idempotent retirement; no cross-store atomicity claim | PASS boundary |
| MIG-001, PLATFORM-001/002, LINUX-001 | Additive Host schema upgrade only; no v0.4 writes; OS material stays in Windows platform; no interactive service or Linux implementation | PASS |

Acceptance mapping: four focused groups cover v1 upgrade/current activation preservation with absent-fingerprint rejection; all six rotation projections and multiple-active rejection; each nonterminal rotation crossed with both recovery reasons and forced audit rollback; current/old-key reuse rejection; publication and delete failures plus restart/idempotent orphan cleanup; cancellation before effects; real P-256 generation/validation; stable/corrupt/missing HMAC cases; and an outgoing-key read trap. Retained Windows CI must validate actual service creation, native cache and two-user TLS. Final actual offline privilege/lease enforcement, intended-user handoff and principal/recovery ceremonies remain 42d2b/c and 42d3, not marked PASS here.

Stale-reference search covered CurrentCredentialRef, public descriptor/Pending state, Prepared/CutOver retirement, secure-store/HMAC consumers, production coordinator claims and the complete changed diff. Existing trust-channel documentation correctly reserves executable startup wiring; the new implementation document spells out the caller guarantees. No contradictory current claim found. Historical unit results remain chronological snapshots.

Corrected code SHA 58c0479fd5ddace500c691b5d454d1ca4d62e92e: local zero-warning Release build, 198/198 suite and strict docs PASS; CI 34005476892 PASS in both jobs, including real Windows service material generation and multi-user TLS; Docs 34005477754 PASS. Complete changed-file and invariant audit rechecked after both freshness corrections; Review Pass A clean (A2). No unresolved Product Decision or known remaining escaped defect. Proceed to experiment-only PR and fresh Pass B.

## #42d2a Review Pass B

Fresh review of PR #76 at b5ef3651fb73f1156a338f61247ff77bc1c5cde9 against unchanged integration e2f0796695759b0541b9135dfe0d392a6e2b3ea2 inspected all ten files again. Re-evaluated the metadata migration and legacy fail-closed path, every rotation/current branch, public-versus-private consumers, recovery freshness by reference and fingerprint, transaction serialization and forced audit rollback, active-peer marking, Owner/grant preservation, service certificate/HMAC creation, publication-before-cleanup, partial cleanup retries, retained CutOver Old, and production lifetime/field exclusions. No additional substantive finding. No executable change after tested code SHA 58c0479fd5ddace500c691b5d454d1ca4d62e92e.

Every registry family was re-evaluated and retains the Pass A PASS/boundary/unchanged classification. Complete diff and stale-reference checks clean. Local zero-warning build, 198/198 tests, strict docs, CI 34005476892 and Docs 34005477754 PASS. Both reviews clean; two correction cycles (A2/B0), no test-discovered failure or known remaining escaped defect in this unit. Actual offline/coordinator/handoff and final principal ceremonies remain mandatory following units. Final ledger-only HEAD, final Docs and exact unchanged base/HEAD checks still gate merge.

## #42d2a integration checkpoint

#42d2a SHADOW DONE as the machine-trust reconciliation foundation. PR #76 final HEAD 5beb9fdf159f447571b4e7666eccf76bc236ec9a merged as c919e99862e151403d1a64f339fd5e3e9fafe66a after exact HEAD/base checks. Code/test SHA 58c0479fd5ddace500c691b5d454d1ca4d62e92e; CI 34005476892 and final Docs 34005652136 PASS. Two correction cycles (A2/B0), both reviews clean; no known remaining escaped defect. Sixteen PRs #61-#76 merged, eight canonical children plus seven #42 foundations and the separate #34 correction. Total correction cycles 42; 198 ordinary tests plus actual Windows integration. Two historical escaped defects remain recorded and corrected. Canonical #42 remains incomplete; downstream local-auth dependencies and #55/Linux/release gates remain blocked or untouched. Continue bounded 42d2b protected handoff, then 42d2c offline coordinator and 42d3 final executable/multi-user integration.

## #42d2b preflight: protected intended-user handoff

Branch astra/42-protected-owner-handoff starts at 84c3cfd72d677cb9d1a4bedb019d61abcb25a63b. Current #42 and Project refreshed read-only. Architecture 2b/2c/7 and client platform graph require the same OS-protected handoff for bootstrap, rotation and re-home. Bounded layers: Host-side Windows privileged writer, independent client-side current-user reader and bounded artifact parser, negative tests and real two-user Windows integration, documentation. No Host/Client dependency edge, SQLite writer, RPC, Owner mutation or executable coordinator is added.

Protected effects: elevated creation of a fresh ticket-named artifact bound to an exact user SID; recipient-only OS ReadData access; immutable contents to the recipient; current-user-only read/delete with exact ACL/owner/reparse validation; durable complete write before exposure; failure cleanup restricted to the newly created artifact. Administrator/SYSTEM retain write/ACL/delete maintenance rights but are not granted ReadData; the trusted privileged creator already holds the secret in memory. Shared product-group or other users gain no read access. Owner/bootstrap/recovery secrets remain outside Host persistence and ISecureCredentialStore. Readers never accept a caller-supplied recipient SID. Ticket identity and purpose are bounded public metadata; raw secret is disposable/redacted and not logged.

Primary OWNER-001/002, LOCAL-001/002/003/004, CLIENT-001/002/003, SEC-001, PERSIST-001, HOST-001/002, PLATFORM-001/002, RECOVERY-001; audit every registry family. Required evidence: malformed/oversized/wrong Host/ticket/purpose artifacts fail closed; actual non-admin recipient read/delete succeeds, other-user and product-group-only direct reads fail; recipient cannot overwrite or replace files; unsafe ownership/inherited/broad ACL/reparse paths rejected; elevated writer cannot silently overwrite a ticket; no partial plaintext on failure; no raw-secret serialization/logging; no unintended cleanup. Full local build/tests/docs and real Windows CI, then both reviews. Actual machine-lease/offline and successful-consumption ordering are later coordinator/client guarantees (42d2c/d3); this unit cannot independently claim full #42.

42d2b initial implementation: fixed-size redacted client artifact parser, independent exact ACL/ancestor validation in both Windows platform assemblies, elevated exact-user writer, current-user-only reader, and three-purpose real two-user integration coverage. First build found an incorrect enum member name and nullable-owner warnings; corrected in pre-PR cycle 1. Corrected Release build has zero warnings/errors and strict docs PASS. Full local tests, actual Windows CI and substantive Pass A remain pending; no PR may open yet.

## #42d2b Review Pass A audit in progress

Cycle A2 strengthens real Windows negative evidence for wrong file owner, client reparse rejection, group SID rejection and pre-creation cancellation, removes unused ACL-creation code from the ordinary client assembly, and updates sibling enrollment/recovery references. Initial local 199/199 tests and Docs 34006034150 passed at 610d8c9d504dc763b602b4bab9c9c128135d69e8. First Windows CI 34006033376 remains pending. Corrected candidate requires rerun evidence; this is not yet a clean-review declaration.

Reviewed every protected-effect path: actual elevation before creation/cleanup; LookupAccountSid user-kind resolution; protected existing/new root and SID directory; no ordinary directory-entry writer; exact protected file owner/allow rights; CreateNew plus exclusive handle, flush and owned-failure cleanup; bounded public identity/purpose plus secret payload; current-token-only reader; opened-object ACL validation; explicit buffer disposal; deletion following caller-confirmed persisted completion; no new ticket/Owner/Host authority or online writer. The writer never reads an old handoff or overwrites one; no privilege bypass from group eligibility.

| Invariant IDs | Evidence / applicability | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | No project edges or WPF/Lan changes; separate Host/client Windows ACL implementations, no shared Host dependency | PASS |
| HOST-001/002, PERSIST-001 | No database/operation writer; trusted offline caller must hold machine lease and commit keyed verifier separately; actual end-to-end coordinator remains 42d2c | PASS boundary |
| OWNER-001/002, LOCAL-001/003 | Exact user-bound elevated handoff only; artifact itself grants no authority; all Owner mutations remain existing ticket transactions | PASS boundary |
| LOCAL-002/004, CLIENT-002/003 | Current-user reader, no machine or client private key access; raw handoff must cross only authenticated channel later; no ordinary CLI privilege substitution | PASS boundary |
| SEC-001 | Explicit recipient ReadData ACL, other-user/group denial required in actual CI; private secret fields, redacted string/JSON, explicit cleared exports; unsafe owner/ACL/reparse/malformed rejection | PASS pending actual CI |
| IDENT-001-004, REMOTE-001/002, PAIR-001-004 | Exact Host/ticket framing; no TLS trust/rotation, remote pairing, replacement or routing change | PASS unchanged |
| AUTH-001-005, PROTO-001 | No grant/default/scope changes; unknown artifact version/purpose fails closed; purpose 2 explicitly unavailable for privileged Owner handoff | PASS |
| OPS-001-004, RECOVERY-001 | No operational executor; exclusive non-overwriting writes, flush before close, only owned failure cleanup; malformed crash remnants rejected, deletion idempotent | PASS boundary |
| MIG-001, PLATFORM-001/002, LINUX-001 | No schema/v0.4 changes; all OS identity/ACL code confined to Windows seams; no interactive service or Linux production behavior | PASS |

Acceptance mapping: ordinary format test checks all purposes, wrong Host/ticket, unknown enums/version/lengths, redaction and disposal. Real Windows integration checks all three purposes with intended recipient versus a second product-group member; privileged ReadData is absent unless that privileged user is itself the intended recipient; recipient cannot write or create replacement entries; non-elevated writer and group target denied; no overwrite, unsafe ACL/owner and partial artifacts denied; reader/writer reject file reparse and cleanup leaves target intact. Actual CI must pass every check with no skip. No claim of complete offline bootstrap/recovery or full #42. Stale-reference search covers all handoff mentions in source and sibling implementation documents; previous contract-only/later-handoff wording is updated. No unresolved Product Decision identified.

CI 34006033376: ordinary job PASS, Windows integration FAIL at intended-user handoff-read after deliberate broad-ACL mutation. All earlier service/native/TLS/DPAPI checks and cleanup passed. Investigation identifies the fixture's restoration via SetAccessControl on a clean previously-read FileSecurity: .NET persists modified sections, so that call need not remove the injected Everyone rule. Cycle A3 reconstructs and marks the saved descriptor before restoring, asserts exact owner/DACL read-back, and adds intended-user peek before any mutation. This is pre-merge test-discovered evidence, not a waived OS boundary and not an escaped defect. Corrected actual CI is required to confirm diagnosis.

CI 34006184134: ordinary job and Docs 34006185015 PASS; Windows integration FAIL in fixture Restore because it requested Owner as well as DACL writes, while the intentional maintenance ACL lacks WRITE_OWNER. The new pre-mutation intended-user read passed. Local reproduction confirms clean-snapshot restore leaves the injected Everyone ACE, while explicitly marked DACL restoration removes it; Windows also adds an informational AutoInherited flag on persistence, so raw SDDL text is not an exact permissions comparison. Cycle A4 restores only Access, compares protected owner plus exact binary ACE list, and creates a separate wrong-owner fixture at creation time rather than requesting an unauthorized owner rewrite. No production ACL right was broadened. Corrected remote validation still required.

Corrected candidate 1084266bee370983c72351c62dc0dc11eeab41b5: local Release build zero warnings/errors, 199/199 ordinary tests (unchanged ordinary production/test path after the fixture-only corrections), strict docs, CI 34006344991 in both jobs and Docs 34006345917 PASS. The real Windows run now confirms both earlier fixture diagnoses and all added negative cases; cleanup passes. Rechecked all fourteen changed files, every audit family and stale references after A4; all pending actual-CI qualifiers are now PASS for this bounded artifact scope. Review Pass A clean after four pre-PR cycles (A1 compiler cleanup, A2 review evidence, A3/A4 fixture corrections). Two CI-discovered fixture failures are honestly retained; no known escaped defect or unresolved Product Decision. Full #42 and remaining executable ceremonies are incomplete.

## #42d2b Review Pass B

Fresh final review of PR #77 at 1177eed46ff78e5ade4b3c6ab53f2a37de1996f2 against unchanged base 84c3cfd72d677cb9d1a4bedb019d61abcb25a63b inspected all fourteen changed files. Re-evaluated every writer/reader/cleanup entry point, actual enabled-Administrator and exact-user SID checks, root/recipient/file ACL distinctions, privileged maintenance without ReadData/WRITE_OWNER, protected ownership and unusual ACE rejection, path and opened-file validation, recipient deletion versus directory replacement rights, fixed format and secret lifetime, non-overwrite/crash/partial-write behavior, fixture privilege and ACL restoration evidence, and sibling documentation/production boundaries. No additional substantive finding. No executable change after tested candidate 1084266bee370983c72351c62dc0dc11eeab41b5.

The complete invariant registry was re-evaluated: all Pass A classifications remain PASS or explicit boundary/unchanged; the actual handoff security evidence now passes CI. Stale-reference and diff checks are clean. Local zero-warning build, 199/199 ordinary suite, strict docs, CI 34006344991 and Docs 34006345917 PASS. Both reviews clean after four correction cycles (A4/B0); compiler/evidence/fixture findings and the two initial CI failures remain recorded, no known escaped defect. No Product Decision is unresolved. Actual offline lifetime and final Owner/client ceremonies remain 42d2c/d3. Final ledger-only HEAD, Docs and exact unchanged base/HEAD checks remain the merge gate.

## #42d2b integration checkpoint

#42d2b SHADOW DONE as the protected intended-user artifact foundation. PR #77 final HEAD 9ca1d8c535f2be9963cbd6d8273e8282dc7812e4 merged as 21fa9f16d54a50a6b9b3e697b30850549afccac7 after exact base/HEAD checks. Code/test SHA 1084266bee370983c72351c62dc0dc11eeab41b5; CI 34006344991 and final Docs 34006537862 PASS. Four correction cycles (A4/B0), both reviews clean; two initial CI fixture failures corrected without broadening production access. Seventeen PRs #61-#77 merged; eight canonical children plus eight #42 foundations and the separate #34 correction. Total correction cycles 46; 199 ordinary tests plus actual Windows integration. Two historical escaped defects remain corrected; none known open. Canonical #42 and its downstream gates remain incomplete. Continue 42d2c offline executable coordinator, then 42d3 final production service/client and multi-user ceremony.

## #42d2c preflight: offline executable coordinator

Branch astra/42-offline-coordinator starts at 70d4eb3fbf3d177522653d8e04bced4096bae9aa. Current #42 and Project refreshed read-only. Compose accepted machine-trust, independent HMAC, Owner ticket and handoff seams into the existing Host.Cli executable. Production project edges remain exact: Host.Cli -> Core, Host.Persistence, Platform.Windows, no Contracts/Host/client dependency. An optional SelfTest -> Host.Cli reference is test-only and does not alter any guarded product graph. Expected additions: bounded CLI parsing/composition, Windows stopped-service/Host-root validation, persistence read-only current-Owner identity needed for rotation delivery, explicit metadata-unavailable recovery classification, isolated retry/parser tests and actual elevated CLI process integration, docs/ledger.

Protected paths: enabled Administrator token before state access; require stopped service and immediate machine lease, then recheck state and validate every existing Host-state object's privileged ownership/ACL before opening SQLite; migration/identity setup; unconditional public/native/secret reconciliation at every invocation before new work; tracked first credential creation; independent HMAC provisioning only without ticket history; bootstrap and rotation/re-home ticket preparation with intended-user handoff outside SQLite, verifier-only commit, exact artifact cleanup on failure; fresh machine loss/compromise recovery without any old-private read and atomic recovery metadata/audit. After a committed machine recovery the process retains its lease and retries publication/cleanup until successful, including cancellation requests; hard termination remains the documented residual window. No ordinary RPC, online authority bypass, remote pairing, client private key generation, operational executor or Linux behavior.

A missing legacy public fingerprint must remain fail-closed for ordinary operations but cannot make explicit lost-machine-credential recovery circular. Distinguish that exact condition from malformed identity/reference/rotation state, attempt the unconditional check, then let explicitly selected fresh recovery establish the new metadata without reading or inventing Old's fingerprint. The later production service performs the corresponding startup check in 42d3. Full #42 remains incomplete until final actual principal/Owner ceremony and unauthorized denial evidence passes.

Audit all registry families, especially HOST-001/002, PERSIST-001, OWNER-001/002, LOCAL-001-004, IDENT-001/003/004, CLIENT-001-003, SEC-001, PLATFORM-001/002 and RECOVERY-001. Tests must cover non-admin refusal, running-service/held-lease refusal before writes, first-bootstrap preparation with zero Owner until online completion, live bootstrap non-retargeting, exact SID Owner rotation/re-home preparation, invalid command/reason denial, failure cleanup, both machine-recovery reasons including physically missing Old, public/native retirement and HMAC preservation, startup repair after interrupted metadata/publication, and lease retention across publication failure/retry. Actual preparation tests may seed an Owner transactionally as an explicitly synthetic fixture; they do not substitute for 42d3's final online ceremony. Full local build/suite/docs, actual Windows CI and two-pass review required.

42d2c initial implementation composes the production offline command handler, strict Windows data-root validation, public-only Owner lookup and precise missing-public-metadata classification. Initial review closed a broken-diagnostic-output escape from the post-commit barrier, ensured all inherited Host-file access is validated, moved exact recipient validation before writes and ensured secret buffers cover retrieval failures. Test compilation found one missing X509 certificate-extension import; corrected. Record these initial corrections as A1 pending substantive complete Pass A. Corrected Release build zero warnings/errors; ordinary suite and actual Windows validation pending. No PR yet. Actual integration calls the compiled production handler under real Windows tokens/SCM/lease; only fixture location injection is used, and a synthetic Owner seed is explicit. This does not claim final executable online ceremonies.

Initial candidate 4b8357c951861000497c9bbfebb517edc84189d2: zero-warning Release build, 201/201 ordinary tests and strict docs PASS; CI 34007265589 PASS in both ordinary and actual Windows jobs. Actual handler integration confirms elevation/running-service/lease refusals, protected random-secret handoff, zero-Owner offline bootstrap, no live retargeting, audit rollback cleanup, Owner-bound preparation, physically missing Old, loss/compromise and missing-public-metadata recovery, native retirement/HMAC preservation and unrelated-command startup repair. Review cycle A2 adds explicit permissive-database-ACL refusal with unchanged-current assertion and updates stale sibling coordinator references. No production defect was found by the initial CI. Corrected build/docs/Windows CI and full Pass A audit remain pending.

## #42d2c Review Pass A invariant audit

Inspected all twenty changed files and the actual lower-level secure-store/native-cache/lease call paths. Native reconciliation reads security metadata and retires unused handles without signing/exporting private material or retrieving Old's secure-store blob. The explicit recovery path bypasses private validation of Old; ordinary preparation validates the selected private credential against authoritative metadata. The exact missing-public-metadata exception does not collapse invalid references/retired authoritative credentials or malformed Host/rotation state into a recovery bypass. The independent HMAC lifecycle remains separate from TLS rotation/recovery.

| Invariant IDs | Evidence / applicability | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | Guarded product graph unchanged; only test harness adds Host.Cli reference; no frozen WPF/Lan mutation or ordinary-client Host dependency | PASS |
| HOST-001/002, PERSIST-001 | Enabled Administrator gate, stopped SCM service, immediate machine lease and state recheck; strict root/ancestor/every-file ACL before opening DB; no alternate production CLI location or online mutation path | PASS, added unsafe-ACL CI pending |
| OWNER-001/002, LOCAL-001/003 | Bootstrap prepares a verifier ticket with zero Owner; exact user SID before setup writes; live initial ticket cannot retarget; rotation uses persisted Owner SID; re-home only prepares existing snapshot-bound transaction | PASS boundary |
| LOCAL-002/004, CLIENT-002/003 | No client private key enters CLI/Host store; protected handoff and public metadata only; actual online Host authentication, client binding and final consumption remain 42d3 | PASS boundary |
| IDENT-001/003/004, SEC-001 | SQLite current selection and all rotation-state reconciliation retained; fresh loss/compromise path never reads/trusts Old private bytes; current/Pending republish precedes retirement; no HMAC replacement; cleared secret buffers and sanitized diagnostics | PASS boundary |
| IDENT-002, REMOTE-001/002, PAIR-001-004 | No remote routing/pairing/approval mechanism; recovery-required markers originate in accepted atomic repository transaction and are never cleared here | PASS unchanged |
| AUTH-001-005, PROTO-001 | No grant/default/capability scope mutations; bounded unknown-command/reason denial; no normal protocol or RPC added | PASS |
| OPS-001-004, RECOVERY-001 | No operational executor; tracked creation/orphan cleanup, ticket-audit rollback with exact handoff deletion; committed publication barrier retains real lease through IO/cleanup/cancellation and broken diagnostics | PASS boundary |
| MIG-001, PLATFORM-001/002, LINUX-001 | Existing additive migrations only; no v0.4 writes; one Windows executable composition root and platform seams; no service UI or Linux implementation | PASS |

Evidence mapping: two ordinary groups cover CLI shape and retained real lease through four barrier attempts; previous 199 groups remain passing. Actual Windows production-handler integration covers non-admin separate process, stopped/running SCM and held mutex, no pre-gate DB creation, group/unknown-input denial, zero-Owner bootstrap, protected random-secret handoff, no retargeting, synthetic Owner preparation with exact SID, forced audit rollback cleanup, actual lost/compromised/legacy recovery, old native/DPAPI retirement, unchanged HMAC/Owner and startup publication repair on an unrelated command. The standalone Host.Cli executable was additionally run with this workspace's actual non-admin token and returned UnauthorizedAccessException refusal (exit 1) before state access. Added A2 unsafe-database-ACL/current-unchanged assertion awaits corrected actual CI.

Stale-reference search updated all sibling implementation docs that reserved offline orchestration; it retains explicit 42d3 production-service/client/final-ceremony exclusions. No fabricated full #42 or remote/field completion. No unresolved Product Decision or known escaped defect. Review Pass A will be declared clean only after the corrected Windows run completes.

Corrected candidate da2163b8c892df5ec397983000274c02754b366f: local zero-warning Release build, 201/201 ordinary tests, standalone actual non-admin executable refusal and strict docs PASS; CI 34007391775 PASS in both jobs including the added unsafe-database-ACL/current-unchanged test; Docs 34007392596 PASS. Every complete-diff/audit family was rechecked after A2; no pending actual evidence remains for this bounded offline foundation. Review Pass A clean after two pre-PR correction cycles (A2). No CI-discovered production defect, known escaped defect or unresolved Product Decision. Full service/client and final Owner ceremony remain mandatory 42d3 work.

## #42d2c Review Pass B finding

Fresh PR #78 review at 0af209d467c1563a3cde700b180ff450c3dd199b found a cancellation window between material creation and the credential-selection transaction: a token canceled before that transaction could still install/replace CurrentCredentialRef because no check preceded the commit. Cycle B1 introduces CommitAndCompleteAsync: check cancellation, commit selection, then enter the uncancelable publication barrier. Tests prove canceled-before-commit performs neither selection nor publication, while cancellation during commit still requires publication. Recursive Host-root validation and identity creation also observe cancellation before further setup writes. Existing post-commit lease guarantees are retained. Both review and required validation must repeat on the corrected PR branch; no merge until clean.

## #42d2c repeated Review Pass B

Corrected exact code HEAD b8202ddcf7e7c4f949b7b4cc7646536573ab2e86: zero-warning Release build, 201/201 ordinary tests, strict docs, CI 34007609651 (both ordinary and real Windows service/multi-user integration jobs) and Docs 34007610485 PASS. Re-read the complete twenty-file PR diff after B1, including all production entry points, test fixtures, project edge and sibling documentation changes. Rechecked privilege and stopped-service gates before state access, lease lifetime, strict ACL/owner/reparse validation, cancellation before selection and uncancelable completion after it, metadata-only legacy recovery, Old-private-key independence, HMAC separation, ticket/handoff ordering and rollback, public-only output and cleared private buffers. No further substantive finding.

The complete invariant registry was re-evaluated against the resulting whole: ARCH-001/002, HOST-001/002, PERSIST-001, CLIENT-001-003, IDENT-001-004, LOCAL-001-004, OWNER-001/002, REMOTE-001/002, PAIR-001-004, AUTH-001-005, PROTO-001, OPS-001-004, RECOVERY-001, SEC-001, MIG-001, PLATFORM-001/002 and LINUX-001 retain the Pass A PASS/boundary/unchanged classifications. The unsafe-ACL actual-evidence qualifier is now PASS. Stale-reference and whitespace searches are clean; final online ceremonies remain explicitly incomplete. Review A and repeated Review B are clean after three correction cycles (A2/B1). B1 was a review-only, pre-merge defect; no CI-discovered production defect or known escaped defect for this unit. No unresolved Product Decision. Final ledger-only HEAD, Docs and unchanged base/exact PR HEAD verification remain the merge gate. No external GitHub Codex reviewer was requested or inspected.

## #42d2c integration checkpoint and remaining #42d3 decomposition

#42d2c SHADOW DONE as the offline executable foundation. PR #78 final HEAD 0e84b427b000ab3177cfc4320b46bf405abb9b0b merged as 0ae20bd024428b64a1d50c0406352c2e226eac02 after exact base/HEAD checks. Code/test SHA b8202ddcf7e7c4f949b7b4cc7646536573ab2e86; CI 34007609651 and final Docs 34007854005 PASS. Three correction cycles (A2/B1), both reviews clean. Eighteen PRs #61-#78 merged; eight canonical children plus nine #42 foundations and the separate #34 correction. Total correction cycles 49; 201 ordinary tests plus actual Windows integration. Two historical escaped defects remain corrected; none known open. Current canonical #42 body and Project refreshed read-only: dependency contract unchanged, canonical status Backlog; accepted trial dependencies permit the remaining bounded work. No downstream #42 dependency or #55/Linux/release gate is declared complete.

Decompose remaining #42d3 before implementation:

| Trial unit | Bounded objective and dependency | Completion evidence / exclusions |
|---|---|---|
| 42d3a | Durable client credential ceremony state, based on accepted per-user store and intended-user handoff foundations | Preserve one fresh key per exact Host/ticket/purpose across lost replies and process restarts; retain current credential until confirmed binding; exact-key commit, safe concurrent/retry/failure behavior, explicit existing-key re-home; ordinary and actual two-user DPAPI tests. No RPC or Host authority mutation. |
| 42d3b | Version-negotiated local authentication/enrollment protocol session and gRPC binding over the accepted TLS named pipe | Connection-scoped challenge/native identity, fresh authority checks, bounded input/error/secret handling, bootstrap-only uninitialized surface and explicit Owner enrollment/removal; actual adapter integration. No remote listener or operational/capability engine. |
| 42d3c | Production service and ordinary client composition with final multi-user ceremonies, depending on 42d3a/b | Real service startup/reconciliation and ordinary current-user clients; actual bootstrap, additional principal, revocation, rotation/re-home, lost-response recovery, unregistered transport-eligible denial, Owner cardinality/audit and offline lease denial. Only then assess full #42 acceptance. No Avalonia feature screens, Linux or fabricated field evidence. |

42d3a preflight: change only Client.Platform.Contracts, Client.Platform.Windows, tests and implementation docs/ledger; exact product graph retained. Add a per-user ceremony storage seam, not a Host/private-key or OS-identity selector. The DPAPI envelope atomically tracks current material and one pending exact Host/ticket/purpose/key choice. Repeated preparation must reuse the durable key; another pending ceremony cannot silently displace it. Confirmation must match exact context and public key before promoting the result. Retain a completed receipt for immediate post-bind retry; no stale completion may overwrite a later credential. Explicit pending discard is only a local recovery seam after authoritative terminal refusal, never an automatic timeout/lost-response action. Existing-key reuse is confined to re-home and must be selected by the later authenticated ceremony after Host target-state validation. Legacy store methods must not bypass pending state; v1 payloads remain readable. Clear private buffers on failure/ownership transfer and never persist handoff secrets.

Review every invariant family, especially LOCAL-002/004, CLIENT-001-003, OWNER-001/002, SEC-001, PLATFORM-001 and RECOVERY-001. Tests cover exact-context retry/reopen/concurrency, fresh rotation versus retained active key, existing-key re-home, mismatched key/context/result, atomic-write failure at preparation and promotion, canceled calls, malformed/versioned payloads, pending protection from legacy APIs, completed retry and stale completion after a later ceremony, and actual second-user DPAPI denial. No product authority is added. Stop if accepted recovery semantics cannot be preserved; full local build/suite/docs, applicable CI and two clean reviews required.

## #42d3a implementation and Review Pass A

Branch astra/42-client-ceremony-state starts at 2f38bfc21bdf4444f62d003f86b0680b4e11ebc8. Implemented the ceremony seam and version 2 current-user DPAPI envelope in the existing store without changing product project edges. One pending exact context preserves the current key until exact Host-result confirmation; a completed receipt makes immediate retry idempotent. Legacy methods cannot displace an unresolved pending key. Existing-key re-home is explicit, rotation requires fresh material and preserves a known principal ID. Version 1 data remains readable. No Host state, secret handoff delivery, RPC or authority change.

Initial build zero warnings/errors and 204/204 ordinary tests PASS. Pre-PR review cycle A1 adds refusal when a completed ticket is reused with a different purpose/key choice, a defensive fresh-key check against current material, and post-rename/lost-result confirmation evidence. Actual Windows fixture now uses real P-256 current and pending keys, tests second-user DPAPI denial both before and after promotion, and confirms exact pending public-key reuse in a separate same-user process. Corrected full suite and actual CI remain pending; no PR yet.

| Invariant IDs | Evidence / applicability | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | Only client platform seams, test and docs changes; no project edge, WPF or Lan change | PASS |
| CLIENT-002/003, LOCAL-002, SEC-001 | Current-user DPAPI contains only user's own keys; no Host store/ref/SID selector or handoff secret; internal private buffers cleared, returned copies caller-owned | PASS; actual two-user evidence pending |
| HOST-001/002, PERSIST-001, OWNER-001/002, LOCAL-001/003 | No authoritative Host writer or grant/Owner mutation; confirmation is a storage primitive requiring later authenticated Host result, not authority evidence itself | PASS boundary |
| LOCAL-004, PROTO-001 | No new transport or credential transmission; later client must authenticate Host and negotiate before use; exact context and unknown purpose/key-choice refusal | PASS boundary |
| IDENT-001-004, REMOTE-001/002, PAIR-001-004 | Client Host binding is semantic identity; no machine credential, rotation, pairing, replacement or remote routing change | PASS unchanged |
| AUTH-001-005, OPS-001-004 | No grant issuance, capability engine or operational executor; exclusive per-file lock serializes state transitions | PASS unchanged/boundary |
| RECOVERY-001 | Pending key survives restart/lost reply; pre-rename failure preserves old state; atomic promotion plus receipt survives post-rename failure; stale confirmation refuses; discard explicit after terminal refusal | PASS boundary |
| MIG-001, PLATFORM-001/002, LINUX-001 | Local v1 envelope remains readable, no v0.4 mutation; DPAPI confined to client Windows seam; no service shell/UI or Linux behavior | PASS |

Complete-diff review covers every read/write/prepare/confirm/discard/legacy entry point, private-buffer ownership, contextual idempotency and same-file atomicity. Final service/client ordering and full #42 ceremonies remain incomplete. No unresolved Product Decision or known escaped defect. External GitHub Codex review intentionally omitted.

Cycle A2 found an introduced legacy-method regression: permitting an empty version 2 envelope after explicit pending discard also let CreateAndStoreAsync accept an empty generator result, replacing its previous rejection. Restored the explicit generation check and added a no-file-created regression assertion. This was pre-PR review-discovered, not an escaped defect. Linked the handoff documentation to the new durable binding seam while retaining final executable exclusions. Candidate 44788957da0b06c64391906fd0b564be271cdfc3 passed 204/204 local tests; its initial actual CI remains pending. Corrected A2 build has zero warnings/errors and strict docs PASS; corrected suite/remote CI required before Pass A is clean.

Cycle A3 cross-checked all four actual Host completion methods and found that their accepted consumed-ticket retry semantics return the original principal ID without validating/re-registering the retry's submitted public key. The initial client receipt tracked only the last completion: after a later rotation, preparing an older completed ticket could therefore generate a fresh key, accept the historical ID confirmation and strand the client. Persist retired ticket contexts atomically when replacing the completed receipt and permanently refuse re-preparation for those exact Host/ticket IDs, even with changed purpose. No old private keys are retained. Added a cross-layer test with real Host bootstrap/rotation transactions, a deliberately lost bootstrap result, later key rotation, historical Host confirmation and continued authentication with the correct current key. This is an A-only pre-PR finding, not an escaped defect or a change to accepted Host retry semantics. Initial CI 34008254031/Docs 34008255049 and A2 CI 34008341970/Docs 34008342796 PASS; A2 local suite 204/204 PASS. Corrected A3 full validation remains required.

Corrected executable/test candidate f3a9437741bee4741801407c0a4b7b130a086df7: zero-warning Release build, 205/205 ordinary tests, strict docs, CI 34008506493 (ordinary and actual Windows service/multi-user jobs) and Docs 34008507307 PASS. Re-evaluated all nine changed files, every invariant family and stale references after A3. The actual second-user/current-and-pending DPAPI qualifier is now PASS. Review Pass A clean after three correction cycles (A3); all were pre-PR review findings, no test-discovered production defect or escaped defect. A3's final comment/documentation clarification explicitly requires prepared-key authentication before client confirmation; an ID-only historical result cannot establish the current key binding. No executable change followed this tested candidate. Final production transport/ceremony composition remains 42d3b/c and full #42 is incomplete.

## #42d3a Review Pass B finding

Fresh PR #79 review at c0bf0f0d23c4d248a3210ff490ca5b7a07cac93f against base 2f38bfc21bdf4444f62d003f86b0680b4e11ebc8 found a lost-result upgrade gap: a v1 unbound key can already be registered by the Host, but the new PrepareAsync generated another key when first attaching a ticket context. Preserve existing unbound material for that first preparation; fresh rotation of a bound credential still requires a different key. Extended the real-repository bootstrap/lost-result/rotation test to start from a DPAPI v1 unbound payload and prove that the upgraded client authenticates the consumed Host principal. Cycle B1 is a post-PR review-only defect, not escaped. Corrected build/suite/docs/CI and complete repeated B audit required before merge.

## #42d3a repeated Review Pass B

Corrected exact code/test HEAD 78295c67af6d022d64ee80ec43919b0f68162d59: Release build zero warnings/errors, 205/205 ordinary tests, strict docs, CI 34008702591 (both ordinary and actual Windows service/multi-user jobs) and Docs 34008703550 PASS. Fresh complete review re-read all nine changed files after B1 and checked every read/write/prepare/confirm/discard/legacy API, v1 bound and unbound upgrade, per-file exclusivity, private-buffer ownership, pending/current/receipt/tombstone transitions, failure before and after rename, replay after later recovery, exact Host/ticket/purpose/key/result checks and the real Host consumed-ticket behavior. The actual Windows fixture proves same-user pending-key persistence across processes and second-user denial before and after promotion. No further substantive finding.

The full registry was re-evaluated: ARCH-001/002, HOST-001/002, PERSIST-001, CLIENT-001-003, IDENT-001-004, LOCAL-001-004, OWNER-001/002, REMOTE-001/002, PAIR-001-004, AUTH-001-005, PROTO-001, OPS-001-004, RECOVERY-001, SEC-001, MIG-001, PLATFORM-001/002 and LINUX-001 retain the Pass A PASS/boundary/unchanged classifications, with actual DPAPI evidence now PASS. Stale-reference and diff checks clean. Review A and repeated B clean after four correction cycles (A3/B1). All four cycles were review-discovered before merge; no CI-discovered production defect or known escaped defect for this unit. No unresolved Product Decision. Final ledger-only HEAD, Docs and exact unchanged base/PR HEAD checks remain the merge gate. Full #42 remains incomplete until 42d3b/c production ceremonies pass. No external GitHub Codex review requested or inspected.

## #42d3a integration checkpoint

#42d3a SHADOW DONE as the durable client ceremony storage foundation. PR #79 final HEAD d6861d0765128639280110d1378f2bd9cae069c5 merged as 67220ff37c1bf7a3474fd50354cbc8bb25cd00dc after exact base/HEAD checks. Code/test SHA 78295c67af6d022d64ee80ec43919b0f68162d59; CI 34008702591 and final Docs 34008873103 PASS. Four correction cycles (A3/B1), both reviews clean. Nineteen PRs #61-#79 merged; eight canonical children plus ten #42 foundations and the separate #34 correction. Total correction cycles 53; 205 ordinary tests plus actual Windows integration. Two historical escaped defects remain corrected; none known open. Current canonical #42/Project refreshed read-only; dependencies and authorized scope unchanged. Continue 42d3b local protocol binding, then 42d3c final production service/client ceremonies. Downstream local-auth dependencies and #55/Linux/release gates remain incomplete or untouched.

## #42d3b preflight: negotiated local security RPC

Compose the accepted authentication and enrollment primitives into bounded protobuf/gRPC methods over the accepted Windows TLS named pipe. Expected files/layers: additive Contracts schema and feature negotiation; Host connection session and RPC adapter; an optional connection-middleware hook in the Windows TLS platform seam; test-only client gRPC binding plus real-adapter tests; docs/ledger. Contracts remains BCL plus protobuf runtime, with no gRPC runtime dependency. Host and tests may use the standard gRPC runtime; generate service stubs outside Contracts and compile only the stub output where necessary, without introducing any new product ProjectReference or changing the architecture guard. No production executable startup/client UI composition until 42d3c.

Protected paths: one disposable protocol/authentication session per actual HTTP/2 TLS connection; native SID supplied only by the Windows platform connection feature, never request input; explicit protocol-major/capability negotiation before security methods; principal challenges/proofs and per-request fresh identity lookup; no capability grants inferred from identity/Owner flag. Before initialization only negotiation and the intended-user bootstrap-completion mechanism may run. Initialized unauthenticated clients may use only bounded native-bound ticket completions and principal authentication; additional enrollment creation/removal requires the authenticated Owner and existing transactional authorization. Owner rotation/re-home completion requires the existing privileged-prepared ticket, never ordinary authenticated authority alone. No offline preparation RPC, machine recovery RPC, remote route, TCP listener, server operation, permission engine, Avalonia feature screen or Linux behavior.

Use generated protobuf messages and the standard gRPC library, bounded message/input sizes and sanitized status errors. Unknown purposes/features deny. Connection close disposes pending challenges/authentication; retries on a new connection must renegotiate and authenticate. Concurrent streams cannot share/reuse a nonce or stale authorization; cancellation must not publish a successful response before durable transaction completion. Raw enrollment/handoff material is confined to authenticated request/response buffers and never logged. Owner invitation output is explicit to its authorized caller; Host persists only the existing keyed verifier. The adapter preserves all existing consumed-ticket/recovery semantics; final clients must prove a prepared key authenticates the returned principal before durable binding and handoff deletion.

Audit every registry family, especially PROTO-001, LOCAL-001-004, OWNER-001/002, PERSIST-001, CLIENT-001-003, SEC-001, PLATFORM-001/002 and ARCH-001/002. Acceptance: protobuf round-trip/unknown-value behavior; actual gRPC/TLS adapter negotiation and refusal before negotiation; uninitialized restrictions; distinct synthetic seeded identities for adapter tests with reliable real native identity supplied by platform; challenge replay/concurrency/connection-close behavior; actual Host transaction effects and rollback; no request-controlled native identity/Owner setters; no unintended TCP listener; secret-safe errors. Real final two-user enrollment/Owner ceremonies remain 42d3c. Required full build/suite/docs, actual Windows CI, complete stale-reference/invariant audit and two clean reviews. Stop if the selected stack cannot preserve accepted ACL/authentication semantics; do not weaken them.

Implementation references verified from primary sources before selecting runtime integration: [gRPC .NET build integration](https://github.com/grpc/grpc/blob/master/src/csharp/BUILD-INTEGRATION.md) documents CompileOutputs=false for separately selecting generated stubs; [ASP.NET Core gRPC hosting](https://learn.microsoft.com/en-us/aspnet/core/grpc/aspnetcore?view=aspnetcore-8.0) documents AddGrpc/MapGrpcService. Current stable [Grpc.AspNetCore](https://www.nuget.org/packages/Grpc.AspNetCore/2.83.0) and [Grpc.Net.Client](https://www.nuget.org/packages/Grpc.Net.Client/2.83.0) are 2.83.0, matching the already accepted Grpc.Tools pin. Runtime restore/build must verify net8.0 compatibility; no unrelated framework/package upgrade authorized.

42d3b starts at da10642eb9940132ccb0030065cadea24dad367c on astra/42-local-security-rpc. Initial schema, separately compiled Host server stub, connection-scoped runtime/service and post-TLS Windows middleware hook build with zero warnings/errors under net8.0. The test client uses standard Grpc.Net.Client CallInvoker and protobuf marshallers, avoiding duplicate generated server/client type names in the same test assembly. No product ProjectReference changed.

Initial review defers installing negotiated state until the entire handshake succeeds and routes malformed principal IDs through the authentication primitive so failed reauthentication clears prior state. The first live probe failed because its test expected gRPC Unavailable for a wrong TLS pin: Grpc.Net.Client reports Internal with the underlying LocalHostAuthenticationException retained in Status.DebugException. The corrected test checks that precise authentication cause and zero delivered RPCs, preserving the required distinction from an absent endpoint. Record this implementation/evidence correction as cycle A1 pending full Review A. Expanded actual single-native-token gRPC probes PASS for negotiation/bootstrap, connection/nonce/native binding, Owner-only authority and recovery/audit rollback. Fixtures explicitly seed other native identities and relocate a synthetic prior Owner to exercise re-home under one actual token; they do not satisfy final two-account production ceremonies. Full suite/docs/CI and complete reviews remain pending; no PR yet.

Initial full suite 207/208: only the mandatory immutable schema-history check failed after the additive feature change. Cycle A2 adds new host-v1-1.pb and local-security-v1-1.pb descriptors, extends the same compatibility guard to the new schema and retains the original snapshot and all negative breaking-change tests. The first host snapshot command used the wrong proto root; regenerated only the new snapshot with the accepted Protos/host.proto source name. Cross-checking imports also identified the new schema's inconsistent host.proto import spelling versus that existing reflected name. Aligned the new schema, stub generation and snapshot to the established root and added import/descriptor-name agreement validation. The corrected schema plus live gRPC probe passes; no production authority defect or escaped defect. Updated sibling protocol/authentication references. Full corrected suite and CI still required before Review A can be clean.

Candidate 970f4a042313c0fcd38a75514d21920dedc95c70: zero-warning Release build, 208/208 ordinary tests and strict docs PASS; initial remote CI 34009621905/Docs 34009622866 dispatched. Review cycle A3 adds direct evidence for server-side oversized-message refusal before handler delivery, exact secret bounds, forbidden preparation/machine-recovery/Owner-setter method absence, all non-bootstrap completion refusals before initialization and malformed-handshake non-authorization. The isolated schema plus four live RPC groups PASS; corrected full suite and remote validation required.

## #42d3b Review Pass A invariant audit

Complete review covers all nineteen changed files, generated stub selection, both immutable schema snapshots and the underlying authentication/enrollment transaction paths. No request field controls native caller identity, connection identity, Owner assignment or a Host private credential. Each protected method has both negotiation/feature gating and its separate identity/ticket/transaction authorization. The retained connection feature is installed after TLS and disposed with the real connection; serialization across HTTP/2 streams prevents nonce reuse. Failed/malformed reauthentication clears prior authentication. The exact platform native SID is checked on every dispatch.

| Invariant IDs | Evidence / applicability | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | Exact product graph retained; Contracts runtime remains protobuf-only; Host compiles only its own server stub; no WPF/Lan change | PASS |
| HOST-001/002, PERSIST-001 | Host runtime requires caller-owned machine lease; all online writes stay in existing Host-owned transactions; no client/offline writer or operational executor added | PASS boundary |
| LOCAL-001-004, OWNER-001/002 | Bootstrap-only uninitialized authority surface; trusted named-pipe native identity; per-connection nonce/proof and fresh principal checks; Owner-only enrollment/removal and privileged-ticket-only recovery completion | PASS adapter; final production multi-user ceremony remains 42d3c |
| CLIENT-002/003, SEC-001 | No machine/client private key input or store reference; TLS verified before delivery; bounded wire buffers and sanitized errors; owned secret copies cleared; generated runtime buffers never logged | PASS boundary |
| PROTO-001 | Explicit major/feature intersection, failed negotiation cannot install state; additive 1.1 schema; unknown features denied; immutable history and import consistency checks preserve prior contracts | PASS |
| IDENT-001-004, REMOTE-001/002, PAIR-001-004 | Semantic HostId in handshake only; no machine rotation/recovery, remote listener/routing/pairing or trust replacement added | PASS unchanged |
| AUTH-001-005 | Identity/Owner flag grants no capability; existing Owner transaction authorization retained; no permission/delegation/default engine or scope change | PASS boundary |
| OPS-001-004, RECOVERY-001 | No server/Host operation executor or lock model change; existing ticket transaction/audit atomicity and consumed-result retries preserved; connection lifetime distinct from durable ticket state | PASS unchanged/boundary |
| MIG-001, PLATFORM-001/002, LINUX-001 | No schema migration/v0.4 writes; Windows SID/pipe behavior remains platform-owned; no service UI or Linux production behavior | PASS |

Stale-reference review updates protocol/authentication documentation and keeps executable and actual two-user qualification outstanding. Test setup that uses synthetic native identities is explicitly labeled. Required corrected actual CI, final full-suite result and repeated whole audit remain before declaring Pass A clean. No unresolved Product Decision or known escaped defect. No external GitHub Codex review requested or inspected.

Candidate 7b548a4810e35fc6390bc8bd9ea508b910d690a6 passes 209/209 local tests, zero-warning build, strict docs and the isolated schema/RPC probe. Initial CI 34009621905 and Docs 34009622866 PASS. Final diff checking exposed Git's text presentation of the new binary descriptor (a protobuf byte sequence was reported as trailing whitespace). Cycle A4 adds a fixture-local *.pb binary attribute, preserving exact bytes across checkout and making diffs/merges treat descriptors correctly. Verified every current snapshot's raw file hash exactly equals its committed blob, including original host-v1.pb; no historical snapshot was regenerated or altered. The code/test path is unchanged; corrected remote validation must include this checkout metadata before acceptance.

Final pre-PR candidate 9f8a113e28d0c52cc32814b95a05aa1e79337588 (same executable/tests as 7b548a4810e35fc6390bc8bd9ea508b910d690a6) passes CI 34009831988 in both jobs and Docs 34009833143. A3 CI 34009721620 and Docs 34009722523 also PASS. Local zero-warning Release build, 209/209 suite, isolated schema/four-group live RPC probe and strict docs PASS. Rechecked the resulting twenty-two-file scope including binary attributes and final sibling documentation clarification, all invariant families and stale references. Review Pass A clean after four correction cycles (A4). The initial schema test failure and TLS-exception expectation mismatch remain honestly recorded; no known escaped defect or unresolved Product Decision. Actual final production service/two-user ceremonies remain mandatory 42d3c work. No executable change after the tested candidate.

## #42d3b Review Pass B

Fresh exact PR #80 HEAD 5177bf1956f9162749bd335f09a26c3a4ef2c615 reviewed against da10642eb9940132ccb0030065cadea24dad367c. Re-read the complete twenty-two-file diff, including connection lifetime, middleware placement, every RPC dispatch/authorization path, failed negotiation and authentication, concurrent streams, native SID binding, immutable schema history/imports, generated stub ownership, message bounds, rollback/cancellation, secret-safe errors, all four live test groups and sibling documentation. No new substantive finding. CI 34009831988 was re-verified successful for its exact code/checkout candidate, with both ordinary and actual Windows integration jobs passing. Only documentation followed that tested candidate.

Re-evaluated ARCH-001/002, HOST-001/002, PERSIST-001, CLIENT-001-003, IDENT-001-004, LOCAL-001-004, OWNER-001/002, REMOTE-001/002, PAIR-001-004, AUTH-001-005, PROTO-001, OPS-001-004, RECOVERY-001, SEC-001, MIG-001, PLATFORM-001/002 and LINUX-001 against the resulting whole. All retain the Pass A PASS/boundary/unchanged classifications. Stale-reference and diff checks clean; documentation consistently leaves final executable/two-user qualification outstanding. Review A and B clean after four correction cycles (A4/B0). Test-discovered schema/exception-expectation failures and review-only findings remain separately recorded above; no escaped defect or unresolved Product Decision. Final ledger-only HEAD/Docs and unchanged integration base checks remain before merge. No external GitHub Codex reviewer was used.

## #42d3b integration checkpoint and remaining decomposition

#42d3b SHADOW DONE as the local security RPC foundation. PR #80 final HEAD d7cc96e8e83fe5cf521b3e3440dbbab2606ae28a merged as 90a5ed7eb46a223ef279f3bf981cabf02b73a0a3 after exact base/HEAD checks. Code/checkout candidate 9f8a113e28d0c52cc32814b95a05aa1e79337588, CI 34009831988 and final Docs 34010254740 PASS. Four correction cycles (A4/B0); both reviews clean. Twenty PRs #61-#80 merged; eight canonical children, eleven #42 foundations and the separate #34 correction. Total correction cycles 57, 209 ordinary tests plus actual Windows integration. Two historical escaped defects remain corrected; none known open. Canonical #42 and Project refreshed read-only; authorized dependencies/scope unchanged. Full #42 remains incomplete.

Final composition crosses two executable seams and actual ceremony qualification, so decompose 42d3c before implementation: **42d3c1** production Host service startup/shutdown, authoritative trust reconciliation and bounded RPC listener, with actual service evidence; **42d3c2** shared ordinary-client ceremony/credential/transport composition used by CLI and Avalonia, with focused failure/retry tests; **42d3c3** actual final two-user bootstrap/enrollment/removal/rotation/re-home and offline exclusivity qualification, correcting integration defects on that bounded branch. Each unit has its own two-pass review and PR. No synthetic test substitutes for the final acceptance. No UI feature screen, permission engine, server operation, remote route or Linux behavior is absorbed. Downstream local-auth dependencies remain blocked until full #42 acceptance.

## #42d3c1 preflight: production Host service composition

Base fdb2bf180597bda7bdc6329cb66b7c26920c44cb; branch astra/42-host-service-composition. Expected changes: Host executable composition and worker lifetime, Windows read-only protected-root validation seam, actual service integration tests and documentation. No product project-reference changes. The existing synchronous SCM lifetime remains usable by prior fixtures; the production callback starts an independently cancellable worker and returns promptly so native CNG/Schannel cannot block SCM StartPending. Worker startup/runtime failure must terminate the failed service after cleanup, never leave a false healthy listener; no credential or exception payload is logged. Normal stop drains/disposes the listener and certificate before releasing the machine lease.

Protected paths: immediate single Host lease, read-only root/ancestor/child ACL and reparse validation under the exact service identity, migration/semantic identity, authoritative public trust reconciliation and retired private/native cleanup, Current metadata/private-key agreement and native cache load, then TLS named-pipe listener with bounded gRPC and connection authentication. The service cannot provision SCM, grant native key-directory access, create a new machine credential or repair missing offline bootstrap. No current credential or invalid metadata/private material means fail closed. Every startup, including crash restart, reconciles before accepting requests. Configuration comes only from the fixed product composition; environment/appsettings/command-line endpoint configuration cannot add TCP or override security options.

Audit every invariant family, especially HOST-001/002, PERSIST-001, CLIENT-001-003, IDENT-001/003/004, LOCAL-001-004, OWNER-001/002, SEC-001, PROTO-001 and PLATFORM-001/002. All protected effects remain online Host writes or existing offline preparation. No new capability/Owner authority, key holder, remote route, operation executor, WPF/Lan or Linux path. Acceptance: actual production Host executable running as its virtual service account; no privileged harness readiness signal; stale publication repaired before RPC; actual production uninitialized negotiation and denial; normal/crash restart and lock ownership; missing/invalid credential failure; exact unchanged service activation ACL and no TCP listener. Ordinary two-user ceremonies and client composition remain c2/c3.

Use disposable GitHub Windows runners for fixed-product executable integration, explicitly refuse pre-existing product service/group/data and remove only resources created by the fixture. Never install the product on this local workstation. Existing unique-service integration remains unchanged. Local full build/suite/docs, remote actual service evidence, complete invariant/stale-reference audits and two clean reviews are required. Stop for a genuine stack/authority incompatibility; do not weaken the service DACL or substitute synthetic readiness evidence.

Initial composition builds without warnings and passes 211/211 local tests plus strict docs. Review cycle A1 replaces CreateSlimBuilder with CreateEmptyBuilder: clearing configuration after construction was too late to make the no-external-hosting-configuration guarantee. The [.NET 8 builder source](https://github.com/dotnet/aspnetcore/blob/v8.0.0/src/DefaultBuilder/src/WebApplicationBuilder.cs) confirms early environment/appsettings loading; the empty builder avoids that and disables automatic development exception pages. All adapter tests now use the production listener builder, with an added hostile environment/hosting-startup/endpoint test. Worker tests prove normal cancellation cleanup and fatal reporting only after failure cleanup. Initial fixture compilation caught a nonexistent NotInstalled enum; the fixture now uses exact SCM error 1060 to establish absence. Review also narrowed fixed-product cleanup to its newly created native grant and replaced a Running wait for expected failed startup with direct SCM start plus Stopped/absent-pipe checks. These are pre-PR corrections, not escaped defects. Actual shipped-service execution remains pending remote CI; local tests do not substitute for it.

Candidate 7231dbc adds actual-service CI 34010679421 and Docs 34010680543. During whole-diff Review A, cycle A2 strengthens actual production refusal evidence for valid-looking but incorrect public fingerprint and a database ACL admitting an ordinary reader. Each fixture mutation/restoration holds the offline lease, with the service stopped; the subsequent production start must terminate without IPC. Expected failed SCM start now accepts the specific wrapped 1053/1067 form as well as direct Win32 errors, still requiring actual Stopped state and no pipe. No production code change in A2. Corrected remote evidence and repeated invariant audit remain mandatory before Pass A is clean.

CI 34010679421 ordinary job PASS, actual integration FAIL: all earlier unique-service/two-user/offline checks passed, then the shipped offline executable refused first fixed-product bootstrap with UnauthorizedAccessException. No readiness or final service acceptance is claimed. Cycle A3 investigation adds fixture-only public directory ACL evidence and directly repeats the read-only root validation on failure; production error sanitization is retained. A2 candidate 8f8766c CI 34010815497/Docs 34010816815 is separately in flight. This is a test-discovered integration failure, not a clean review or an accepted product limitation.

A3 root-cause preflight: [.NET Windows directory creation](https://github.com/dotnet/runtime/blob/v8.0.0/src/libraries/Common/src/System/IO/FileSystem.DirectoryCreation.Windows.cs) applies the supplied descriptor to every missing parent, not only the final directory. The accepted #41 installer calls Create(BuildHostDirectoryAcl(serviceSid)) on a missing product/Host path; a fresh machine therefore gives the service FullControl of the product parent too. That contradicts the later Owner handoff's Administrator/SYSTEM-only ancestor replacement boundary. Earlier fixtures pre-created the parent and missed this default-layout interaction. Treat this as a historical escaped #41 provisioning defect discovered by actual integration, subject to confirmation from the diagnostic runner; do not weaken handoff validation. Bounded correction within c1: provision missing parent directories separately with Administrator ownership, Administrator/SYSTEM write authority and public traversal, validate existing ancestors without repairing them, and reserve the service's full rights for Host itself. No service-time provisioning or ordinary-user authority is added. Re-audit provisioning, handoff ancestry, public trust traversal, client private-key separation and cleanup. The local ACL probe was policy-blocked; no local product installation/permission change was attempted, and framework source plus actual runner evidence are used instead.

Diagnostic CI 34010909255 confirmed the exact defect: both PalworldServerManager and Host had the same service FullControl ACL; direct offline Host-root validation passed, while the handoff ancestor check correctly refused bootstrap. Ordinary job PASS. CI 34010815497 had the same integration failure; Docs 34010816815 PASS. The third historical escaped defect is now confirmed (#41 default parent provisioning; discovered during c1 actual executable tests). A3's correction gives only newly created parent folders Administrator/SYSTEM write and public traversal, validates existing ancestry, and adds explicit actual-install parent ACL assertions. No handoff/credential validation is weakened. Corrected full build/suite/docs and actual CI remain required before this defect can be recorded fixed or the unit accepted.

## #42d3c1 Review Pass A invariant audit (validation pending)

Complete source review covers the single fixed executable root, asynchronous worker and SCM interaction, root and parent provisioning/validation, every pre-listener trust/material stage, RPC configuration, connection shutdown and lease ordering, ordinary tests, actual executable fixture and sibling documentation. No root or endpoint is selected by untrusted inputs. Existing synchronous runtime/lifetime APIs remain for their explicitly synthetic integration fixture; production uses the independent worker. Native provider calls may start on that worker while SCM completes OnStart, but never block the SCM callback itself. The original fixture's external Running signal is not a production dependency and no QUERY_STATUS rights were added to the virtual service.

| Invariant IDs | Evidence / applicability | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | No WPF/Lan modification or new product graph edge; all ordinary-client exclusions retained | PASS |
| HOST-001/002, PERSIST-001 | Immediate global lease before any Host state, online writes only in Host, stopped/offline CLI refusal, listener/certificate disposed before lease; actual default executable qualification in flight | PASS code; actual evidence pending |
| CLIENT-002/003, LOCAL-002/004, SEC-001 | Machine DPAPI authority/native derived cache remain service-side; empty configuration excludes external endpoints and development errors; TLS precedes bounded RPC, no request or exception payload logging; parent traversal does not open Host data | PASS code/tests; fixed-layout evidence pending |
| IDENT-001/003/004, PAIR-001-004 | Semantic Host identity retained, startup projects authoritative overlap/recovery metadata before listener, cache cannot replace missing private authority; no peer activation/rotation/pairing policy added | PASS boundary |
| LOCAL-001/003, OWNER-001/002 | Existing initialization/nonce/native/Owner/ticket gates unchanged; no installer/group/SCM state creates a principal; handoff ancestry strengthened through corrected parent provisioning | PASS adapter; actual two-user ceremonies remain c3 |
| AUTH-001-005, REMOTE-001/002, IDENT-002 | No capability grants, delegated authority, remote client path, server reference or remote listener added | PASS unchanged |
| PROTO-001 | Same negotiated service/feature gate; production builder runs all adapter negative tests, schema unchanged | PASS |
| OPS-001-004, RECOVERY-001 | No server-operation engine; normal stop drains resources, failure exits, crash startup reconciles; ticket transactions remain independent of RPC connection | PASS boundary; actual restart evidence pending |
| MIG-001, PLATFORM-001/002, LINUX-001 | No v0.4 writes; Windows APIs in platform/composition; no service shell/UI or Linux behavior | PASS |

Stale-reference search updates native-cache, reconciliation, offline, authentication, enrollment, Owner-recovery and RPC docs to distinguish production service composition from still-required ordinary-client/final two-user qualification. No unresolved Product Decision. A3 actual CI 34011079481/Docs 34011080362 dispatched against d80e7cc; Pass A is not yet declared clean.

CI 34011079481 ordinary job PASS; actual fixed-product bootstrap, first authenticated RPC, public-trust repair, normal restart, crash restart and original service ACL checks all executed successfully. Both mismatched-fingerprint and unsafe-ACL startup refusal checks then passed. The following positive startup failed because the fixture attempted to restore an unchanged FileSecurity object: its modification flags were clear, so SetAccessControl did not remove the injected Everyone permission. Cycle A4 corrects the fixture to restore saved descriptor bytes through an explicitly modified access descriptor, then directly validates the restored root before restarting. No production boundary change is needed. Local A3 suite 211/211, zero-warning corrected build and strict docs PASS; Docs 34011080362 PASS. Final actual failure-path qualification and both reviews remain pending.

Review cycle A5 found that excluding OutOfMemoryException from the worker catch would leave a faulted background Task behind a still-running SCM service; unlike a synchronous entry-point failure it would not terminate the process. The worker now routes every non-cancellation exception to its minimal fatal exit callback after resource unwinding, without formatting/logging exception data. Added an explicit synthetic out-of-memory failure to the lifecycle test. This is a pre-PR review-only finding, not an escaped defect. A4 CI 34011248797/Docs 34011249646 remains separate evidence; the corrected worker requires fresh full local and remote validation.

Final code/test candidate 8b49f5c22dc62c510b7119d8cc56cb87cb303d9a passes zero-warning Release build, 211/211 local tests, strict docs, CI 34011317222 (ordinary and actual Windows jobs) and Docs 34011318424. A4 CI 34011248797/Docs 34011249646 also PASS, including all shipped executable refusal/recovery/cleanup checks. Re-read all eighteen changed files and re-evaluated every registry family after the last correction. The matrix's pending actual startup, lock, trust, default-layout, refusal and restart evidence is now PASS. Final actual two-user credential/Owner ceremonies remain c3 and full #42 is incomplete.

Review Pass A clean after five correction cycles (A5). Review-only findings: early hosting configuration and catastrophic worker-fault handling; evidence/fixture corrections: negative startup checks, exact SCM absence/wrapped errors, native-grant cleanup ownership and actual ACL restoration; test-discovered production integration defect: #41 parent ACL propagation. That historical escaped defect is now fixed and validated, bringing the historical escaped count to three, all corrected. No escaped defect introduced by this unmerged unit is known. Stale-reference/diff checks clean, no unresolved Product Decision, no external GitHub Codex review. Open the bounded PR into the experiment branch and perform a fresh exact-HEAD Review B.

## #42d3c1 Review Pass B

Fresh PR #81 HEAD 3aec3b4b86548010dc12227b20ee99f6f5452212 reviewed against fdb2bf180597bda7bdc6329cb66b7c26920c44cb. Re-read the complete eighteen-file diff after PR creation: service/worker and exception ownership, startup configuration, exact virtual identity/root/lease checks, authoritative trust and private material ordering, connection/certificate shutdown, parent provisioning and handoff ancestry, every actual executable refusal/restart/cleanup test, hostile configuration and worker tests, test registration and all changed documentation. No new substantive finding. Actual evidence uses the shipped executables and SCM; synthetic account/Owner-state adapter fixtures remain explicitly separate from still-required c3 ceremonies.

Re-evaluated ARCH-001/002, HOST-001/002, PERSIST-001, CLIENT-001-003, IDENT-001-004, LOCAL-001-004, OWNER-001/002, REMOTE-001/002, PAIR-001-004, AUTH-001-005, PROTO-001, OPS-001-004, RECOVERY-001, SEC-001, MIG-001, PLATFORM-001/002 and LINUX-001. All retain the final Pass A classifications, including actual service evidence now PASS and final two-user ceremonies still outstanding. The parent correction strengthens the established handoff boundary without changing Owner or service authorization. Stale-reference/diff checks clean. Review A and B clean after five correction cycles (A5/B0); three historical escaped defects are corrected, none known open. No unresolved Product Decision or external reviewer. Final ledger-only Docs and exact unchanged integration base/PR HEAD remain before merge.

## #42d3c1 integration checkpoint

#42d3c1 SHADOW DONE as the production Host service composition foundation. PR #81 final HEAD 997bd108899965ee3de3d9e154284ec25c7b79d9 merged as 3b56698c2893c05633180d585afcffcb48d9ebf6 after exact base/HEAD checks. Code/test SHA 8b49f5c22dc62c510b7119d8cc56cb87cb303d9a; CI 34011317222 and final Docs 34011564446 PASS. Five correction cycles (A5/B0), both reviews clean. Twenty-one PRs #61-#81 merged; eight canonical children, twelve #42 foundations and the separate #34 correction. Total correction cycles 62, 211 ordinary tests plus actual Windows integration. Three historical escaped defects are corrected, including #41 product-parent provisioning fixed here; none known open. Canonical #42 and Project refreshed read-only: scope/dependencies unchanged, canonical Backlog retained. Continue c2 ordinary-client composition and c3 actual final two-user ceremonies before full #42 can pass. Downstream dependencies, #55/Linux and release gates remain incomplete or untouched.

## #42d3c2 preflight: ordinary-client security composition

Base dda3a4ffddfd9f085c91be82c76d954d1928691e; branch astra/42-client-security-composition. Implement one shared source coordinator compiled into ordinary CLI and Avalonia, with a Windows composition using their existing client-only platform seams. No new product project-reference edges and no Host/Core/persistence/Host-platform dependency in either client. Standard Grpc.Net.Client plus protobuf messages and a private typed CallInvoker adapter avoid duplicate generated service types in the self-test assembly. Tests compile the same shared source and use the actual production listener builder.

Protected paths: read the protected public trust anchor; exact TLS/HTTP2/HostId/protocol/feature negotiation before any credential transmission; activate/query-start only after a precisely classified LocalHostEndpointUnavailableException, never based on a generic gRPC status or a TLS failure; new channel/session per command, without retrying mutations automatically. Load only the current user's DPAPI key, sign the bounded Host/principal challenge, clear caller-owned private buffers, and distinguish authenticated identity/Owner status from capability grants. Owner invitation/removal remain RPCs with Host-side authorization. Invitation secret output is explicit authorized delivery, not diagnostics; completion reads the enrollment code from standard input rather than process arguments.

For protected bootstrap/rotation/re-home artifacts and enrollment codes: preserve the exact Host/ticket/purpose/key-choice context in the durable ceremony store before sending a public key; authenticate the returned principal using that prepared key before ConfirmAsync; require actual Owner status for Owner ceremonies; delete a protected artifact only after durable confirmation. A lost response/cancellation never discards pending keys. Existing active re-home targets reuse their key only after it authenticates; a failed proof is not treated as credential absence. No client offline preparation, machine recovery, Owner setter, direct database write, remote transport or arbitrary endpoint/path switch. Runtime-owned protobuf/string copies are never logged; only owned buffers can be cleared.

CLI supplies bounded identity, invitation/removal, enrollment completion and protected-handoff completion commands. Avalonia receives the same coordinator through its composition root for later authorized screens; this unit adds no feature screen or operational UI. Final real two-user/Owner ceremonies with the shipped clients remain c3. Audit all registry families, especially CLIENT-001-003, LOCAL-001-004, OWNER-001/002, SEC-001, PROTO-001, HOST-002, PERSIST-001 and PLATFORM-001/002. Acceptance tests cover actual pinned RPC authentication/completion, lost-result/retry with the same durable key, confirmation-before-deletion, wrong pin and unknown principal without activation/authority, malformed/incompatible negotiation, Owner-only invitation/removal, rotation/re-home key selection and safe errors. Full build/suite/docs, applicable actual Windows CI, stale-reference/invariant audit and two reviews required; do not waive the final c3 field contract.

Initial coordinator/CLI/lazy Avalonia composition builds with zero warnings and no new product graph edge. Review cycle A1 identified a re-home lost-result gap before tests: a revoked target can retain an old bound client credential, then be reactivated under the same principal ID with a fresh pending key. Recomputing key choice on retry would challenge that now-active ID and fail proof with the old current key before reaching the pending key. Extend the client-only ceremony store with a read-only exact-ticket context lookup (pending or most recent completed, with retired/mismatched context refusal), and reuse its recorded key choice before attempting any new selection. This is public metadata only, with the same DPAPI/file-lock validation; no new persistence writer, authority or schema version. Add a real Host transaction/lost-reply regression for that revoked-target path. No pending state is discarded to recover from transport failure.

## #42d3c2 Review Pass A invariant audit

Whole-diff review covers the shared coordinator, bounded CLI parser/error output, fixed Windows factory, both executable roots/package/source linkage, read-only ceremony-context lookup, real RPC test fixture visibility, all six new test groups/registration and sibling documentation. A1 was found during review before its regression test; no automated test failure or further substantive finding is known. Local zero-warning build and strict docs PASS; full suite and remote validation remain pending. The lookup/preparation race is revalidated by the existing locked PrepareAsync contract; conflicting contexts cannot replace recoverable keys. A lost mutation reply is not automatically resubmitted. A returned principal ID is insufficient without prepared-key proof. Failure after durable confirmation retains the receipt for deletion retry.

| Invariant IDs | Evidence / applicability | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | No frozen WPF/Lan edit or product graph change; linked source and Grpc.Net.Client depend only on existing client/wire seams | PASS code; full graph tests pending |
| HOST-001/002, PERSIST-001 | Fixed local authoritative Host; only client DPAPI state is written locally, all online authority effects remain authenticated Host RPC transactions | PASS |
| CLIENT-002/003, LOCAL-002/004, SEC-001 | Identical CLI/Avalonia coordinator; pinned transport plus exact Host/protocol/feature before proof/secret; only current-user private keys; bounded stdin, fixed errors, explicit secret delivery and owned-buffer clearing | PASS ordinary tests; actual shipped two-user client remains c3 |
| LOCAL-001/003, OWNER-001/002 | Activation grants no identity; Owner-only invitation/removal remain server checked; protected privileged tickets, key proof and current Owner status before durable completion; no Owner setter | PASS ordinary RPC tests; final c3 evidence pending |
| IDENT-001/003/004, PAIR-001-004 | Exact semantic Host binding; public trust reconciliation/cache policy unchanged; no remote trust/rotation/replacement behavior | PASS boundary |
| AUTH-001-005, REMOTE-001/002, IDENT-002 | Identity/Owner result is not a capability grant; no grant defaults, delegation, remote route or server identity added | PASS unchanged |
| PROTO-001 | Explicit 1.1/feature negotiation, bounded messages and exact HTTP2; malformed/missing/major/Host mismatch negative tests; no schema changes | PASS |
| OPS-001-004, RECOVERY-001 | No server operations; durable ceremony context and receipts survive lost replies without unsafe automatic mutation retries | PASS boundary |
| MIG-001, PLATFORM-001/002, LINUX-001 | v0.4 untouched; shared coordinator uses platform seams and fixed Windows composition; lazy UI factory adds no feature screen or Linux behavior | PASS |

Stale-reference search updates credential, trust, Host, authentication, enrollment, Owner, offline and RPC docs to reflect implemented shared client composition and still-required actual c3 ceremonies. No unresolved Product Decision; no external GitHub Codex review. Complete #42 acceptance remains pending final shipped-client multi-user integration. Re-read canonical #42 read-only: scope and dependencies unchanged.

Code/test candidate 60f50360bf69f821ff51c2ac58ec276b39f4f993 passes zero-warning Release build, 217/217 local self-tests, strict docs, CI 34012593108 (ordinary and actual Windows jobs) and Docs 34012593866. Complete Pass A re-audit preserves the matrix classifications; full graph checks and existing actual service/platform evidence are now PASS. Six new groups cover the bounded composition contract; final actual shipped-client two-user ceremonies remain c3. Review Pass A clean after one correction cycle (A1). No further test-discovered or review-only finding; three historical escaped defects remain corrected, none known open. Stale-reference/diff checks clean. Open the bounded PR into the experiment branch, then perform fresh exact-HEAD Pass B.

## #42d3c2 Review Pass B

Fresh PR #82 HEAD e738bbf63081c0a6c4ec5686777d3d4066621cf5 reviewed against dda3a4ffddfd9f085c91be82c76d954d1928691e. Re-read all 25 changed files: channel lifetime and precise activation classification, negotiated Host/feature checks, per-command current-key proof, prepared-key and Owner confirmation, persisted retry context, lock/race refusal, CLI secret/error handling, shared executable roots and graph, all six negative/positive test groups, registration, ledger and sibling docs. No new substantive finding. Runtime-owned serialization copies are not falsely claimed to be erased. The invitation's explicit secret output is not reused by diagnostics. Revoked-target and post-confirmation deletion retries use durable state; no authority is inferred from transport access or a returned ID alone.

Re-evaluated ARCH-001/002, HOST-001/002, PERSIST-001, CLIENT-001-003, IDENT-001-004, LOCAL-001-004, OWNER-001/002, REMOTE-001/002, PAIR-001-004, AUTH-001-005, PROTO-001, OPS-001-004, RECOVERY-001, SEC-001, MIG-001, PLATFORM-001/002 and LINUX-001. Final Pass A classifications stand: bounded composition acceptance and existing actual platform integration PASS; actual shipped two-user ceremonies remain c3. No unresolved Product Decision, new escaped defect or external reviewer. A1/B0, one correction cycle. Final ledger-only Docs and exact unchanged integration base/PR HEAD checks remain before merge.

## #42d3c2 integration checkpoint

#42d3c2 SHADOW DONE as the ordinary-client composition foundation. PR #82 final HEAD b864567012fc6173e244d044b781be8a3289ecbb merged as bee72663f31e339b9b1fea03144a62ed384a8e09 after exact base/HEAD checks. Code/test SHA 60f50360bf69f821ff51c2ac58ec276b39f4f993; CI 34012593108 and final Docs 34012793790 PASS. One correction cycle (A1/B0); both reviews clean. Twenty-two PRs #61-#82 merged; eight canonical children, thirteen #42 foundations and separate #34 correction. Total correction cycles 63; 217 ordinary tests plus actual Windows integration. Three historical escaped defects corrected, none known open. Full #42 still requires c3 actual shipped-client two-user/Owner ceremonies. Canonical #42/#44/#28 contracts refreshed read-only; dependencies unchanged. Continue c3 before downstream security/UI work; #55/Linux/release gates remain untouched.

## #42d3c3 preflight: final actual Windows security qualification

Base 2ae07185b93bca9cdd59662089251dd9e2c6963f; branch astra/42-final-windows-security-qualification. Extend the disposable-runner fixed-product integration to invoke the built ordinary Client.Cli executable under the existing two distinct non-admin Windows logons/profiles. Copy its complete existing solution-build output into a separate fixture binary directory; add no product project edge or production path override. Add a test-only child-process probe and protected per-recipient request/result directories. Enrollment bearer delivery travels only through those ACL-protected files and redirected stdin/stdout, never shared PASS logs, diagnostic exceptions or process arguments. Private keys remain in each user's default DPAPI store; test evidence exports only principal/public-key information, with returned private buffers cleared inside that user process.

Protected effects and evidence: use actual shipped offline Host.Cli to prepare bootstrap/rotation/re-home only while SCM stopped and the machine lease is free; use actual shipped ordinary CLI for all online authority effects. Add both real users to the product activation group, prove eligibility cannot claim identity or Owner, complete intended-user bootstrap and verify artifact deletion only after persisted client binding. Explicit Owner invitation yields distinct second-user key/identity; non-Owner invitation and Owner revocation attempts refuse. Owner revocation survives service restart; explicit new enrollment reactivates the original principal with a fresh key. Rotate Owner credential preserving identity; re-home to an active existing target preserving its key, then back to a revoked target using a fresh key. Verify exactly one active Owner, former Owner denial, stable HostId, secret-free expected audit events and no TCP endpoint. Test same-machine private-state access refusal from the shipped client's user token. Existing storage/native cross-user denial and real service startup/refusal/crash checks remain intact.

Re-audit every registry family, particularly LOCAL-001-004, OWNER-001/002, CLIENT-001-003, HOST-001/002, PERSIST-001, SEC-001, IDENT-001/004, PROTO-001 and PLATFORM-001/002. No synthetic Owner seeding or direct authority writes in the new end-to-end ceremonies; elevated repository access is read-only evidence. Only test fixture transport/result files are written by the harness. No remote transport, permission engine, server operations, UI screens, real Palworld, reboot, #55/Linux or release evidence is claimed. Existing fixed-root absence guards and cleanup ownership remain; all actual process/service/profile/artifact cleanup must PASS. Build/full self-test/docs locally plus manual CI with actual privileged multi-user execution, complete invariant/stale-reference audits and two clean reviews before accepting full #42. Correct any discovered production defect with explicit impact/review history; stop for unresolved product semantics rather than weakening boundaries.

Review cycle A1 corrects an evidence assumption before CI: the ordinary handoff reader selects only the current user's directory. A non-recipient submitting the public bootstrap ticket ID sees its own missing artifact (invalid security data), not a read attempt against the intended user's protected file. The fixture now expects that exact safe classification. Existing real two-user handoff tests still prove direct cross-user OS read denial. No production behavior changed; this was a review-only fixture finding. Initial build passed without warnings; actual c3 execution is still pending.

## #42d3c3 Review Pass A invariant audit (actual evidence pending)

Complete diff review covers both production-executable fixture stages, all per-user request/result handling, exact user-token assertions inherited from UserProbe, binary copying, real SCM/group activation, actual offline and online calls, read-only SQLite evidence, verification of public bindings and Owner cardinality, process deadlines, audit checks and owned-resource cleanup. No product assembly, schema, protocol, authority path or project edge changes. Both user delivery directories remove inherited access and grant only Administrator/SYSTEM and their intended recipient. Every invocation proves the other recipient's marker is unreadable and Host state is unreadable. The shared PASS/error file receives no command output or enrollment secret. Public verifier evidence crosses from user helper to elevated fixture; client private keys do not. Delivery files are removed in finally; all users/profiles/directories remain under the existing mandatory cleanup.

| Invariant IDs | Evidence / applicability | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | Test-only changes; real Client.Cli build output selected, no graph or frozen WPF/Lan changes | PASS code; full suite pending |
| HOST-001/002, PERSIST-001 | One fixed product Host; actual ordinary CLI performs every online write, actual offline executable refuses live service; evidence connection is explicitly read-only and not pooled | PASS design; actual pending |
| CLIENT-002/003, LOCAL-002/004, SEC-001 | Actual non-admin child executable uses fixed pinned transport and own DPAPI store; only public key/ID returned as evidence; other-user delivery and Host-state reads denied; codes via protected files/stdin, fixed diagnostic assertions | PASS code; actual pending |
| LOCAL-001/003, OWNER-001/002 | Two real identities, group-only activation/refusal, intended bootstrap, Owner-only enrollment/removal, durable revoke/re-enroll, rotation and active/revoked-target re-home, single persisted Owner and old-Owner denial | Actual pending, never synthesized |
| IDENT-001/003/004, PAIR-001-004 | Semantic HostId checked after offline preparations, existing actual machine-loss recovery retained; no pairing/rotation-policy edits | PASS boundary |
| AUTH-001-005, REMOTE-001/002, IDENT-002 | No capabilities, delegation, remote routes or server identities added; Owner remains distinct from grant authority | PASS unchanged |
| PROTO-001 | Actual shipped client uses accepted negotiated RPC; no wire change; ordinary protocol negative suite retained | PASS code; full suite pending |
| OPS-001-004, RECOVERY-001 | No server operations; real restart and durable security state checked, cleanup/process deadlines bounded | PASS boundary; actual pending |
| MIG-001, PLATFORM-001/002, LINUX-001 | No legacy writes, new OS behavior or feature UI; explicit Windows runner only; #55/Linux and releases remain gated | PASS |

A1 fixture classification is the only correction to date. No known production defect, unresolved Product Decision or external reviewer. CI 34013057938 and Docs 34013058776 dispatched against 08bb1ed5986a102a0d4b72e7606c81d1889023b9. Local corrected build/strict docs PASS; local full suite and actual remote acceptance still in flight. No c3 or full #42 PASS claim yet.

CI 34013057938 PASS for both jobs at 08bb1ed5986a102a0d4b72e7606c81d1889023b9; Docs 34013058776 PASS. Local full suite 217/217, zero-warning build and strict docs PASS. Actual runner logs explicitly confirm shipped ordinary CLI/two real user credentials, unregistered/intended-user refusal, bootstrap, Owner-only enrollment/removal, durable re-enrollment, shipped offline/ordinary rotation and active/revoked-target re-home, one persisted Owner, old-Owner denial, artifact deletion, explicit recovery audits, unchanged service ACL, no TCP and complete fixture cleanup. All pending actual rows in the Pass A matrix are now PASS. No synthetic result was promoted into this evidence.

Re-read the complete code/test diff and re-audit all registry families after A1. No further substantive finding or production defect. New qualification documentation maps all eight #42 acceptance criteria to actual and ordinary evidence without claiming Avalonia screens, real Palworld/reboot, remote pairing, #55/Linux or release evidence. Sibling client/store/Host/RPC/enrollment/Owner/offline docs now link to the final actual qualification. Review A is clean after one correction (A1/B0 so far), with a final docs-only validation required before PR creation. Three historical escaped defects remain corrected, none known open. Full #42 may become SHADOW DONE only after final review and merge.

Review A2 stale-reference correction: the broader search found earlier seam documents still using prospective composition/recovery language and a full-#42-incomplete statement that would become contradictory at this acceptance. Updated native-cache, trust-channel, reconciliation, secure-store and offline references to their implemented consumers and final actual qualification, while preserving later peer/migration/parity gates. No code or test change; no new production defect. Re-audit classifications unchanged, local strict docs and a corrected remote Docs run required. A2/B0, two correction cycles for this unit.

## #42d3c3 Review Pass B

Docs 34013288132 (7a9e8a3) and corrected Docs 34013317807 (0b84f9f3a89ac724b675a750186dd29d6baac825) PASS; Review A clean after A2. Fresh PR #83 exact HEAD 0b84f9f3a89ac724b675a750186dd29d6baac825 reviewed against 2ae07185b93bca9cdd59662089251dd9e2c6963f. Re-read all 17 changed files: complete new user-process fixture and actual ceremony assertions, existing fixture invocation/cleanup integration, retained bootstrap identity and read-only evidence, TCP/ACL checks, qualification acceptance mapping, every sibling documentation delta and correction history. No new substantive finding. Explicitly checked that command output cannot enter shared diagnostic files, client private buffers remain in the user helper, an expected process error is not treated as a success result, and directory enumeration establishes handoff deletion without relying on a potentially access-denied File.Exists result.

Re-evaluated ARCH-001/002, HOST-001/002, PERSIST-001, CLIENT-001-003, IDENT-001-004, LOCAL-001-004, OWNER-001/002, REMOTE-001/002, PAIR-001-004, AUTH-001-005, PROTO-001, OPS-001-004, RECOVERY-001, SEC-001, MIG-001, PLATFORM-001/002 and LINUX-001. All final Pass A classifications stand, with actual #42 evidence now PASS. The complete qualification does not infer capability authority, change product semantics or cross any later field gate. Stale-reference and diff checks clean. Two corrections (A2/B0), both reviews clean; no automated test failure or production defect in this unit; three historical escaped defects remain corrected. No unresolved Product Decision or external reviewer. Final ledger-only Docs and exact base/HEAD gate remain before merge and full #42 shadow completion.

## #42 completion and #42d3c3 integration checkpoint

#42d3c3 SHADOW DONE; full canonical #42 is now SHADOW DONE in the trial ledger only. PR #83 final HEAD 07762e602a0e08eb65b31d463c7ff004be7401f8 merged as df07fa8125cb393c0cce523659185bac8576a11f after exact base/HEAD checks. Code/test SHA 08bb1ed5986a102a0d4b72e7606c81d1889023b9; CI 34013057938 and final Docs 34013379567 PASS. A2/B0, both reviews clean. All eight #42 acceptance criteria have actual/ordinary evidence mapped in the qualification document. Actual privileged multi-user/shipped executable criteria and cleanup passed; no mandatory #42 field evidence remains outstanding.

Twenty-three PRs #61-#83 merged; nine canonical children (#27, #34-#38, #41-#43), fourteen bounded #42 units including final qualification, and separate #34 correction. Total correction cycles 65; 217 ordinary self-tests plus actual Windows integration. Three historical escaped defects remain corrected, none known open. No external GitHub Codex reviewer. Canonical milestone issue bodies/dependencies refreshed read-only: #44 and #52 now have satisfied shadow dependencies; #45-#51/#53-#55 and later milestone work retain their stated gates. Canonical #42 remains Backlog, not mutated. Next investigate bounded #44 transport/pairing dependency selection and decompose before implementation; #52 is independent if #44 encounters an actual Product Decision. Windows parity, Linux and release/publication remain unqualified/untouched.

## #44 research checkpoint and independent continuation

#44 is SHADOW PRODUCT DECISION REQUIRED after the bounded #44a dependency assessment, preserved at branch astra/44-pairing-preflight commit 87623b9cf7ddc4dd044c506e4ac685d7e02804e8. [Decision packet](https://github.com/shoemin/Palworld-Server-Manager/blob/87623b9cf7ddc4dd044c506e4ac685d7e02804e8/docs/experiments/astra-44-pairing-decision.md): no suitable reviewed RFC 9382 dependency has been established; preserve the required construction pending qualified dependency evidence. No #44 implementation/PR or canonical planning mutation. Strict research docs PASS. User decision requested; elapsed time is not approval. #44/#45 and their dependents remain gated. #52 is independently Shadow Ready from accepted #38/#39/#41/#42/#43 and continues without choosing a pairing construction. Accepted code/test state and totals remain unchanged.

## #52 trial decomposition and #52a preflight

Product Owner approved the recommended #44 decision: preserve RFC 9382 and hold implementation until a suitable reviewed dependency and integration qualification are supplied or commissioned. This resolves the requested direction, not the dependency gap: #44 is SHADOW BLOCKED — QUALIFIED DEPENDENCY REQUIRED; it is not done, and no alternative construction is authorized. Its dependents remain gated. Independent #52 continues under the existing scope. No canonical planning state changes.

#52's canonical dependency/scope/acceptance contract was refreshed read-only. Its dependencies are accepted in the shadow lane, including #38 design acceptance and full #42. Keep the accepted #34-#38 artifacts authoritative; no redesign. Decompose into (a) semantic token and Host-qualified shell-state foundation, (b) Avalonia shell/components/responsive keyboard layout with actual rendered/input evidence, (c) local connection/activation integration and complete layout/theme/accessibility qualification. These are trial-only units, not duplicate canonical issues. Full #52 requires all units and actual applicable visual evidence; pure model tests alone cannot complete it.

#52a base 1916725d75642feafcddfe35e386359db45405b7, branch astra/52-shell-state-foundation. Implement client-owned presentation state for authorized inventory, full Host-qualified selection/focus, stable non-reused display aliases, collision-safe labels, All Servers/This PC/remote grouping and responsive dimensions from the accepted semantic token file. Compile the model source into the ordinary self-test assembly without a new product reference. Reuse the accepted tokens as embedded resources, not copied color literals or a new theme design. Input fixtures may be synthetic only in tests; production must not ship synthetic servers or infer inventory/authority from pairing. Future inventory adapter must supply only Host-authorized data. Selection calls an injected Host revalidation seam; stale async results after inventory/selection changes cannot redirect the target. Focus inspection does not select; filtered/removed selection is cleared explicitly. No inventory RPC or server mutation contract is invented here.

Applicable invariant audit includes every registry family, especially ARCH-001/002, CLIENT-001-003, HOST-002, PERSIST-001, IDENT-002, LOCAL-003/004, AUTH-003, REMOTE-001/002, PROTO-001, PLATFORM-001/002 and LINUX-001. No new credential holder/writer, Host/Core edge, operation executor, remote connection, capability default or Linux behavior. Expected files: Client.Avalonia shell-model source/resource linkage, self-test linkage/cases, bounded shell documentation and ledger. Acceptance: duplicate labels/UUID prefixes and repeated profile UUID across Hosts cannot confuse exact targets; aliases remain stable without reuse; focus/selection separate; revoked/removed/stale inventories fail closed; token variants share exact layout dimensions; width/reduce-motion decisions match accepted design. Full build/self-test/docs and existing actual Windows CI, complete invariant/stale-reference review, and two clean passes required. #52b/c remain required for actual Avalonia/visual behavior and connection UI. #44 remains Product Decision; this unit must not decide its cryptography.

## #52a Review Pass A

Initial build and 221/221 ordinary tests PASS; accepted #34 generator/contrast checks PASS. Review A1 found that an exception or cancellation while revalidating the currently selected target could retain that selection. Corrected current-generation failure handling to clear that target and extended tests from an already selected state; stale failures cannot clear a newer selection. One review-only correction, no automated failure before review. Corrected build PASS without warnings; corrected full suite and remote validation pending. The earlier #44 pending-direction wording is historical; the approved hold above governs current continuation.

Reviewed every model/token/test line, resource/project linkage, registration and documentation against base 1916725d75642feafcddfe35e386359db45405b7. Invalid inventory is validated before mutation; exact typed identities have constructor validation; copied immutable rows and resources prevent caller mutation. All scope, inventory and selection transitions reject stale replies. Focus remains independent. Alias and discriminator changes never alter exact selection keys. Embedded accepted tokens avoid runtime configuration/file authority. No product inventory is populated yet.

| Invariants | Complete applicable audit and evidence | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | Only Avalonia presentation and self-test source/resource linkage; no product project edge or frozen WPF/Lan edit | PASS code; guards rerun pending |
| HOST-001/002, PERSIST-001 | No Host creation, filesystem/process/profile mutation or authoritative writer; injected selector is presentation validation only | PASS |
| CLIENT-002/003, LOCAL-001-004, OWNER-001/002, SEC-001 | No credential holder, enrollment, recovery or security plumbing changed; no secrets/logging in model | PASS unchanged; retained actual CI pending |
| IDENT-001-004 | Exact semantic HostId and full ServerRef retained through inventory, focus and async selection; repeated names/profile IDs and suffix collisions tested; no credential/rotation changes | PASS code; corrected tests pending |
| REMOTE-001/002, PAIR-001-004, AUTH-001-005 | No remote connection, inferred paired inventory, grant/default or authority shortcut; model supplied only authorized inventory, Host must independently authorize later operations | PASS |
| PROTO-001 | No wire/RPC, negotiation, app-version or authorization-enum change; unknown theme denied | PASS |
| OPS-001-004, RECOVERY-001 | No operations, locks or durable operation lifecycle; stale presentation response cannot redirect selection; no operation percent/status fabricated | PASS |
| MIG-001, PLATFORM-001/002, LINUX-001 | No migration, OS probing, interactive Host action, Linux or release work; pure presentation state on client UI thread | PASS |

Acceptance mapping: ExactIdentityAndFocus covers Host-qualified targets and inspection; InventoryAndAliases covers immutable/atomic refresh and removed resources; StaleSelection covers reordering, removal, denial, transport failure and cancellation; TokensAndResponsiveRules covers three palettes, contrast, widths and motion. Full dependency guards/build/tests remain mandatory. Actual rendering/input/native accessibility are explicitly not claimed by #52a and remain #52b/c/full #52 gates. Stale/contradictory-reference search found no contradictory current implementation claim; scaffold view intentionally remains until #52b. No scope change or unresolved #52a Product Decision. Full review rerun after A1 leaves no further substantive finding; A clean only after corrected validation passes.

Corrected local 221/221 suite, zero-warning Release build, strict docs and accepted #34 generator PASS. CI 34014234249 at cf91f4701ae00b4748e8544bef97f81e8f1ac6e3 PASS for ordinary build/tests and actual Windows service/multi-user integration; Docs 34014235495 PASS. All pending Pass A validation rows are now PASS. Review A clean after one correction. No #52a mandatory evidence is being substituted for the later full #52 rendered/native qualification. PR creation and fresh B remain.

## #52a Review Pass B correction

Fresh PR #84 exact HEAD 39c3af18a2a8632370c709260e21de3d110a1b56 reviewed against unchanged base 1916725d75642feafcddfe35e386359db45405b7. Full diff, all eight changed files and every invariant family rechecked independently. B1 found that Unicode line/paragraph separators (U+2028/U+2029) were outside the control/format sanitizer and could still inject identity-layout breaks. Corrected both categories and replaced the loose absence assertion with exact sanitized-output coverage including bidi/newline/both separators. This is a review-only presentation defect, not an authority-key defect. A1/B1, two correction cycles. Repeating validation and full B on this same PR; no merge yet.

## #52a final Review Pass B

Fresh corrected HEAD 25e9346bce7daeb01aee97cce509f2d189c3a49c reviewed against base 1916725d75642feafcddfe35e386359db45405b7. Re-read complete eight-file diff, final sanitizer and async state transitions, project/resource linkage, every acceptance mapping and all ledger/docs changes. No further finding. Full registry re-audited: ARCH-001/002, HOST-001/002, PERSIST-001, CLIENT-001-003, IDENT-001-004, LOCAL-001-004, OWNER-001/002, REMOTE-001/002, PAIR-001-004, AUTH-001-005, PROTO-001, OPS-001-004, RECOVERY-001, SEC-001, MIG-001, PLATFORM-001/002 and LINUX-001 retain the Pass A classifications. No authority creation, credential/persistence change, direct remote/Host/Core edge or operation executor. Cleanup is in-memory invalidation, with no authoritative rollback claim. Stale reference/diff checks clean; later UI/field gates remain explicit.

Corrected zero-warning Release build, local 221/221 self-tests and strict docs PASS. CI 34014473544 at corrected code HEAD PASS for both ordinary and actual Windows service/multi-user jobs; Docs 34014474625 PASS. A1/B1, two corrections, both review passes clean after corrections; no automated failure, no new escaped defect. Three historical escaped defects remain corrected. No unresolved Product Decision in this bounded unit; #44 remains on the approved dependency hold. Final ledger-only Docs and exact base/HEAD merge gate remain before #52a integration.

## #52a integration checkpoint

#52a SHADOW DONE, full #52 still IN PROGRESS. PR #84 final HEAD 3bafd1c522d62dc05140226d6506aa89e9ba80e9 merged as bf1b669615ef898aa96a9d18d2b6173877e4a8ff after unchanged base/HEAD checks. Code/test HEAD 25e9346bce7daeb01aee97cce509f2d189c3a49c; corrected CI 34014473544 and final Docs 34014681342 PASS. A1/B1, two review-only corrections. Twenty-four PRs #61-#84 merged; nine canonical children done, fourteen #42 units, one #52 foundation and separate #34 correction. Total correction cycles 67; 221 ordinary tests plus actual Windows integration. Three historical escaped defects corrected, none known open. No external GitHub Codex reviewer. #44 is on the Product Owner-approved qualified-dependency hold; downstream gates unchanged. Next #52b implements actual shell controls, responsive layout and keyboard behavior with real rendered/input tests; #52c local connection/native qualification remains required. No full #52/Windows parity/Linux/release acceptance is inferred.

## #52b actual shell preflight

Base 1eef9f33b412fe7c8ba6f4dffc045bc0283006f7; branch astra/52-avalonia-shell. #52 dependencies remain accepted; read-only contract refreshed. Implement actual Avalonia shell controls over accepted #52a state: permanent rail, Host groups and exact identity inspection, five workspace tab entry points, global Activity/Alerts/Manager Settings, semantic palettes, compact rail/drawer overlays, focus return and explicit keyboard navigation. Production starts without synthetic inventory or invented operational status. Later feature content stays unavailable with visible reasons. #52c owns actual local connection UI; no new RPC is invented here.

Files/layers: Client.Avalonia view/composition/styles; a separate client UI self-test executable referencing the real client and same-version Avalonia.Headless test package; solution/build-script test registration; docs/ledger. No product dependency edge, credential/authority/persistence/operation executor changes; no Host/Core/remote connection. All registry families apply by boundary audit, particularly CLIENT-001, IDENT-002, HOST-002 and PLATFORM-001/002. System Segoe UI font replaces scaffold Inter composition, matching accepted #34. Headless tests use Avalonia Skia pixels and actual input routing, not HTML/static SVG; native OS accessibility remains distinct #52c/field evidence. Validate required sizes/themes, enlarged text, tree focus without selection, exact Enter target, panel return, target removal, no hidden row leakage and disabled-action reasons. Full ordinary build/tests, actual Windows CI, docs, two passes and visual inspection before merge. Test package is existing Avalonia 11.1.3's own test platform, not a new product framework or Linux implementation. No Product Decision is prejudged.

## #52b pre-PR corrections and Review Pass A

Fifteen correction/verification iterations preceded this checkpoint. They are retained here rather than presenting the first rendering pass as clean: (1) compiler-detected ZIndex API misuse and Theme member collision; (2) visual-detected clipped labels, Fluent token overrides and ineffective enlarged-text/layout evidence, plus a nullable test warning; (3) test lookup collision with Fluent's own Root name; (4) enlarged rail-foot starvation and source-detected identity focus reopening; (5) compiler-detected sealed Border.Render misuse while adding corner lines; (6) remaining compact alias clipping and source-detected stale view failure updates; (7) newly opened control hit-testing before rendered layout plus bounded failure-process handling; (8) failed broad transition override; (9) diagnostic refinement identifying remaining scrollbar template-local transitions; (10) removal of those local transitions; (11) visual-detected variable Fluent tree indentation replaced by an explicit shell template; (12) source review corrected tab overflow affordances and semantic focus outline; (13) runtime-detected wrong focus-template type; (14) resize-overlay state correction and stronger theme/scope/overflow assertions; (15) final Review A corrected stale server focus on group navigation and lost group-collapse state during rebuild. Diagnostic iteration 9 is included in the total; tool-only rejected patches with no changes are not counted. These include both test/compiler findings and source/visual-only findings. None escaped integration because no PR/merge existed yet.

The final A sweep read every source/template/test/project/solution/build-script/doc change against 1eef9f33b412fe7c8ba6f4dffc045bc0283006f7. Removed obsolete Fluent-template style/resource overrides after the explicit tree template replaced them. Accepted model/identity and per-user security code remain unchanged. All UI execution is presentation only; disabled actions have persistent reasons. Actual automation verifies model scope, exact activation, no focus-driven selection, native tree grouping, identity/focus return, overlay dismissal, no Tab trap, no transitions, removed inventory, late failure cancellation, three-theme geometry/brush equality and 640/200% navigation/tab affordances. Rendering/input PASS is genuine Avalonia/Skia; native assistive technology and actual Host connection are not inferred.

| Invariants | Whole-result audit / evidence | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | Product edges unchanged; test-only project points to actual Avalonia client and same-version headless package. Solution builds it and build.ps1 executes it. Frozen WPF/Lan untouched; no hidden MSBuild edges | PASS; full guard rerun pending |
| HOST-001/002, PERSIST-001 | No Host creation or state/process/filesystem mutation; no generic shell launcher, server operation or authoritative writer in views | PASS |
| CLIENT-002/003, LOCAL-001-004, OWNER-001/002, SEC-001 | Existing lazy security composition unchanged; views never access a credential, secret, enrollment/recovery path or machine key; source labels are plain sanitized model data | PASS unchanged; retained actual Windows CI pending |
| IDENT-001-004 | Full ServerRef retained on each node and every selector request; semantic HostId in groups/full accessible details; alias never becomes target; no credential policy changes | PASS actual UI/model tests |
| REMOTE-001/002, PAIR-001-004, AUTH-001-005 | No remote connection, default grant, paired-inventory inference or UI authority shortcut; production begins disconnected/empty; test inventory explicitly synthetic | PASS |
| PROTO-001 | No protocol/RPC/version/authorization enum changes | PASS |
| OPS-001-004, RECOVERY-001 | No operation execution, cancellation, percent, lock or recovery outcome fabricated; closing panels affects presentation only | PASS |
| MIG-001, PLATFORM-001/002, LINUX-001 | No legacy writes, OS action in shared view, Host UI, Linux or release work. Test rendering is Windows Skia/headless, not native parity certification | PASS |

Build has zero warnings; actual UI suite passes after corrections. Prior ordinary 221/221 suite passed; full corrected suite, final strict docs and remote CI remain before declaring A clean/opening PR. Manual source/design comparison covers the accepted permanent rail, compact geometry, three semantic palettes and original Refined corners; inspected frames are stored under docs/design/astra-52-shell. The new documentation distinguishes real rendered controls with synthetic inputs from actual service/native accessibility acceptance. No unresolved #52b Product Decision. Full #52 remains IN PROGRESS; #44 remains on the approved qualified-dependency hold. No external reviewer and no canonical planning mutation.

Final local 221/221 ordinary tests, actual Avalonia rendering/input suite, zero-warning build and strict docs PASS. All twelve final frames inspected, including all themes/widths, disconnected production, compact identity and genuine 640/200% text. CI 34015971125 at db8919c3071c376bbd8af20b0d447bf7621a0ac1 PASS for ordinary/rendered tests and actual Windows service/multi-user integration; Docs 34015972420 PASS. All pending Pass A validation rows are now PASS. Complete diff, registry and stale-reference checks remain clean. Review A is clean after fifteen recorded pre-PR correction/verification iterations. Full #52 connection/native qualification remains outstanding. Next open the bounded experimental PR, then fresh exact-HEAD Review B.

## #52b Review Pass B correction

PR #85 exact HEAD eb28036a22ee01e33c9fc8ac26a8a61195ee472b reviewed against 1eef9f33b412fe7c8ba6f4dffc045bc0283006f7. B1 found BindState(null) reset the cached rows to null before Refresh compared them with the new null state, potentially skipping the tree clear when scope was also null. Old rows could remain visible after disconnect. Corrected BindState to rebuild unconditionally and return focus to All Servers when the tree disappears. Strengthened the actual late-failure/disconnect test to assert zero tree items, null selection and hidden identity as well as unchanged disconnected status. This was source-review-only; the prior test checked status but missed retained rows. Sixteen total iterations (fifteen pre-PR, one B); no escaped defect because not merged. Repeat affected validation and the complete review on this PR before acceptance.

## #52b corrected Review Pass B

Fresh corrected code HEAD c04ce417d17f6a9c07d1979c7dd9606450211d6e reviewed against unchanged base 1eef9f33b412fe7c8ba6f4dffc045bc0283006f7. Re-read all 26 changed files, complete source/template/test/project/build integration and documentation deltas. Twelve corrected rendered frames hash-match the inspected committed evidence. The view replacement fix clears native items even for null-to-null cached comparisons, with no credential or authoritative state mutation. Event subscriptions, cancellation/generation checks, scoped inventory removal, tree focus/selection separation, exact identity keys, pointer/keyboard paths, theme geometry and visible unavailable reasons rechecked. No further substantive finding. No native accessibility or actual connection claim inferred from headless tests.

Full invariant re-audit: ARCH-001/002 and CLIENT-001 pass unchanged product edges and actual architecture guards; HOST-001/002 and PERSIST-001 pass no client authority/writer; CLIENT-002/003, LOCAL-001-004, OWNER-001/002 and SEC-001 retain accepted shared security boundaries; IDENT-001-004 pass full semantic identity and disconnect removal; REMOTE-001/002, PAIR-001-004 and AUTH-001-005 pass no remote/entitlement inference; PROTO-001 has no wire change; OPS-001-004 and RECOVERY-001 have no operation or rollback claim; MIG-001, PLATFORM-001/002 and LINUX-001 pass unchanged platform/release boundaries. All Pass A classifications stand. Stale-reference/diff checks clean. Fifteen pre-PR iterations plus B1, sixteen total; three historical escaped defects remain corrected, none newly escaped. No unresolved Product Decision for this bounded unit; approved #44 dependency hold and full #52 native/connection gates remain explicit.

Corrected local zero-warning build, 221/221 ordinary tests, actual UI suite and strict docs PASS. Corrected Docs 34016296396 PASS. Corrected CI 34016295341 still requires final Windows integration success before merge; the earlier CI is not substituted. Technical Review B is clean, with final remote validation and exact base/HEAD gate still mandatory.

## #52b integration checkpoint

Corrected CI 34016295341 PASS for both ordinary/UI and actual Windows service/multi-user jobs. Final ledger Docs 34016439443 PASS at PR HEAD 49d8a1c71ef811e1f11c7d82c0b8ddc3f42fa63d. Exact unchanged base/HEAD verified; PR #85 merged as ce3fbba44f7b42f2e66edcf3baa8a33cb4d91238. #52b SHADOW DONE; full #52 remains IN PROGRESS. Sixteen correction/verification iterations (15 pre-PR, B1), both reviews clean. Twenty-five PRs #61-#85 merged; nine canonical children done, fourteen #42 units, two #52 units and separate #34 correction. Total correction cycles 83; 221 ordinary tests plus actual rendered/input and Windows integration suites. Three historical escaped defects corrected, none known open. No external reviewer or canonical mutation. #44 qualified-dependency hold unchanged. Continue #52c with connection UI and explicit actual/native qualification.

## #52c1 connection UI preflight

Base 6ad05b66a78d258a4a5e9de95ca3572728aa9de5; branch astra/52-local-connection. Read-only #52 contract refreshed, dependencies unchanged. Decompose remaining c into c1 ordinary connection UI/client integration and c2 actual Windows UI connection plus native accessibility qualification. Implement explicit Connect to This PC through the existing LocalSecurityClient and single App composition root. Add a read-only ConnectAsync result exposing the already validated semantic HostId with proved LocalPrincipal identity, without a new RPC. Only an absent pipe permits the existing bounded IHostActivation start/retry path; no direct Host/Core/service/process/filesystem call in the view. No automatic enrollment, bootstrap, Owner action or authority inference. A successful check is a point-in-time result; inventory remains unavailable because no inventory RPC exists. No fabricated live connection, servers or heartbeat.

All registry families audited in preflight, especially CLIENT-001-003, LOCAL-001-004, IDENT-001/002, SEC-001, HOST-002 and PLATFORM-001/002. Changes are shared client API, App/view wiring, actual UI tests and existing local-RPC seam tests, docs/evidence/ledger. Validate exact Host identity/proof, no activation on trust/protocol/auth failure, single-flight and cancellation, stale completion after close, fixed non-secret diagnostics, empty unavailable inventory and accessible/rendered connection controls. Existing real Windows service tests retained; new end-to-end UI/service identity qualification remains c2 and cannot be inferred from injected UI replies. Native screen-reader/high-contrast evidence remains an explicit actual field gate. Both reviews, full build/tests/guards, remote CI and strict docs required before c1 merge. No new Product Decision or change to approved #44 hold.

## #52c1 Review Pass A

Five pre-PR correction iterations: A1 compiler warning required a true public parameterless XAML constructor alongside the injected constructor; A2 source review found focus could remain on the disappearing Cancel button, fixed with focus return and an assertion; A3 review strengthened evidence to use the real App factory for production startup and added actual 640/200% connection scrolling; A4 compiler found a missing Avalonia extension namespace in that new test; A5 the new test failed because BringIntoView preceded rendered resized layout, corrected to render/layout first, then scroll and assert actual bounds. The final enlarged screenshot confirms the action is reachable and the full Host UUID remains available; no product overflow fix was needed for A5. No escaped defect or authority change.

Complete changed source/API/App/view/test/doc impact review: the public result is returned only after pinned negotiation and proof on one disposed session; identity never comes from a fingerprint or OS group. Single-flight resets old data, cancellation rejects late replies, close prevents further view updates, and every displayed diagnostic is fixed text. The only activation path remains LocalSecurityClient.Open through IHostActivation after a proven absent pipe. All credential holders, enrollment/recovery and grant-issuance paths remain unchanged. Production App construction is lazy and does not connect until the user invokes the button. Real service/UI integration and native accessibility remain explicitly distinct from injected UI tests.

| Invariants | Whole-result evidence | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | No new project/package edges, frozen WPF/Lan untouched; ordinary architecture guards retained | PASS code; CI pending |
| CLIENT-002/003, LOCAL-001-004, OWNER-001/002, SEC-001 | Same pinned channel/per-user proof; no machine key, enrollment/Owner shortcut or raw diagnostics; API unregistered/wrong-pin tests and UI failure tests | PASS local; actual regression CI pending |
| IDENT-001-004 | HostId from exact validated negotiation; principal proof matches; full selectable Host UUID; no rotation changes | PASS local |
| HOST-001/002, PERSIST-001 | Only accepted activation seam; no local Host/process/filesystem writer or privileged client | PASS |
| REMOTE-001/002, PAIR-001-004, AUTH-001-005 | Empty inventory explicitly unavailable; no remote connection, pairing inference or capability creation | PASS |
| PROTO-001 | No new RPC/version/enum; existing negotiation checks unchanged | PASS |
| OPS-001-004, RECOVERY-001 | UI cancellation only cancels the request; explicitly no rollback/stopped-Host claim | PASS |
| MIG-001, PLATFORM-001/002, LINUX-001 | Single App/Program Windows composition; no legacy, Host UI, Linux or release changes | PASS |

Corrected zero-warning build, 222/222 ordinary tests and actual rendered/input/connection suite PASS. Strict docs previously passed and final refresh validation remains. Required remote CI, full refreshed-frame inspection, final stale-reference review and both review gates remain before merge. #52c1 is not full #52 acceptance.

A6 source review found that an external BindState replacement could retain the last verified identity or allow an in-flight connection reply to replace the newer view. Binding now clears verified identity/details and advances a generation; stale success/failure cannot establish a new identity after replacement. Added actual UI tests for clearing a successful check and replacing a pending view before its late success. Six total pre-PR corrections; the earlier CI candidate is superseded for acceptance. All eighteen refreshed frames inspected; corrected renders must match or be re-inspected. Complete registry classifications remain unchanged, with lifecycle evidence strengthened. No new authority or product semantics.

Corrected code 4d2581d6b812a0c9b64cbf79bbf51d3273d5a314 passes local zero-warning build, final 222/222 suite and actual UI suite. Seventeen corrected frames hash-match inspected evidence; the refreshed compact-identity frame was separately re-inspected and committed. Corrected CI 34016893987 PASS for ordinary/UI and actual Windows integration; Docs 34016894890 PASS. Initial candidate CI 34016788875 also passed but is not substituted for the correction. All Pass A pending rows are now PASS. Complete 31-file diff and registry re-audit/stale-reference search clean after A6. Review A clean; no unresolved Product Decision, and later full #52 field gates remain explicit. Proceed to PR and fresh exact-HEAD Review B.

## #52c1 Review Pass B

Fresh PR #86 exact HEAD c67afd082d8f4589f351bb885a3863147659bdb4 reviewed against unchanged base 6ad05b66a78d258a4a5e9de95ca3572728aa9de5. Re-read complete 31-file diff, all shared/API/App/view/test changes, documentation and final rendered evidence mappings. Re-evaluated request lifetime, current-binding generation, cancellation/close, fixed diagnostic classifications, conditional activation, point-in-time identity semantics and unavailable inventory. The injected test factory is not used in the production App path; no normal UI ceremony or authority shortcut exists. No additional finding. Actual UI/service and native fields remain unclaimed and required for later qualification.

All registry families re-audited: ARCH-001/002, HOST-001/002, PERSIST-001, CLIENT-001-003, IDENT-001-004, LOCAL-001-004, OWNER-001/002, REMOTE-001/002, PAIR-001-004, AUTH-001-005, PROTO-001, OPS-001-004, RECOVERY-001, SEC-001, MIG-001, PLATFORM-001/002 and LINUX-001 retain the final Pass A PASS/boundary classifications. Existing secret holders, grant paths and authoritative writers are unchanged. No direct OS operation in the view, no protocol/production dependency change, and cancellation promises no authoritative rollback. Stale-reference and diff checks clean. Corrected 222/222 ordinary, actual UI, zero-warning build, strict docs and CI 34016893987 PASS. A6/B0, six corrections; both reviews clean, no newly escaped defect. Final ledger Docs and exact base/HEAD gate remain before bounded c1 merge. Full #52 is incomplete; #44 remains on its approved dependency hold.

## #52c1 integration checkpoint

PR #86 final HEAD 2261d0beaac719b2de81cf703ed05bd578372bfd merged as 87cf3a91874778262e93b6b04c6e73560463203b after exact unchanged base/HEAD checks. Code 4d2581d6b812a0c9b64cbf79bbf51d3273d5a314, CI 34016893987 and final Docs 34017115203 PASS. #52c1 SHADOW DONE, full #52 IN PROGRESS. A6/B0, six corrections; twenty-six PRs #61-#86 merged, nine canonical children, fourteen #42 units, three #52 units and separate #34 correction. Total correction cycles 89; 222 ordinary tests plus actual UI and Windows integration. Three historical escaped defects remain corrected. No new external reviewer/canonical mutation. Continue c2a actual UI/service/two-user qualification, then assess c2b native accessibility field evidence; #44 approved qualified-dependency hold remains.

## #52c2a actual UI/service qualification preflight

Base f0e98ce179ec5960e73c51b082bb28e4a6cae1b9; branch astra/52-windows-ui-qualification. Read-only #52 contract/dependencies unchanged. Extend the existing explicit Windows CI fixture to run the actual Avalonia App-created window/connection button in the two existing real non-admin user processes. Use the real WindowsClientSecurity composition, DPAPI credentials, pinned named pipe and installed production Host service. The UI harness may use headless windowing/input for this service test, but must inject no connection reply or authority. Exercise unregistered refusal, exact Host/principal success for both users, revoked refusal, re-enrollment/recovery and dormant-service activation. Existing protected per-user delivery and cleanup remain; only public results/fixed diagnostics cross the helper. No new product command/flag, library, protocol, authority or canonical mutation.

Actual acceptance requires successful Windows CI logs, not local synthetic execution. Local token is not elevated, so do not install a product service here. Full ordinary/UI tests, complete diff/invariant/stale-reference audits and two clean reviews remain required. Native screen-reader/high-contrast/window-decoration acceptance is separate c2b FIELD EVIDENCE REQUIRED until genuinely executed; this subunit cannot complete full #52 or #55. All invariant families apply by retained production boundaries and fixture evidence, particularly CLIENT-001-003, LOCAL-001-004, IDENT-001/002, HOST-001/002, PERSIST-001, OWNER-001/002, SEC-001 and PLATFORM-001/002.

## #52c2a Review Pass A candidate

A1 source review caught that inserting UI activation checks before existing CLI checks would replace two prior dormant-CLI scenarios. Restored the original CLI cases and added separate stops for UI activation, preserving both clients' actual coverage. One review-only correction; no compiler/test failure to date. Local zero-warning build, ordinary actual-rendered/input UI suite and strict docs PASS. Full ordinary suite and actual Windows run pending. Production code and reference graph unchanged.

Complete fixture audit: new mode exists only in the test executable, uses the real App factory without reply/transport injection, and reports public identity through existing protected user delivery. Failed UI cannot emit success JSON or retain rendered identity. Unexpected fixture exception uses fixed text. The thirty-second UI fixture deadline, forty-five-second child deadline and existing outer cleanup remain bounded. Existing enrollment/revocation/recovery ceremonies still create all authority explicitly; added checks only authenticate. No database fixture writes or service permission changes added.

| Invariants | Whole-result evidence | Result |
|---|---|---|
| ARCH-001/002, CLIENT-001 | Test/docs only, no new product/package edge or frozen source change | PASS code; guards pending |
| CLIENT-002/003, LOCAL-001-004, OWNER-001/002, SEC-001 | Real App/shared production IPC, actual per-user DPAPI and refusal; protected result paths and fixed diagnostics | PASS code; actual pending |
| HOST-001/002, PERSIST-001 | Existing installed service/exclusivity and offline ceremony fixture; UI only invokes accepted activation/IPC | PASS boundary; actual pending |
| IDENT-001-004 | Exact HostId/principal compared with actual fixture, repeated across recovery; no credential-policy edits | PASS code; actual pending |
| REMOTE-001/002, PAIR-001-004, AUTH-001-005, PROTO-001 | No remote traffic, grants or new wire method; unchanged negotiated authenticated path | PASS |
| OPS-001-004, RECOVERY-001 | No server operation/rollback claim; actual Host stop/restart remains fixture-owned | PASS |
| MIG-001, PLATFORM-001/002, LINUX-001 | Existing isolated Windows-only test; no Host UI, Linux, migration or release | PASS |

Native accessibility remains c2b FIELD EVIDENCE REQUIRED, explicitly documented with observations/build identifiers needed. Actual Windows CI must pass before A is clean, PR creation or c2a acceptance. No unresolved Product Decision; #44 stays on its approved qualified-dependency hold.
