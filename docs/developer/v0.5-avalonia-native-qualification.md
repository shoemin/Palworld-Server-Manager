# Native Avalonia functional qualification

Trial reference: #52c2b. On 2026-09-06 the Product Owner explicitly removed screen-reader/accessibility qualification from this isolated trial. Assistive-technology announcements and OS high-contrast certification are **WAIVED BY PRODUCT OWNER**, not PASS. Existing keyboard/focus, semantic identity, contrast and reduced-motion code remains unchanged. This deviation applies to the shadow acceptance contract; it does not rewrite the canonical Issue or certify accessibility.

## Actual environment and build

The existing Release application was launched through its real Windows desktop composition, without the headless test platform. Environment: Windows NT 10.0.26200.0, interactive non-elevated user, Avalonia `11.1.3+45a2d36e3214ac4b3623940ba728b2503de5c668`. Native observations were made around 13:47 America/Chicago on 2026-09-06 using the computer-use runtime's targeted window input/state capture. Screenshots were inspected directly in the task. UI Automation supplied control indexes and window/focus observations; no screen reader was run.

Executable: the workspace's `src/PalworldServerManager.Client.Avalonia/bin/Release/net8.0-windows/PalworldServerManager.Client.Avalonia.exe`. Client assembly informational version: `0.3.0+f0e98ce179ec5960e73c51b082bb28e4a6cae1b9`; SHA-256: `4B640D758BCAF47D8204B5C5CC4FB89D2A7DF21714008801D9D572D4594EF4B2`. The assembly was built before the test/docs-only #52c2a commit. `git diff --quiet f0e98ce179ec5960e73c51b082bb28e4a6cae1b9 080fb6341277d771bff4439a8060402cbbb017f3 -- src` returned zero: production source matches the accepted checkpoint exactly. The version string is evidence identity, not a release/version change.

## Observations

| Action | Observed result |
|---|---|
| Launch exact workspace executable | One targeted native window; disconnected shell, empty authorized inventory, disabled unsupported features and visible reasons. No automatic connection or service action. |
| Native Maximize button | Window control changes to Restore; settled screenshot shows full desktop-sized shell and reachable controls. Immediate animation frame was not used as final layout evidence. |
| Native Restore button | Window control returns to Maximize; normal-size shell restored. |
| Native Minimize button | State capture explicitly reports the window is minimized. |
| Activate minimized window | Same application window returns with disconnected identity and controls intact. |
| Manager Settings, then Light Minimal | Real side panel opens; inspected screenshot confirms light semantic palette and unchanged shell/panel layout. No OS setting or persisted preference changed. |
| Escape from theme button | Panel disappears and focus returns to Manager Settings. |
| Native Close button | Trial window disappears from enumeration and the application process exits. |
| Relaunch then Alt+F4 | Clean disconnected window reappears; keyboard close removes it and process enumeration confirms exit. Immediate enumeration before close settled was rechecked; no forced termination used. |

All listed functional observations PASS. No native functional defect was found. No connection request, credential operation, service installation, OS security/high-contrast setting change or unrelated application action was performed. The test-created application is closed.

The [eighteen rendered/input frames](../design/astra-52-shell/README.md), 222-test ordinary suite and [actual Windows UI/service qualification](v0.5-avalonia-windows-qualification.md) remain separate evidence for unchanged code. These native observations complete the residual functional window checks; full #52 acceptance under the revised contract still requires the recorded A/B review gates. Server inventory/features, #53/#54, Windows parity #55, Linux and release are not qualified here.
