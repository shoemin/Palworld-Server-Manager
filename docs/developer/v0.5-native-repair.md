# Native revoked-relationship re-pair qualification

Trial #28o exercises a complete same-key revoke/re-pair sequence using the genuine hash-checked Windows Rust SPAKE2 provider, actual local named-pipe TLS/native identity/signatures, both Host network generations and actual peer mutual TLS. It first pairs through local Owner RPCs, verifies that pairing creates no grants, then explicitly activates through the local Owner RPC with each Host's canonical configured-default activation hook.

Both Hosts independently revoke the relationship through canonical commands. The qualification supplies separately signature-authenticated local Owner proof to those commands; their public wire initiation is not yet exposed. Old root and delegated child grants become invalid while an unrelated Owner-issued grant remains intact. Stale activation and old-incarnation capability use refuse. A fresh invitation and genuine PAKE produce verified replacement candidates, with both relationships still revoked. Neither candidate is manufactured by calling the verified-binding persistence method directly.

One local Owner approves its exact candidate without approving the other Host. The other Host continues rejecting contact until its own Owner separately approves its candidate. Both create fresh current-Owner/null-parent roots from current configured defaults; original grant IDs and their invalidated children never revive. Both generation-owned senders exchange the original approval acknowledgments over actual TLS, commit one receipt and one confirmation each, and a repeated sender call has no pending work. Fresh authenticated contact and canonical capability use succeed with current incarnations; the old incarnation and a local user lacking independent permission remain refused. Revocation and approval audits identify the actual local Owner, and each Host retains its original active Owner.

This additional native-provider scenario runs in the guarded native qualification alongside existing PAKE/wrapper tests. It is not an added ordinary self-test: that suite remains634. The test's extra non-Owner local identity is explicit fixture setup used to check grant lineage, not a claim of additional OS-user enrollment. Pairing/activation are actual public local RPCs; revocation/approval are canonical commands with separate authenticated proof fixtures. No new production or wire behavior is introduced.

This proves same-key repair after revocation. Complete own fresh-key offline recovery, opportunistic authenticated-contact retry, public revoke/approval initiation and actual remote process/network hang/drop remain parent #28 work. No physical Palworld, Windows parity, Linux or release qualification is claimed. Creator permissions and limits remain unchanged; accessibility/screen-reader certification remains WAIVED, never PASS.

The Astra trial used no external GitHub Codex reviewer. Astra was responsible for both implementation and technical review.
