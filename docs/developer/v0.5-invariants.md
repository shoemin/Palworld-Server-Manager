# v0.5 invariant registry

This registry exists because [#19](https://github.com/shoemin/Palworld-Server-Manager/issues/19) exposed repeated cross-section contradictions in the v0.5 architecture even when individual correction requests were followed correctly. A large prose document is a poor structure for keeping requirements consistent across many rounds of editing — a short, numbered, checkable list is not.

**This registry is normative.** Detailed design documents (`docs/developer/v0.5-architecture.md`, future implementation docs) explain and elaborate these invariants; they do not override them silently. If a design document and this registry ever disagree, the design document is wrong and must be corrected — not the other way around, and not by quietly editing this file to match a design decision without recording why.

Every `§`-numbered Source citation below refers to sections of [`docs/developer/v0.5-architecture.md`](v0.5-architecture.md), the [#19](https://github.com/shoemin/Palworld-Server-Manager/issues/19) deliverable.

Each invariant has a stable ID. IDs are never reused or renumbered — a retired invariant is marked retired, not deleted, so historical references (issues, commits, review reports) stay meaningful.

Every applicable-invariant claim in an issue or PR must cite IDs from this table, not paraphrase them.

## How to use this registry

- **Planning an issue**: list every invariant ID the work could plausibly touch (see the preflight requirement in `docs/developer/agent-workflow.md`) — err toward including a borderline one rather than omitting it.
- **Reviewing an issue before Review Required**: audit the resulting whole against every applicable ID with a compact PASS/FAIL matrix (`docs/developer/agent-workflow.md` defines the required format) — not just the specific delta that was changed.
- **Writing a new invariant**: keep it short, normative, and checkable. If it takes a paragraph to state, it's a design decision, not an invariant — put the paragraph in the architecture doc and link to it from a one-line invariant here.

## Registry

| ID | Statement | Source |
|---|---|---|
| `ARCH-001` | The existing v0.4 WPF application remains completely frozen. No v0.5 refactor or feature work touches it; critical bug fixes are allowed only when separately authorized. | [#17](https://github.com/shoemin/Palworld-Server-Manager/issues/17), [#16](https://github.com/shoemin/Palworld-Server-Manager/issues/16), `v0.5-architecture.md` §1 (`Core`'s bounded legacy carve-out) |
| `ARCH-002` | `PalworldServerManager.Lan` remains a legacy v0.4 compatibility dependency for the frozen WPF application during v0.5. It is not renamed into or reused as the new `Contracts` assembly, and new v0.5 Host/Avalonia/CLI code does not depend on it. | `v0.5-architecture.md` §1 |
| `HOST-001` | Exactly one authoritative machine-wide Manager Host exists per physical PC. | [#17](https://github.com/shoemin/Palworld-Server-Manager/issues/17), `v0.5-architecture.md` §2 |
| `HOST-002` | All normal server/process/filesystem/profile mutations execute through the authoritative Host — never directly by a client. | [#17](https://github.com/shoemin/Palworld-Server-Manager/issues/17), `v0.5-architecture.md` §2 |
| `PERSIST-001` | The Host is the sole *online* writer of authoritative Host state. The offline recovery CLI may write only under the same machine-wide exclusivity lock, and only while the Host is not running. | `v0.5-architecture.md` §5a |
| `CLIENT-001` | Every ordinary local client (`Client.Avalonia`; `Client.Cli` as of `CLIENT-003`) has no dependency path to `Core`, `Host`, `Host.Persistence`, or any Host-side `Platform.*` project. | `v0.5-architecture.md` §1 |
| `CLIENT-002` | No ordinary local client (`Client.Avalonia`; `Client.Cli` as of `CLIENT-003`) ever possesses the machine Host's private identity credential. | `v0.5-architecture.md` §4c |
| `CLIENT-003` | Ordinary local CLI and Avalonia clients authenticate through the same server-authenticated local channel and per-user `LocalPrincipal` credential boundary. Privileged `Host.Cli` is offline bootstrap/recovery only and never substitutes for ordinary local-client authorization. | `v0.5-architecture.md` §1, §2c, §5a |
| `IDENT-001` | A stable, semantic Host identity (`HostId`) is separate from its rotatable authentication credential. | `v0.5-architecture.md` §4a |
| `IDENT-002` | Every server identity that crosses a Host boundary is collision-safe and Host-qualified: conceptually `ServerRef(AuthoritativeHostId, ServerProfileId)`, never a bare, unqualified server ID. | [#17](https://github.com/shoemin/Palworld-Server-Manager/issues/17) §11, `v0.5-architecture.md` §4d |
| `IDENT-003` | Routine Host-credential rotation is staged. A new credential cannot become current until every remaining Active peer has durably accepted it, or an Owner explicitly revokes/excludes peers that have not. Peers accept old and new credentials during the bounded transition; routine rotation never silently strands an Active trusted peer. | `v0.5-architecture.md` §4a-i |
| `IDENT-004` | A suspected-compromised Host credential can never authorize or authenticate its own replacement. Recovery requires the privileged offline Host recovery path to establish a fresh local trust anchor; every existing remote relationship resumes only through fresh pairing and peer-local Owner-approved credential replacement. | `v0.5-architecture.md` §3b, §4a-i step 7 |
| `LOCAL-001` | Each authorized local OS user maps to a distinct, persisted `LocalPrincipal`. Every *initialized* Host has exactly one active Owner. Before initialization completes, the Host is `Uninitialized` and exposes no ordinary authority surface — only the privileged bootstrap-completion path is permitted. | `v0.5-architecture.md` §3a, §2b, §2c |
| `LOCAL-002` | A local principal's private credential is held client-side, by that user; the Host stores only the verifier/public side. | `v0.5-architecture.md` §3a |
| `LOCAL-003` | OS transport/group eligibility never authorizes `LocalPrincipal` enrollment or re-enrollment. Non-Owner enrollment/reactivation requires explicit Host-side Owner authorization, and revocation remains durable until explicitly reauthorized. | `v0.5-architecture.md` §3a |
| `LOCAL-004` | Every local client authenticates the authoritative Host and uses an integrity-protected local channel before transmitting credentials, enrollment/bootstrap secrets, authentication signatures, or operational RPCs. | `v0.5-architecture.md` §3b |
| `OWNER-001` | Remote actors and ordinary local operations cannot revoke, demote, or replace the Owner. OS Administrator/root has only the explicit, exceptional offline recovery path. | `v0.5-architecture.md` §2b |
| `OWNER-002` | Initial Owner establishment requires explicit Administrator/root-authorized machine bootstrap bound to the intended OS principal. OS-group membership, transport access, or first connection can never claim Owner authority. | `v0.5-architecture.md` §2c |
| `REMOTE-001` | Outbound administration routes `Avalonia -> local Host -> remote Host`. `Client.Avalonia` never connects to a remote Host as the machine identity directly. | `v0.5-architecture.md` §4c |
| `REMOTE-002` | Outbound remote administration requires **both** local-principal authorization and independent remote `PeerHostId` authorization. A remote grant is a machine-level ceiling, not automatic authority for every local user. | `v0.5-architecture.md` §4c |
| `PAIR-001` | Pairing/trust and authorization are separate concerns. Being paired grants no server/admin authority by itself. | [#17](https://github.com/shoemin/Palworld-Server-Manager/issues/17), `v0.5-architecture.md` §5 |
| `PAIR-002` | A `PeerBound` trust state permits only pairing-finalization/activation-recovery traffic on that relationship — not ordinary management. Ordinary management requires `Active` trust. | `v0.5-architecture.md` §4b, §4c |
| `PAIR-003` | Pairing and revocation logic never assumes an atomic transaction spanning two Hosts. Each Host commits only its own local state. | `v0.5-architecture.md` §4b, §8 |
| `PAIR-004` | After proof of the currently trusted Host credential is lost, a newly paired credential claiming an existing `HostId` cannot replace trust or inherit authority automatically. Explicit local-Owner-approved credential replacement is required, and prior grants never silently reactivate. | `v0.5-architecture.md` §4b-i |
| `AUTH-001` | Host-capability and server-capability grants are type/scope valid by construction. A Host-level capability can never become a server-scoped grant. | `v0.5-architecture.md` §5 |
| `AUTH-002` | Delegation authority is explicit. Every non-root delegated grant records exact provenance (`DerivedFromGrantId`), and downstream validity follows the single-parent grant forest — never a multi-parent DAG unless that's a deliberate future design change. | `v0.5-architecture.md` §5 |
| `AUTH-003` | No indirect path — role preset, default template, migration, Owner adoption/re-home, remote-creator auto-grant, or any other UI convenience — may manufacture authority that bypasses the canonical grant-issuance rules. | `v0.5-architecture.md` §5 (`DefaultGrantTemplate`/role-preset fix) |
| `AUTH-004` | Shipped factory pairing defaults are least-privilege. A Host's *currently configured* defaults may differ from the shipped factory defaults, and changing them never retroactively rewrites grants already issued. | `v0.5-architecture.md` §5, §4b-i |
| `AUTH-005` | Every `HostCapabilityGrant` targets exactly one authoritative `HostId`. A Host-level authorization check must match that `HostId` exactly, and delegated `HostCapabilityGrant`s cannot change the target `HostId`. | `v0.5-architecture.md` §5, §4c |
| `PROTO-001` | Protocol compatibility uses explicit version/capability negotiation, never app-version-string comparison. An unrecognized authorization-relevant enum value is denied by default, never permitted by default. | `v0.5-architecture.md` §13 |
| `OPS-001` | A stale write to an editable resource is rejected via a revision/version check — never silently merged or overwritten. | `v0.5-architecture.md` §9 |
| `OPS-002` | Incompatible or destructive work acquires a Host-owned operation lock, visible to every connected client, not just the initiator. | `v0.5-architecture.md` §9 |
| `OPS-003` | A long-running operation belongs to its own Host-owned operation identity, not to the client connection that started it. | `v0.5-architecture.md` §9 |
| `OPS-004` | Durable operations have an explicit Host or Server target, and exclusive operation locks have an explicit Host or Server scope. A Host-scoped exclusive lock conflicts with every server-scoped exclusive lock on that Host; a server-scoped lock can never bypass an active Host-scoped lock. | `v0.5-architecture.md` §9, §10 |
| `RECOVERY-001` | An operation interrupted by a crash is classified explicitly (safe-to-retry / safe-to-resume / requires-manual-review / safe-to-discard) — destructive work is never blindly resumed by default. | `v0.5-architecture.md` §10 |
| `SEC-001` | Secret material is protected at rest per the accepted platform threat model for its OS, and never appears in logs, audit records, or diagnostic bundles. | `v0.5-architecture.md` §7 |
| `MIG-001` | v0.4 → v0.5 migration is non-destructive copy-and-validate. Source v0.4 state is left untouched, and multiple OS users' independent v0.4 stores are never silently merged. | [#17](https://github.com/shoemin/Palworld-Server-Manager/issues/17) |
| `PLATFORM-001` | OS-specific behavior lives behind explicit platform seams and a single composition root per executable — never scattered `OperatingSystem.IsWindows()` branches through shared code. | `v0.5-architecture.md` §1, §11 |
| `PLATFORM-002` | Interactive shell/UI actions execute in an interactive client session, never inside the machine-wide Host service/headless daemon. Host may authorize and return only bounded resource information required for a local client action. | `v0.5-architecture.md` §11, §11a |
| `LINUX-001` | No Linux-specific production implementation begins before explicit Windows Avalonia/Host feature-parity acceptance. | [#17](https://github.com/shoemin/Palworld-Server-Manager/issues/17) Gate E, [#20](https://github.com/shoemin/Palworld-Server-Manager/issues/20)/[#21](https://github.com/shoemin/Palworld-Server-Manager/issues/21) |

## Retired invariants

None yet.

## Change log

- 2026-08-31 — registry established ([#33](https://github.com/shoemin/Palworld-Server-Manager/issues/33)), sourced primarily from the accepted [#17](https://github.com/shoemin/Palworld-Server-Manager/issues/17) plan and the `docs/developer/v0.5-architecture.md` state as of [#19](https://github.com/shoemin/Palworld-Server-Manager/issues/19)'s fourth review round. `IDENT-002`, `PAIR-002`, and `AUTH-004` are recorded here in their intended final form even though the referenced architecture document has not yet been corrected to exactly match — that correction is #19's next authorized scope after this issue.
- 2026-08-31 — [#19](https://github.com/shoemin/Palworld-Server-Manager/issues/19)'s three remaining corrections landed: `v0.5-architecture.md` gained §4d defining `ServerRef`, closing `IDENT-002`; §4c now explicitly gates the dual-authorization invariant on `Active` (not merely `PeerBound`) trust, closing `PAIR-002`; §4a/§5/§8 now consistently distinguish the shipped factory `DefaultGrantTemplate` from a Host's currently configured one, closing `AUTH-004`. All three "pending" statuses removed — no invariant statement changed substance, only the architecture document was corrected to match.
- 2026-09-01 — first Codex review of #19's PR ([#57](https://github.com/shoemin/Palworld-Server-Manager/pull/57)) found four pre-existing gaps outside those three corrections; product owner + ChatGPT accepted all four as a Product Decision and authorized bounded fixes. Three new invariants added: `LOCAL-003` (durable, OS-group-independent `LocalPrincipal` revocation; Owner-authorized enrollment/reactivation via `PendingLocalPrincipalEnrollment`, §3a), `PAIR-004` (credential-loss trust replacement via an Owner-approved `PendingCredentialReplacement`, §4b-i — this is also where "Owner adoption/re-home" gets a concrete definition), `PLATFORM-002` (interactive shell actions are client-side only, §11/§11a — Host-side `IShellIntegration` removed). A fourth finding (the frozen-WPF app's existing dependency on `Core`'s Velopack integration, contradicting a "BCL only" claim) needed no new invariant — `ARCH-001` already governs; `v0.5-architecture.md` §1 documents the bounded legacy carve-out, no production source changed.
- 2026-09-01 — second Codex review of PR #57 found two gaps in the just-landed `LOCAL-003`/`PAIR-004` mechanisms themselves (fixed inline, no new invariants — an `EnrollmentCode` binding for `PendingLocalPrincipalEnrollment`, and a durable `Revoked` tombstone state for `TrustedManagers` instead of row deletion) and one gap in already-accepted, unrelated §5 architecture: `HostCapabilityGrant` had no destination/`PeerHostId` scope. The latter was escalated and accepted as a Product Decision. New invariant `AUTH-005` added: every `HostCapabilityGrant` now targets exactly one `TargetHostId`, checked exactly on both the local-authorization and remote-authorization sides of §4c's outbound dual-authorization model, with matching delegation/role-preset/`DefaultGrantTemplate` updates. `AUTH-001` unweakened — it still governs type/scope validity between Host- and server-capabilities; `AUTH-005` adds the missing Host-identity scope.
- 2026-09-01 — third Codex review of PR #57 found two more gaps in mechanisms just built this session (fixed inline, no new invariants — a `PendingCredentialReplacement` trigger corrected to exempt a symmetric-pairing partner's unchanged credential, and `EnrollmentCode` moved to verifier-only persistence) and two gaps in already-accepted, unrelated architecture, both escalated and accepted as a Product Decision. Two new invariants added: `LOCAL-004` (local IPC gains server authentication via `LocalHostTrustDescriptor` and standard TLS before any secret or ordinary RPC crosses it, §3b — closing a named-pipe/Unix-socket-squatting exposure), `OWNER-002` (initial Owner establishment requires a privileged, `OsPrincipalRef`-bound `Host.Cli` bootstrap with a single-use `OwnerBootstrapSecret`, never mere OS-group membership, §2c). `LOCAL-001` clarified, not weakened, to state the one-Owner rule applies to an *initialized* Host, with an explicit `Uninitialized` pre-bootstrap state that exposes no ordinary authority.
- 2026-09-01 — fourth Codex review of PR #57 found two more gaps in mechanisms just built this session (fixed inline, no new invariants — `PAIR-004`'s unchanged-credential reconfirmation exemption in §4b-i restricted to `State == Active` rows specifically, with a matching-credential `PeerBound` row now correctly routed through §4b's existing crash/reconnect resumption path instead of being treated as reconfirmed `Active` trust; and `LOCAL-004`'s §3b credential-recovery claim — which cited a §2b operation that does not and was never meant to cover the Host's own machine credential — replaced with an actual, newly defined offline Host machine-credential-loss recovery operation, using the identical `Host.Cli` bootstrap/recovery privilege model §2b/§2c already established, that regenerates `HostIdentity.CurrentCredentialRef` and republishes `LocalHostTrustDescriptor` together, explicitly not claiming to restore already-paired remote peers, which independently re-pair through the existing §4a/§4b-i path) and one gap in already-accepted, unrelated §4a architecture (a Host with multiple paired peers rotating its single global mTLS credential has no staged/overlap mechanism, so an offline-at-rotation-time peer is stranded regardless of whether the Host switches immediately or waits) — escalated as a Product Decision, not yet resolved; no new invariant added pending that decision.
- 2026-09-01 — the escalated staged-rotation finding was accepted as a Product Decision and resolved. New invariant `IDENT-003` added: routine Host-credential rotation is staged across every `Active` peer (§4a-i) — a `HostCredentialRotation` proposes the new credential under the old, still-current one; each peer durably stages the new fingerprint as `PendingTrustedPublicKeyFingerprint` while continuing to accept the old one; the Host cuts over only once every remaining `Active` peer has staged (an offline peer either is waited for or is explicitly, audibly revoked by the Owner — never auto-revoked on a timeout); `LocalHostTrustDescriptor` (`LOCAL-004`) stages the same way for local clients. A separate, explicitly non-staged Owner-authorized emergency-compromise-replacement path exists for a suspected-compromised (not merely offline-inconvenient) old credential, which may strand an unreachable peer by design, requiring that peer to recover via the existing `PAIR-004`/§4b-i `PendingCredentialReplacement` flow — routine rotation, suspected compromise, and credential loss remain three distinct, non-interchangeable mechanisms. No `CapabilityGrant` changes at any step of routine rotation. `IDENT-001`, `PAIR-001`–`PAIR-004`, `LOCAL-004`, `AUTH-003`, and `SEC-001` are unweakened by this addition.
- 2026-09-01 — fifth Codex review of PR #57 found five findings: three were bugs in mechanisms built this session (fixed inline, no new invariants — a `FailedAttempts` rate limit added to `PendingLocalPrincipalEnrollment`'s `EnrollmentCode`, mirroring §4b's existing ten-attempt pairing-code lockout; `IDENT-003`'s §4a-i rotation corrected to dynamically add and stage a peer that reaches `Active` mid-rotation instead of freezing the peer set at step 1; `IDENT-003`'s cutover corrected to durably promote `HostIdentity.CurrentCredentialRef` before any old-key retirement, instead of leaving it stale) and two gaps in already-accepted, unrelated §1 architecture, both escalated and accepted as a Product Decision. Two new invariants added: `ARCH-002` (legacy `PalworldServerManager.Lan` is retained unchanged for the frozen WPF app; the new `Contracts` assembly is not a rename of it, and no v0.5 project depends on `Lan`, §1 — closing a gap where the original "`Contracts` is renamed/evolved from `Lan`" claim was unimplementable while `ARCH-001` keeps the WPF app frozen, since the frozen app's own `AppServices.cs` directly consumes `Lan` types), `CLIENT-003` (a new `PalworldServerManager.Client.Cli` project is the ordinary online command-line client, structurally a `Client.Avalonia` peer sharing the same `Client.Platform.Contracts` tree and `LocalPrincipal` credential model; `Host.Cli` loses its former "normal operation" mode entirely and becomes privileged offline bootstrap/recovery only, never an ordinary authenticated client — closing a gap where `Host.Cli`'s normal-mode dependency set had no path to a `LocalPrincipal` credential at all, so it could never actually satisfy §3a's local-IPC authentication requirement). `CLIENT-001`/`CLIENT-002` clarified, not weakened, to state their existing rules apply to every ordinary local client (`Client.Avalonia` and, now, `Client.Cli`), not `Client.Avalonia` alone.
- 2026-09-01/02 — sixth through fourteenth Codex reviews of PR #57 (nine further rounds) found only bugs in mechanisms built earlier this same session — all fixed inline, no new invariants, and none weakening `IDENT-003`/`PAIR-004`/`LOCAL-003`/`LOCAL-004`/`OWNER-002`: server-side `EnrollmentCode` brute-force backoff; §4a-i cutover-eligibility corrected to re-check every peer's acknowledgment as currently unexpired at the moment cutover is attempted, not merely once-received; the emergency compromise-replacement path's forged-staging-message rejection tightened; two-phase Owner credential-loss (`Rotate`) and re-home (`Re-home`) flows defined (`Host.Cli` offline-prepares, `Client.Avalonia`/`Client.Cli` complete online), then corrected for a single-user auth gap and for a re-home leaving the displaced principal's old grants un-cascaded (fixed via `LOCAL-003`'s existing procedure); step 6's bounded-acceptance-of-`Old` rule corrected back to indefinite — a prior round's "stop accepting `Old` past the staging deadline" was itself an availability bug severing an ordinary, merely-slow, unrotated peer from its own still-`Old`-presenting Host for no security reason, when only a *bare re-proposal's* ability to manufacture new `New`-trust actually needed bounding; cross-store cutover atomicity fixed so `HostIdentity.CurrentCredentialRef`/`HostCredentialRotation.State` commit together in one SQLite transaction, with `LocalHostTrustDescriptor`'s republish as a separate following step and §3b's startup reconciliation extended to cover a crash between the two (`Current` and, separately, abandoned `Pending*`); a new grant-preserving `PendingReconfirmationRequired` gate added — distinct from and narrower than `PAIR-004`'s grant-invalidating gate — for a peer whose staged fingerprint it already cryptographically verified before its own deadline lapsed, so routine rotation's "no `CapabilityGrant` ever changes" promise holds even on a lapse; a required minimum cutover margin added (a peer within that margin of expiring is treated as not-yet-eligible, not merely checked for a future timestamp); Abort extended to clear `LocalHostTrustDescriptor.Pending*` and retire the abandoned `NewCredentialRef` in the same step; idempotent-retry (a `Result*Id` field returned again, unchanged, on a matching-secret replay) added to all four one-time-secret-then-respond mechanisms (`PendingOwnerEnrollment`, `PendingOwnerCredentialRotation`, `PendingOwnerRehome`, `PendingLocalPrincipalEnrollment`); and a scoping bug where §2c's post-initialization lockout blocked that same bootstrap retry path, fixed alongside a remaining contradiction between the Abort paragraph and step 6 (a lapsed peer's `Pending*` is retained with `PendingReconfirmationRequired = true`, never silently cleared merely by the deadline itself, until an Abort notice or a live reconfirmation check actually resolves it).
- 2026-09-02 — fifteenth Codex review of PR #57 found three more bugs, all inside `IDENT-003`/§4b mechanisms built this session — fixed inline, no new invariants. (1) Step 6's Owner-reconfirmation-approval path (added the prior round) promoted a lapsed peer to `CurrentTrustedPublicKeyFingerprint` whenever the Host confirmed the `RotationId` as `Staging`/`ReadyForCutover`/`CutOver` alike — but promoting while the Host is still mid-`Staging` (not yet cut over, so the Host itself is still presenting `Old`) would make the peer stop accepting `Old` with nothing else to authenticate against, stranding exactly the peer this path exists to recover; corrected to promote only on `CutOver`/`Completed`, and to instead renew the peer's `PendingRotationExpiresUtc` (no promotion, `Pending*` otherwise unchanged) when the Host reports the rotation still genuinely in progress — re-satisfying step 3's cutover predicate without jumping the peer's trust ahead of the Host's own credential. (2) Testability item 34a asserted old-key retirement was unblocked once a peer "has neither promoted, had its deadline lapse, nor been revoked" — contradicting step 6's own retirement-gating text (a lapsed, `PendingReconfirmationRequired` peer still relies on `Old` and still blocks retirement); corrected to remove deadline-lapse as a qualifying condition. (3) §4b's `Expired`/`Failed` cleanup text said cleanup "removes the pending `TrustedManagers` row" unconditionally — but for a row that already reached `PeerBound` (durably recorded, per §8's tombstone-retention rule established round one of this session), deleting it would let a later same-`HostId` pairing attempt fall into the "no existing row" genuinely-new-peer branch instead of `PAIR-004`'s replacement gate; corrected so a `PeerBound` row that expires without reaching `Active` transitions to `State = Revoked` (the same tombstone an explicit Owner revocation already produces) rather than being deleted — a gap in §4b's own text that round one's tombstone-retention fix never propagated to. Testability items 9, 34a, and 34e updated to match.
- 2026-09-02 — sixteenth Codex review of PR #57 (against the fifteenth round's fix) found three more bugs, all inside mechanisms built this session — fixed inline, no new invariants. (1) Step 2's "a different-`RotationId` proposal always overwrites existing `Pending*`" rule applied even to a peer already sitting in `PendingReconfirmationRequired` — since staging is authenticated by nothing but possession of `Old`, this let an attacker holding a leaked `Old` bypass the very Owner-approval-plus-live-verification gate step 6 requires for a lapsed peer, simply by labeling the forged proposal with a fresh `RotationId` instead of reusing the stale one; corrected so the overwrite applies only to a peer still actively (non-lapsed) staging — a lapsed peer's `Pending*` now changes only through its own live-initiated reconfirmation exchange (step 6), which surfaces a genuinely different in-progress `RotationId` the same way it already surfaces `Aborted`. Abort's overwrite paragraph and item 34c updated to match the corrected timing. (2) The prior round's reconfirmation-approval fix described the peer connecting to the Host "authenticated under the still-trusted `Old`" — but once global cutover genuinely happens, the Host presents only `New`, and nothing established that a lapsed peer's retained (not-yet-promoted) `Pending` fingerprint remains valid for *live-connection* authentication (as opposed to merely being retained as a value) — leaving no way for a post-cutover reconfirmation attempt to ever reach the Host at all. Clarified: `PendingRotationExpiresUtc`/`PendingReconfirmationRequired` bound only whether a bare re-proposal can establish/refresh `Pending*` (fix 1 above), never whether an already-recorded `Pending` fingerprint keeps being recognized live alongside `Current` — which also means a peer in this state typically promotes automatically via the *ordinary* observation rule the moment it connects post-cutover for any reason, with the Owner-approval gate mainly load-bearing pre-cutover. Item 34e extended. (3) All four one-time-secret-then-respond idempotent-retry mechanisms (`PendingOwnerEnrollment` and, by cross-reference, `PendingOwnerCredentialRotation`/`PendingOwnerRehome`; `PendingLocalPrincipalEnrollment` directly) re-applied the *original* `ExpiresUtc` to the *retry* of an already-consumed record, not just to first-time consumption — since the underlying transaction can commit a moment before `ExpiresUtc` and a lost response's first retry can just as easily land a moment after it, this could reject a legitimate retry and strand the client exactly as the idempotent-retry fix (an earlier round) was meant to prevent; corrected so `ExpiresUtc` gates only first-time consumption, never the retry of an already-consumed record, which now succeeds at any later time. Testability items 17, 31, 38, 39 updated to match.
- 2026-09-02 — seventeenth Codex review of PR #57 found the same three sixteenth-round fixes still stated incorrectly in other restatements of the same rules elsewhere in the document, which the sixteenth round's fix had corrected only in the main §4a-i/§2c prose — fixed inline, no new invariants, all three a direct continuation of round sixteen's own fix rather than new findings: the `TrustedManagers` struct's field comments and the §6 persistence-table row for `TrustedManagers` still described `PendingReconfirmationRequired` as letting Owner approval "promote the already-verified fingerprint directly," and `PendingRotationExpiresUtc` as bounding dual old/new acceptance itself, rather than the corrected Case-1a/1b/live-verification behavior and the deadline bounding only bare-proposal establishment; step 2's own "the peer now accepts both... until that deadline" sentence had the identical staleness; and the §6 table rows for `PendingLocalPrincipalEnrollments`, `PendingOwnerEnrollments`, and `PendingOwnerCredentialRotations` still said their idempotent retries were "bounded by `ExpiresUtc`," unchanged from before round sixteen's fix to the corresponding prose. All four corrected to match their already-accepted prose fixes; the `TrustedManagers` table row also picked up round fifteen's PeerBound-tombstone note, which had likewise never been propagated to it.
- 2026-09-02 — eighteenth Codex review of PR #57 found one finding, outside `IDENT-003`/rotation entirely: §5's "five-part predicate, and only that" sentence, read literally, requires an issuing actor to already hold a source grant `G` — but the Owner's own authority is never itself a grant (§3a), so a literal reading leaves the Owner unable to issue the very first grant to anyone, breaking non-Owner enrollment, Owner adoption/re-home's permission reissuance, and Owner-applied role presets at their first step — a direct textual contradiction with the same section's own "Owner root-grant representation" paragraph, which already (and correctly) describes the Owner creating root grants directly from `IsOwner`. Fixed inline, no new invariants: this session's own `AUTH-005` commit (round two) directly edited the exact "Owner root-grant representation" sentence this contradicts, so the gap sits inside text this session actively touched, not architecture reopened from scratch. Clarified that the five-part predicate governs delegated ("downstream," non-null `DerivedFromGrantId`) grants specifically and never gates root-grant creation, which is unconditional `IsOwner` authority; extended the same clarification to role-preset application (a non-Owner applier still runs every entry through the predicate against their own grants; an Owner applier creates ordinary root grants instead) and to testability item 2. `AUTH-002`'s registry statement ("every *non-root* delegated grant records exact provenance") already implied exactly this distinction and required no change — only the architecture document's prose was out of step with it.
- 2026-09-02 — nineteenth Codex review of PR #57 found two more bugs in `IDENT-003` mechanisms built this session — fixed inline, no new invariants. (1) Round sixteen's fix let a lapsed peer's live reconfirmation exchange (step 6) also discover and stage a genuinely different in-progress `RotationId` from the same response — but that exchange authenticates under nothing but `Old`, exactly like the bare proposal message the overwrite restriction was built to stop trusting; an attacker holding a leaked `Old` could answer the check, report the original `RotationId` `Aborted`, and simultaneously advertise a fresh attacker-controlled `RotationId` that the peer would then stage and later promote on observation — recreating the identical forgery the overwrite fix had just closed, just relocated into the live check's own response. Corrected so the live check resolves only the specific `RotationId` it asked about (promote, renew, or clear); a genuinely new rotation reaches the peer only through its own later, separate step 2 proposal, accepted the ordinary way once the peer has no `Pending*` left to protect. Step 2's overwrite paragraph, the Abort paragraph, and testability items 34c/34e updated to match. (2) Step 6's opening paragraph still contained the pre-round-fourteen sentence "continues accepting `Old` for ordinary authentication only until its own `PendingRotationExpiresUtc` deadline... not indefinitely" — directly contradicted by round fourteen's own correction two sentences later in the same paragraph ("`Old` remains fully valid... indefinitely"), which had added the fix without ever removing the sentence it was fixing. Corrected the opening sentence to state the deadline bounds only the pending state's own re-establishability, matching the paragraph's own later (and correct) text.
- 2026-09-02 — twentieth Codex review of PR #57 found two more bugs, both in the reconfirmation gate round nineteen had just corrected — fixed inline, no new invariants. (1) Round nineteen narrowed the live check to resolving only the specific `RotationId` asked about, but left "Case 1a" (Host's *message* claims `CutOver`/`Completed` → promote) intact — this check is authenticated under `Old` (the peer hasn't promoted yet), and an attacker holding only a leaked `Old` can answer it and falsely claim `CutOver` exactly as convincingly as the real Host, causing the peer to promote to `New`, stop accepting the real Host's still-current `Old`, and become permanently unreachable — the identical class of forgery being fought all along, just moved into the one case the previous two rounds' fixes hadn't touched. Removed "Case 1a" entirely: since a genuinely cut-over Host presents only `New` from that point on (step 5), a peer's own reconfirmation connection would itself already authenticate under `New` in that case, triggering ordinary, unconditional observation-based promotion (step 6's first paragraph) independent of this gate — so nothing is lost. The `Old`-authenticated live check now resolves only to Renew (`Staging`/`ReadyForCutover`) or Clear (`Aborted`/unknown), never Promote; struct comments, the section 6 table row, and testability items 34a/34e updated to match. (2) Separately: the Abort flow's "abandoned `NewCredentialRef` private key material is retired... immediately" was never explicitly guaranteed atomic with `HostCredentialRotation.State = Aborted` becoming durable — a crash mid-retirement could in principle leave the key still reachable while `Aborted` is already durable, and ordinary observation's unconditional promote-on-presented-credential rule would trust a live connection presenting it, letting anyone who obtained it inherit the relationship's grants even though the Host never cut over and considers the key abandoned. Fixed by extending §3b's general startup-reconciliation pass (already used for every other crash window in this document) to also verify/complete this retirement before serving any connection; testability item 33b extended to assert it directly.
- 2026-09-02 — twenty-first Codex review of PR #57 found two more bugs — fixed inline, no new invariants. (1) `ILocalPrincipalCredentialStore` (§3a/§11, `CLIENT-003`) was missing any method to persist or reload the `LocalPrincipalId` half of `LocalPrincipalClientCredential` — `CreateAndStoreAsync` necessarily runs before Host has assigned an ID, and the prior `LoadAsync` returned only a bare private key, leaving the "the client persists this binding" prose paragraph, and testability items 16/37 which already assumed a full round-trip, with no interface method that could ever actually do it: after the completing process exited, a later invocation could load a key but had no way to know which principal to authenticate as. Added `BindPrincipalIdAsync` (persists the ID once Host assigns it) and changed `LoadAsync` to return the complete `LocalPrincipalClientCredential?`; `HasCredentialAsync` now means both halves are present. (2) A self-inflicted regression from round nineteen's own fix to this same paragraph: correcting the false "Old is accepted only until the deadline" claim replaced it with an equally false new claim, "Pending* is retained only until the deadline" — directly contradicting round thirteen/fourteen's own established (and, elsewhere in this same document, correctly stated) rule that Pending* survives the deadline with `PendingReconfirmationRequired = true`, never simply discarded. Corrected to state Pending* is retained past the deadline, matching every other reference to this rule already in the document — a reminder that a fix to one false claim in this paragraph must be checked against the paragraph's own other claims, not just against the one sentence being replaced.
- 2026-09-02 — twenty-second Codex review of PR #57 found two more bugs, both self-inflicted by the immediately preceding round's own fixes — fixed inline, no new invariants. (1) Round twenty-one's new `BindPrincipalIdAsync`/`LoadAsync` correctly made an unbound stored key report `HasCredentialAsync == false` — but `CreateAndStoreAsync` still always generated a *fresh* keypair, so a client that lost its completion response, restarted, and retried from that false-`false` state would silently discard its original key, generate a new one, and bind an already-known `LocalPrincipalId` (returned idempotently by Host without the underlying public key ever changing) to a private key the Host's stored `PublicVerificationKey` will never match — permanently locking the client out of its own restored credential. Fixed: `CreateAndStoreAsync`, called while a key is stored but not yet bound, returns that same stored keypair instead of generating a new one; a fresh keypair is generated only when none is stored at all or after `DeleteAsync`. (2) Round twenty's fix to the Abort key-retirement guarantee overclaimed: "no peer can ever observe a live connection presenting an abandoned New credential once abort is durable, by construction" — true only of the Host's *own* copy; deleting it cannot un-leak a copy an attacker already obtained before retirement had a chance to run. Corrected the claim's scope and added the actual remedy for that narrower, genuine risk: an abandoned New suspected of exposure before retirement is the same class of problem step 7 already solves for a suspected-compromised Old, just for a staged-and-abandoned credential and narrower in scope — the Owner explicitly revokes (§8) every peer that had staged that specific RotationId, which clears their `Pending*` immediately via §8's existing revocation rule and forces re-establishment through `PAIR-004`'s `PendingCredentialReplacement` gate, rather than trusting Abort's ordinary notice/reconfirmation path to resolve a suspected-compromise case on its own timeline.
- 2026-09-02 — twenty-third Codex review of PR #57 found three findings — fixed inline, no new invariants. (1) The newly-activated-peer rule ("whenever a pairing reaches Active while HostCredentialRotation is in a non-terminal state...") could be read to include `CutOver` as non-terminal (only `Completed`/`Aborted` are actually terminal), staging a peer that pairs *after* cutover against `Old` even though it already pins `New` from ordinary pairing at that point, and would fail its own old-credential-match check. Restricted the rule to `Prepared`/`Staging`/`ReadyForCutover`; a peer activating at `CutOver` or later pairs directly against the Host's current credential and needs no staging at all. (2) A genuine, previously-undefined gap: step 6 requires the Host to know, per peer, that promotion has happened before retiring `OldCredentialRef`, but promotion is an entirely local event on the peer's own machine that the Host has no way to observe server-side — no acknowledgment message, retry behavior, or durable per-peer receipt was ever defined for the Host side, only for the peer side. Defined an idempotent, authenticated promotion acknowledgment sent back over the same connection where the peer observes and promotes, durably recorded by the Host and retried opportunistically on the peer's next contact if the first attempt's delivery is lost, mirroring step 2's staging-acknowledgment pattern; testability item 34a extended to assert retirement waits for the Host's durable receipt, not merely the peer's own local state. (3) The exact same "fixed one sentence, left an earlier contradicting sentence in the same paragraph" mistake from round twenty-one recurred in a different paragraph: §2c's bootstrap-retry fix's own "Fixed:" sentence, describing the *original* round-eleven idempotent-retry mechanism, still said the retry required "the record's ExpiresUtc window hasn't lapsed" — directly contradicted by the very next sentence's already-correct "no further ExpiresUtc check" fix from an earlier round. Removed the stale condition; swept all four retry mechanisms (bootstrap, Rotate, Re-home, ordinary enrollment) for the same pattern and found no further instances.
- 2026-09-02 — twenty-fourth Codex review of PR #57 found two more bugs — fixed inline, no new invariants. (1) Owner re-home's phase-2 transaction (§2b) never actually registered the submitted public key as `PublicVerificationKey` on the new-or-located row — only `IsOwner`/`State` were listed as set. The single-user case (a brand-new row) would end up with no key registered at all; a re-home target that happens to be a retained `Revoked` tombstone would find its key already cleared by that earlier revocation. Either way the freshly re-homed Owner would be unable to authenticate with the exact key it just submitted. Added `PublicVerificationKey` to the same transaction; testability item 39 extended to assert it directly, including the revoked-tombstone-target sub-case. (2) A genuine gap in round twenty-three's own new promotion-acknowledgment mechanism: promotion clears `PendingRotationId` as one of the "`Pending*` fields," but the retry this mechanism promised for a lost acknowledgment has nothing durable to retry from after a peer *process restart* — `CredentialHistory` records only fingerprints and timestamps, never a `RotationId`. Fixed: `PendingRotationId` is no longer cleared at promotion — only `PendingTrustedPublicKeyFingerprint` (now redundant) is; `PendingRotationId` is retained, repurposed to track the still-unacknowledged rotation, until the Host's acknowledgment-receipt is durably confirmed, surviving a restart the same way every other durable pending-state field in this document already does. Struct comments and the section 6 table row updated to reflect that `PendingRotationId` alone can be non-null post-promotion; testability item 34a extended with the restart case.
- 2026-09-02 — twenty-fifth Codex review of PR #57 found two more genuine race-condition bugs, both concerning two concurrently-outstanding pending records for the same target — fixed inline, no new invariants. (1) Owner re-home's `PendingOwnerRehome` had no binding to which Owner it was authorized to displace: if `Host.Cli` prepared a ticket for X, then a second for Y, before either was consumed, and Y's completed first (revoking the prior Owner, installing Y), X's still-valid original ticket could later complete too — finding "the current `IsOwner = true` row" is now Y, not who held the role when X's ticket was created — and would revoke the legitimately-installed Y, silently overriding a later Administrator decision with a stale one. Fixed: `PendingOwnerRehome` now captures `ExpectedCurrentOwnerLocalPrincipalId` at creation; first-time consumption verifies the current Owner still matches before proceeding, rejecting a stale ticket exactly like a wrong secret. (2) `PendingLocalPrincipalEnrollment` had no protection against two concurrently-outstanding `ExistingLocalPrincipalId = null` ("create new") tickets for the same `OsPrincipalRef` — the Owner creating a second one before the first was consumed (e.g. believing the first code was lost) let each independently create its own `LocalPrincipal` row, producing two rows for one OS user, violating `LOCAL-001`'s one-principal-per-user mapping and defeating later revocation (revoking only the row the Owner remembered leaves the duplicate's grants silently active). Fixed: consumption of an `ExistingLocalPrincipalId = null` record now first verifies no row already exists for that `OsPrincipalRef`, atomically with creation, rejecting a second such record's consumption exactly like a wrong code rather than creating a duplicate. Testability items 17 and 39 extended for both cases.
- 2026-09-02 — twenty-sixth Codex review of PR #57 found the exact same concurrent-stale-ticket bug class in the two sibling sub-cases round twenty-five's fix hadn't covered — fixed inline, no new invariants, both closing the same enumerated set of one-time-secret-then-respond mechanisms this round completes. (1) `PendingOwnerCredentialRotation` (Rotate) had the identical gap as Re-home: two tickets for the same Owner, older one completing after a newer one already installed a different key, would silently revert it — potentially locking the sole Owner out of the key they're actively using. Fixed the same way: captures `ExpectedCurrentPublicVerificationKey` at creation; first-time consumption verifies the Owner row's current key still matches before proceeding. (2) `PendingLocalPrincipalEnrollment`'s reactivation branch (`ExistingLocalPrincipalId` set) had the sibling gap to round twenty-five's create-new fix: two reactivation tickets for the same `Revoked` row, the older one's later consumption re-registering a stale key over an already-reactivated principal and inheriting whatever grants accumulated since. Fixed: first-time consumption of a reactivation record now verifies the target row is still `Revoked` before proceeding. Confirmed `PendingOwnerEnrollment` (bootstrap) needs no equivalent fix — its own already-documented `HostBootstrapState = Initialized` gate structurally blocks *every* other unconsumed ticket's completion the moment any one succeeds, a categorically different (and already-sufficient) protection than the others' "does the target still match what I expected" check. This is the last of the four sub-cases (Rotate, Re-home, enrollment-create, enrollment-reactivate) sharing this race; testability items 38 and 17 extended.
- 2026-09-02 — twenty-seventh Codex review of PR #57 found one more bug in §3b's startup reconciliation — fixed inline, no new invariant. The rule that clears the local descriptor's abandoned `Pending*` fields after a crash enumerated `HostCredentialRotation.State = Aborted`/`Completed` as the "no rotation genuinely in progress" bucket, omitting `CutOver` — but a crash between step 5's SQLite transaction (`CurrentCredentialRef = New`, `State = CutOver`, committed) and its own descriptor republish (which normally clears `Pending*` in the same step as updating `Current`) lands exactly in that omitted state; the existing `Current`-mismatch check (item 33a) would still update `Current`, but `Pending*` would be left populated indefinitely, since nothing else in this design ever revisits a rotation once it reaches `CutOver`. Fixed by widening the bucket to include `CutOver`, in both the main §3b prose and testability item 33b, so this crash state is reconciled the identical way `Aborted`/`Completed` already are — `Pending*` cleared alongside the `Current` update, in the same startup pass.
- 2026-09-02 — twenty-eighth Codex review of PR #57 found two more bugs — fixed inline, no new invariants. (1) The same leftover-sentence pattern as several earlier rounds, this time in the Unresolved/deferred-decisions section: an entry describing why `PendingRotationExpiresUtc` must be bounded still framed the bound as being about "a peer's staged acceptance of `Old`" — the pre-round-fourteen understanding, corrected everywhere else in the document but never updated here. Corrected to say the deadline bounds only whether a bare re-proposal can establish/refresh `Pending*` trust, never the peer's indefinite acceptance of `Old` itself. (2) A genuine remaining gap in round twenty-five/twenty-six's own stale-ticket fix: `PendingOwnerRehome` bound only to the current Owner's *identity* (`ExpectedCurrentOwnerLocalPrincipalId`), not their *credential* — the same Owner can legitimately rotate their key (Rotate, item 38) without changing `LocalPrincipalId`, so a stale re-home ticket prepared before that rotation would still pass the identity check and revoke the just-recovered Owner anyway, undoing a rotation-based recovery decision the same way the identity check was meant to prevent for a re-home-based one. Fixed: `PendingOwnerRehome` now also captures `ExpectedCurrentOwnerPublicVerificationKey` at creation; first-time consumption verifies both fields still match. Testability item 39 extended with the cross-mechanism scenario (stale re-home ticket, intervening Rotate).
- 2026-09-02 — twenty-ninth Codex review of PR #57 found one more cross-mechanism gap — fixed inline, no new invariant. Host machine-credential-loss recovery (§3b) never addressed a `HostCredentialRotation` still `Staging`/`ReadyForCutover` at the moment it runs: `OldCredentialRef` — exactly the credential this recovery exists because is lost — is what routine rotation's staging proposals are signed with and what already-staged peers are durably waiting to see cut over from; installing a third, unrelated fresh credential without resolving that record would leave it dangling forever, with the general reconciliation rule (extended to `CutOver` two rounds earlier) repeatedly republishing the now-orphaned staged `New` fingerprint into the local descriptor's `Pending*` on every startup, since nothing else in this design ever revisits it. Fixed: this same recovery transaction now also marks any nonterminal `HostCredentialRotation` `Aborted`, retires its staged `NewCredentialRef` the same way an ordinary Abort's is, and clears `LocalHostTrustDescriptor.Pending*` in the same republish step as the fresh current fingerprint. No new peer-side mechanism was needed — every remote peer, staged or not, already required the ordinary credential-loss `PendingCredentialReplacement` re-pairing flow, since `Old` is lost regardless of staging status. Testability item 33 extended with this exact scenario.
- 2026-09-02 — thirtieth Codex review of PR #57 found three more bugs — fixed inline, no new invariants. (1) A repeat of round twenty-seven's own already-fixed mistake, this time in round twenty-nine's brand-new machine-credential-loss-supersedes-rotation fix, which listed only `Prepared`/`Staging`/`ReadyForCutover` and again omitted `CutOver` — the identical omission, one commit after the general reconciliation rule had already been corrected for it elsewhere. Widened to include `CutOver`; testability item 33 extended again. (2) A genuine race in `PAIR-004`'s original (round one) `PendingCredentialReplacement` approval flow, never previously examined this session: nothing prevented two unexpired candidates from coexisting for one `PeerHostId`, and approving one didn't invalidate the other — a later or delayed approval of the sibling would re-run the same replacement steps, discarding the grants accumulated since the first approval and reverting trust to the older, no-longer-current candidate. Fixed: approving one candidate now invalidates every other unapproved sibling for the same `PeerHostId` in the same transaction; testability item 18 extended. (3) A subtler version of the enrollment-reactivation race rounds twenty-five/twenty-six had already partly closed: their state-only check ("is the target still `Revoked`") passes whenever the row happens to currently be `Revoked` — but a principal can cycle through `Revoked`/`Active` more than once, so a ticket from an *earlier* cycle could still pass a *later*, unrelated revocation's state check, letting anyone who retained its code reactivate the principal unauthorized by anything issued after the most recent revocation, defeating `LOCAL-003`'s durable-revocation guarantee. Fixed: any revocation or reactivation of a `LocalPrincipal` row now proactively invalidates every other still-unconsumed `PendingLocalPrincipalEnrollment` targeting that row, at the moment of the state change, not merely re-checked reactively at each sibling's own later consumption; testability item 17 extended with the full-cycle scenario.
- 2026-09-02 — thirty-first Codex review of PR #57 found two more bugs, both extending the immediately preceding round's own fixes to a sibling case — fixed inline, no new invariants. (1) Step 7's emergency compromise-replacement path had the identical gap round twenty-nine/thirty had just closed for machine-credential-loss recovery: no answer for a routine rotation still `Prepared` through `CutOver` when compromise replacement fires, leaving that record referencing the now-irrelevant old/new pair while a fourth, unrelated emergency credential takes over — startup reconciliation would keep republishing the abandoned staged fingerprint, and surviving rotation-protocol code could try to resume staging with the compromised key. Fixed the identical way: this same operation now also marks any nonterminal `HostCredentialRotation` `Aborted`, retires its `NewCredentialRef`, and clears the local descriptor's `Pending*` in the same step as the fresh emergency fingerprint; testability item 34 extended. (2) `PAIR-004`'s sibling-candidate fix (round thirty) closed only the case where *another candidate's approval* changed the peer's trust state — but the same peer's trust can also change via an unrelated explicit Owner revocation, or via the legitimate peer completing an ordinary routine rotation, while a `PendingCredentialReplacement` candidate sits unapproved; a later, stale approval would then either resurrect a just-revoked relationship or overwrite a legitimately-rotated credential and reset its grants. Fixed: each candidate now captures `ExpectedTrustState`/`ExpectedCurrentTrustedPublicKeyFingerprint` at creation; approval verifies both still match before proceeding, atomically — a single check broad enough to subsume the sibling-candidate case too, though that explicit invalidation step is kept alongside it so an invalidated candidate stays permanently unusable even if state later happens to cycle back to a matching value. Testability item 18 extended with both new scenarios.
- 2026-09-02 — thirty-second Codex review of PR #57 found two more bugs, each a further gap in the immediately preceding round's own fixes — fixed inline, no new invariants. (1) `PendingCredentialReplacement`'s new `Expected*` freshness check (round thirty-one) covered `State` and `CurrentTrustedPublicKeyFingerprint` but not `PendingRotationId` — an in-progress *staged* routine rotation changes only `Pending*`, so a candidate created before that staging began would still pass approval, silently ignoring the legitimate staging acknowledgment: the stale `Pending*` could later auto-promote over the Owner-approved replacement via ordinary observation, inheriting its fresh grants, while the rotating Host cuts over expecting a peer that no longer matches. Added `ExpectedPendingRotationId` to the captured/verified triple; testability item 18 extended again. (2) Re-home's `Expected*` freshness check (rounds twenty-five/twenty-eight) covered only the *current Owner's* identity and key — but a re-home ticket targeting an already-enrolled principal X doesn't stop being consumable merely because X's own state changes (revoked, reactivated, or re-keyed) independently while the ticket sits unconsumed, since neither check touches when only X changes. A stale ticket could then reactivate a just-revoked X as the new Owner and revoke whoever revoked them, undoing that decision as completely as the current-Owner-side race the earlier fix closed. Added `ExpectedTargetLocalPrincipalId`/`ExpectedTargetState`/`ExpectedTargetPublicVerificationKey` (all null for the single-user, brand-new-account case), captured at creation and verified at first-time consumption alongside the existing current-Owner check; testability item 39 extended.
- 2026-09-02 — thirty-third Codex review of PR #57 found the general root cause behind five straight rounds of "add one more field to the snapshot" fixes: both round thirty-two fixes are the classic **ABA problem** — a snapshot-equality staleness check, however many fields it captures, cannot distinguish a genuinely-fresh pending record from a stale one whose target happened to cycle away from and back to a matching state (revoked, reactivated, revoked again with the same resulting tuple; staged, aborted, staged again under a coincidentally-matching value). No amount of additional captured fields closes this by construction. Fixed at the root rather than with a sixth field: added a general principle to §8 — any transition to a `LocalPrincipal` or `TrustedManagers` row's `State`, credential/key, or `PendingRotationId` (for any reason, including another pending record's own approval) proactively and durably invalidates every other outstanding pending record whose validity depends on that row's state (`PendingOwnerCredentialRotation`/`PendingOwnerRehome` targeting it, `PendingCredentialReplacement` candidates for it, `PendingLocalPrincipalEnrollment` reactivation tickets targeting it), in the same transaction as the transition — never revivable afterward regardless of what the row's state later cycles back to. The existing `Expected*` snapshot checks are retained as a cheap, redundant first pass (already correct in the overwhelmingly common case), with this proactive rule as what actually closes the gap; `PendingLocalPrincipalEnrollment`'s round-thirty fix already independently implemented this exact pattern and is now named as the same general principle rather than a one-off. Testability items 18 and 39 extended with the specific ABA scenario for each.
- 2026-09-02 — thirty-fourth Codex review of PR #57 found two findings. (1) Fixed inline, no new invariant: `PendingRotationId`'s freshness check (added round thirty-two) compared it for *equality* against the captured `ExpectedPendingRotationId` like the other two fields — but a candidate created *after* a peer already staged a rotation captures that rotation's real, non-null `RotationId` from the start, so equality would never flag it as stale, yet approving it still leaves an approved replacement sitting alongside an untouched, unresolved staged rotation whose key can later auto-promote over it. Equality is the wrong shape for this field: fixed by checking `PendingRotationId` for **absence** instead — approval is now rejected outright whenever the row has *any* staged, unresolved rotation, regardless of whether that staging predates the candidate or happened afterward, until the Owner resolves it. `ExpectedPendingRotationId` removed from the struct as now-unused; testability item 18 restructured to cover both orderings. (2) **Escalated, not fixed** — a genuine, deep gap in step 7's (emergency compromise replacement) execution model, predating this round's edits and going beyond a bounded bug fix: because step 7 is designed as "an ordinary Owner-authorized operation on a **running** Host," and because the local client authenticates that Host via `LocalHostTrustDescriptor`'s pinned fingerprint — which, at the moment compromise replacement is needed, is still the *compromised* `Old` — an attacker who already holds a copy of `Old` and can occupy the local IPC endpoint (particularly while the real Host is stopped or dormant) can impersonate the Host, be classified as authenticated by the client, and either intercept or falsely confirm the Owner's attempted recovery while the real credential remains compromised. Every candidate fix (routing recovery through the offline `Host.Cli` privileged path instead, or establishing the replacement trust anchor through an independent channel) changes step 7's fundamental privilege/execution model and its explicitly-stated distinction from the offline `Host.Cli` recovery operations — a product/architecture decision, not a bounded correction. Set #19 to Product Decision; PR #57 to draft; this finding's thread replied-to but left unresolved; no code or doc change made for it this round.
- 2026-09-02 — the escalated step-7 finding was accepted as a Product Decision and resolved. New invariant `IDENT-004` added: a suspected-compromised Host credential can never authorize or authenticate its own replacement. §4a-i step 7 no longer defines an online, Owner-authorized operation on a running Host; suspected compromise now uses the same privileged, offline `Host.Cli` recovery operation as Host machine-credential loss (§3b, renamed to cover both trigger reasons), as a second, distinctly-audited trigger for the identical mechanism — `Old` is never read, trusted, or used as proof, transport, or confirmation by any step, and is immediately invalid for local trust once recovery commits. `TrustedManagers` gains `PeerRecoveryRequired`, set on every currently-`Active` peer in the same recovery transaction (either trigger reason); it blocks ordinary outbound administration to that peer **locally** (§4c step 1, checked in addition to `Active` trust) until that peer independently completes fresh pairing and its own local Owner approves the resulting `PendingCredentialReplacement` (`PAIR-004`) — never cleared by mere reconnection or by this Host's own recovery transaction. Revocation (§8) clears `PeerRecoveryRequired` alongside its existing `Pending*` cascade. Recovery's structural inability to proactively warn already-paired peers over the now-suspect channel is documented as a bounded threat-model limitation, not silently assumed away. `IDENT-001`, `IDENT-003`, `PAIR-001`–`PAIR-004`, `LOCAL-004`, `OWNER-001`, `PERSIST-001`, `SEC-001` are unweakened. Testability items 33 (retitled to cover both trigger reasons) and 34 updated; new item 33c added for compromise-specific and `PeerRecoveryRequired`-lifecycle scenarios.
- 2026-09-02 — a Codex review of the `IDENT-004` fix (PR #57, HEAD `38221ad`) found one gap in this round's own new `PeerRecoveryRequired` mechanism — fixed inline, no new invariant. Approving a §4b-i `PendingCredentialReplacement` is a purely local transaction on the approving peer's own machine; the far Host (whose credential changed, which is why the candidate existed in the first place) has no way to observe that approval happened merely from a live connection succeeding, since an unchanged-credential exchange "explicitly touches nothing" on that side. Left as originally written, the far Host could never safely clear its own `PeerRecoveryRequired` for that relationship — clearing on connection alone would resume outbound administration before approval actually happened, while never clearing it would block the relationship forever once approval genuinely did. Fixed the identical way §4a-i step 6 already solves the analogous problem for rotation's promotion acknowledgment: §4b-i gains a new step 7 — the approving side sends a durable, idempotent, authenticated recovery-completion acknowledgment, retried opportunistically on any future contact (surviving a dropped connection or process restart) until the far Host durably records receipt; only then does `PeerRecoveryRequired` clear. Sent unconditionally after every approval, including when the far side has no `PeerRecoveryRequired` condition to clear (a harmless no-op). Testability items 18 and 33c extended.
- 2026-09-02 — a Codex review of PR #57 at HEAD `20d97e7` found two more gaps, both in mechanisms this session built or restated — fixed inline, no new invariants. (1) §3b's recovery-transaction bullet relied entirely on the general startup-reconciliation rule to close the gap between committing `HostIdentity.CurrentCredentialRef` and republishing `LocalHostTrustDescriptor` — but that rule runs only when something actually starts and performs it; if `Host.Cli` crashes between the two writes on a desktop installation where the real Host stays stopped and nothing reruns immediately, the descriptor keeps naming `Old` indefinitely, letting an attacker who already holds a copy of `Old` (suspected compromise specifically) squat the local endpoint and authenticate successfully against the still-stale descriptor. Fixed: `Host.Cli` no longer reports the operation complete, or releases the exclusivity lock, until the descriptor republish is also durably confirmed (narrowing the window to a genuine hard crash); every `Host.Cli` invocation additionally performs the same unconditional reconciliation check at its own start, self-healing a prior crashed invocation the moment the operator reruns it for any reason; the residual "operator does neither" window is explicitly disclosed as a bounded limitation, matching this round's existing threat-model-disclosure pattern. (2) §4a-i's Abort paragraph claimed the Owner explicitly revoking a peer that missed both the abort notice and staged-key exposure concern "clos[es] the window regardless of whether that peer ever receives the notice" — true only for §8's *online* two-sided revocation; the remedy exists specifically for an *offline* peer, where the Owner's revocation is necessarily §8's offline case and changes only this Host's own local row, never reaching the peer's own retained `Pending*` at all — an attacker with a copy of the abandoned key can still connect directly to that peer and trigger step 6's unconditional observation promotion regardless of what this Host's own row says. Corrected to state the actual remedy precisely (the peer's own eventual reconnection-and-reconfirmation, or a separate out-of-band action performed directly on the peer's own machine) rather than overclaiming immediate closure. Testability item 34c corrected to test both the online and offline cases separately.
- 2026-09-02 — a Codex review of PR #57 at HEAD `f2cdb64` found two more gaps in the round-thirty-five acknowledgment mechanism itself — fixed inline, no new invariants. (1) The acknowledgment's authentication instruction said "under this Host's own credential, just bound in step 3" — but step 3 binds the *far peer's* new candidate credential, not the approving side's own; the approving side never held that private key and cannot authenticate with it. Corrected: the acknowledgment is authenticated under the approving side's own, unchanged identity credential, which the far peer already trusts since this whole exchange never disturbed it. (2) The ABA problem, recurring in the brand-new mechanism this same round introduced: a bare Boolean "acknowledgment received" check let a *delayed* acknowledgment for an *earlier*, now-superseded recovery credential clear `PeerRecoveryRequired` set by a *later*, unrelated recovery — prematurely hiding a still-genuinely-pending recovery. Fixed using the general principle §8 already established for this exact class of bug: the acknowledgment carries the specific `CandidatePublicKeyFingerprint` approved (already an existing field), and `PeerRecoveryRequired` clears only if it exactly matches `HostIdentity.CurrentCredentialRef`'s fingerprint at receipt time, never merely because some acknowledgment arrived. Testability items 18 and 33c extended for both.
- 2026-09-02 — a Codex review of PR #57 at HEAD `dbe0c80` found two more gaps — fixed inline, no new invariants. (1) §4b-i step 0's approval check, added round thirty-four, tested `PendingRotationId` for absence — but §4a-i step 6 deliberately retains a non-null `PendingRotationId` on an already-*promoted* peer, purely to track a still-unconfirmed acknowledgment (`PendingTrustedPublicKeyFingerprint` is already null by then); a peer sitting in that ordinary, benign state would have every legitimate replacement approval wrongly rejected for a "staged rotation" that no longer exists, permanently stranding it if the Host it promoted to has since itself lost or compromised that very credential. Corrected the check to `PendingTrustedPublicKeyFingerprint` specifically — the field that actually represents a not-yet-promoted, auto-promotable credential, which is the only real risk this check exists to prevent — and approval now also clears any leftover `PendingRotationId`/`PendingRotationExpiresUtc`/`PendingReconfirmationRequired` as part of retiring the old binding, since a full credential replacement makes any prior rotation-tracking moot regardless. (2) §3b's recovery operation replaced `HostIdentity.CurrentCredentialRef` and republished the descriptor but never retired the displaced `Old` private key material — for suspected compromise specifically, this left a credential the Host itself just declared untrustworthy sitting recoverable as an orphan in `ISecureCredentialStore` indefinitely, serving no purpose once every peer must re-pair regardless. Fixed: `Old` is now retired crash-safely as a further step, verified by the same startup-reconciliation pass that already verifies the descriptor republish (round thirty-six); for credential loss, an already-missing key is treated as successful (idempotent) retirement, never an error. Testability items 18 and 33c extended for both.
- 2026-09-02 — a Codex review of PR #57 at HEAD `d766798` found two findings. (1) Fixed inline, no new invariant: §4a-i step 1 ("Prepare") had no guard against a second `HostCredentialRotation` being created for the same `HostId` while one was already nonterminal — a retried Owner request after a lost response, or two genuinely concurrent requests, could each pass this step, but each peer's `TrustedManagers` row has only one `PendingRotationId`/pending-fingerprint slot; staging the second rotation would silently overwrite each peer's record of the first, while the first `HostCredentialRotation` row remained durably present and eligible for cutover, letting a later cutover or startup reconciliation act on two conflicting credential pairs. Fixed the same way this document already treats every other one-time-initiation mechanism: preparing while a nonterminal rotation already exists is now an idempotent no-op, returning the existing rotation's identity rather than creating a second one; a genuinely new rotation still requires the existing one to actually resolve first. Testability item 34f added. (2) **Escalated, not fixed** — a genuine, pre-existing gap in §9's concurrency model, unrelated to anything #19 has touched this session: the only defined `OperationLock`/`OperationRecord` shape requires a `ServerRef`, so a Host-wide exclusive operation with no server target (`ManageHostUpdates` and other Host-wide maintenance) cannot acquire a lock at all — it either invents a fake server or runs unlocked, letting incompatible Host-wide operations overlap despite `OPS-002`. This is §9's own operation-locking/concurrency architecture (coordinating with [#26](https://github.com/shoemin/Palworld-Server-Manager/issues/26)'s open investigation), never touched, added to, or authorized for #19's Product Decisions (`IDENT-003`/`IDENT-004`/`LOCAL-003`/`PAIR-004`/`AUTH-005`/`LOCAL-004`/`OWNER-002`/`PLATFORM-002`), and defining a Host/global lock scope is a genuine architecture decision, not a bounded correction to any of them. Set #19 to Product Decision; PR #57 back to draft; this finding's thread replied-to but left unresolved; the fix above is committed and pushed regardless.
- 2026-09-02 — the escalated Host-wide operation-lock scope finding was accepted as a Product Decision and resolved. New invariant `OPS-004` added: durable operations have an explicit Host or Server target, and exclusive operation locks have an explicit Host or Server scope, independently of each other. §10's `OperationRecord` gains a discriminated `Target: OperationTarget = HostTarget { HostId } | ServerTarget { ServerRef }`, replacing the unconditional `Server: ServerRef` field — no fake/sentinel `ServerRef`, no nullable field, no inference from `Kind` alone. §9's `OperationLock` gains a separately discriminated `Scope: OperationLockScope = HostScope { HostId } | ServerScope { ServerRef }`, deliberately independent of the operation's own target — a server-targeted operation may still declare `HostExclusive` and hold a `HostScope` lock if it mutates a Host-shared resource. Fixed v0.5 conflict hierarchy, no shared/read/write modes: a `HostScope` conflicts with every `ServerScope` on that Host and every other `HostScope` for it; a `ServerScope` conflicts with its Host's `HostScope` and any other `ServerScope` for the exact same `ServerRef`; independent servers' `ServerScope`s never conflict. Every operation kind declares an explicit `LockRequirement: None | ServerExclusive | HostExclusive`, never inferred; acquisition is atomic (check-then-insert, one Host-owned transaction); remote operations still lock at the authoritative destination Host, never fabricated locally; `OperationLock`/`OperationRecord` lifetime and recovery semantics (owned by `OwningOperationId`, survives client disconnect and Host restart, resolved via `RecoveryDisposition`) extend unchanged to Host-wide operations. `HOST-001`, `HOST-002`, `IDENT-002`, `OPS-001`, `OPS-002`, `OPS-003`, `RECOVERY-001`, `REMOTE-001`, `PERSIST-001` are unweakened; `OPS-002` unchanged in substance, `OPS-004` makes its scope semantics concrete. Testability item 40 added (scenarios A–I).
- 2026-09-02 — a Codex review of PR #57 at HEAD `0ee8c8e` found one gap in the brand-new `OPS-004` mechanism — fixed inline, no new invariant. §9's atomic-acquisition bullet guaranteed atomicity only for the lock's own conflict-check-then-insert, leaving unstated whether the corresponding `OperationRecord` (§10) is created in that same transaction — a crash between two separate writes could leave an orphaned `OperationLock` with no `OperationRecord` for startup recovery to classify (blocking that scope indefinitely), or a seemingly-recoverable `OperationRecord` with no actual lock behind it (defeating `OPS-002`/`OPS-004`). Fixed: acquiring a lock and creating its owning operation's record are now explicitly the same transaction — no state exists where one is durable without the other. Testability item 40 extended with scenario J.
- 2026-09-02 — a Codex review of PR #57 at HEAD `7c7489c` found two more findings — fixed inline, no new invariants. (1) §8's "online two-sided unpair" had the initiator clear its own local pin and set `State = Revoked` *before* sending `RevokeTrust` — but doing so immediately destroys the initiator's own ability to authenticate outbound to that peer (§4c requires `Active` trust to route), so if the connection dropped or the initiator crashed after that local commit but before the peer ever received the notice, the peer remained `Active` with every grant intact indefinitely and the initiator had no way to ever retry delivery — a "two-sided" unpair silently degrading into a permanent one-sided one on a single lost message. Fixed with the identical durable-acknowledgment-before-finalizing pattern already established twice this session (§4a-i step 6's promotion acknowledgment; §4b-i step 7's recovery-completion acknowledgment): the initiator records revoke-intent and sends `RevokeTrust` while still `Active`, retries it opportunistically until the peer's acknowledgment is durably confirmed, and finalizes its own `State = Revoked` only then. (2) A gap in this same round's own `OPS-004` mechanism, mirroring the acquisition-atomicity fix from the round before: releasing an `OperationLock` and marking its owning `OperationRecord` terminal were never required to be the same transaction — a crash between them could leave a terminal record with its lock still present (invisible to §10's non-terminal-only startup scan, permanently blocking that scope) or a non-terminal record with no lock left (letting recovery resume it with no actual exclusion). Fixed the identical way acquisition already is: release and terminal resolution are now one atomic unit. Testability item 40 extended with scenario K; new item 41 added for the two-sided-unpair fix.
